# Vampire to Megalodon Proof Reconstruction: Expert Review Report

Date: 2026-07-18

Revision: late July 18 refresh after Skolem macro-edge child metadata

Requested path: `/project/reports/REPORT-2026-07-18.md`

Mirrored project path: `/project/Megalodon/reports/REPORT-2026-07-18.md`

Repositories reviewed:

- Megalodon: `/project/Megalodon`, branch `vampire/megalodon5`
- Vampire fork: `/project/vampire-leancheck`, branch `vampire/megalodon5`
- Temporary artifact root used for generated proof/search material: `/project/tmp`

This report is a consolidated handoff for expert review. It takes into account
the previous daily reports, the audit reports through
`audit-REPORT-2026-07-16.md`, the audit responses, the Prover9/Ivy analysis,
the July 16 and July 17 design documents, the Smolka-style transformation
discussion, and the current local branch state on July 18.

The report is deliberately not a success narrative. The project has made real
technical progress, but the original goal is not yet achieved. The strongest
recent progress is that the work has mostly stopped treating generated proof
scripts and Python translation as the intended final path; Vampire now emits
substantially richer Megalodon-oriented metadata, and Megalodon has a native
`Syntax.pf` checking path for several fragments. The main remaining concern is
that too much proof reconstruction is still being developed inside the large
Megalodon OCaml importer instead of through a small Prover9/Ivy-style
certificate calculus emitted by Vampire and checked by a small Megalodon
kernel-side elaborator.

## Executive Summary

The target remains:

```text
100 representative original Megalodon theorems, exported as THF, proved by
Vampire with a per-problem timeout no greater than 10 seconds, reconstructed
into Megalodon proof terms, and checked by Megalodon in the original source
context, with no aby, no admit, no incomplete QED, no hidden bridge premise, no
certificate-local trusted theorem, and no Python proof reconstruction on the
qualifying path.
```

That target has not been met.

The current branches contain several important building blocks:

- a Vampire-side `kernel_v1` metadata vocabulary;
- a Vampire-side `MegalodonKernelSyntax` boundary;
- explicit metadata for many major Vampire families, including source formula
  transforms, rectification, CNF clauses, definition folds, AVATAR components
  and split dependencies, URR traces, subsumption-resolution pivots, predicate
  definitions, and Skolemization;
- primitive-expansion metadata for some macro inferences;
- native Megalodon `Syntax.pf` construction for a core clausal fragment and
  selected preprocessing/AVATAR/Skolem fragments;
- stricter trust-boundary checks that reject certificate-derived `Known`
  declarations on counted proof-term paths unless explicitly running a
  structural diagnostic mode;
- source-context plumbing that can identify global facts, local hypotheses,
  definition facts, generated facts, unresolved facts, and set-reflexivity
  obligations;
- focused strict native successes on representative targets, including line
  172 and line 183 in `examples/hammer/100thms_12_h.mg`.

The current branches also contain major unfinished work:

- no passing E1 suite of 100 original-context Megalodon theorems;
- no completed small-kernel primitive IR fully separating Vampire macro
  expansion from Megalodon checking;
- no clean standalone Megalodon kernel checker module consuming that IR;
- no fully solved Skolemization story;
- no fully solved Smolka-style transformation pipeline;
- no complete source-context composition for arbitrary original Megalodon
  lemmas, definitions, local hypotheses, generated `set` equalities, and
  conjectures;
- significant remaining implementation mass in `src/vampire_cert_v1.ml`;
- a current focused blocker around Skolem CPS replay on line 233, where
  source witness ordering has been corrected but branch-local proposition
  binding and certificate-local choice elimination are still not fully
  correct;
- committed work that moves Skolem macro-edge metadata out of ad hoc Vampire
  string assembly and into the `MegalodonKernelSyntax` rendering boundary,
  including explicit helper-edge source/target decomposition and binder
  metadata consumed by Megalodon;
- late July 18 commits that add Vampire-emitted formula/source/target
  quantifier-sequence and free-variable metadata for Skolem macro edges, with
  Megalodon-side indexing and semantic validation and live metadata-audit
  coverage.

The best short summary is:

```text
The project has moved toward the right architecture, but the architecture is
not yet fully realized. Current evidence is a mixture of E3 focused proof-term
checking, E4 structural certificate evidence, and a few E1-like focused live
successes. It is not yet a general proof reconstruction system.
```

## Evidence Classes

The previous reports and audits exposed repeated confusion between structural
certificate validation and proof reconstruction. This report keeps the same
evidence classes used in the recent design documents:

```text
E1  Original-context proof reconstruction.
    A proof term is checked inside the original Megalodon development and
    proves the current theorem goal. No aby, no admit, no incomplete QED, no
    hidden bridge theorem, and no certificate-derived trusted theorem.

E2  Exported-THF-bound closed proof reconstruction.
    A native Megalodon proof term checks a Vampire certificate against the
    exported THF/source problem, but the proof has not yet been composed back
    into the original source theorem position.

E3  Primitive-kernel proof-term checking.
    Synthetic or focused live certificates exercise native proof-term
    elaboration for the small clausal or preprocessing kernel.

E4  Structural and diagnostic integration evidence.
    Vampire emits certificates; Megalodon parses them, validates structure,
    source maps, metadata, and trust policy, or runs transitional diagnostics.
    This is useful but is not proof reconstruction.
```

Only E1 satisfies the user's original goal.

The earlier broad `PASS 100` live runs should be classified as E4 unless the
run explicitly checks native proof terms and composes them into the original
Megalodon context. Likewise, preprocessing frontiers that rely on transitional
known implications or diagnostic environment variables are E4, not proof
reconstruction. Focused native proof-term checks are valuable, but they are E3
or E2 unless they discharge the original theorem in place.

## Current Repository State

At the time of writing, the relevant branches are:

```text
/project/Megalodon
  branch: vampire/megalodon5
  latest committed implementation head at report refresh:
          7c84fd5 Validate Skolem macro edge child metadata
  tracked status at report refresh:
          clean tracked status; generated untracked hammer/export/build
          artifacts remain present

/project/vampire-leancheck
  branch: vampire/megalodon5
  latest committed head at report refresh:
          18253e552 Emit Skolem macro edge child metadata
  tracked status at report refresh:
          clean tracked status; generated untracked object files and local
          binaries remain present
```

The most recent Megalodon commits relevant to this report are:

```text
7c84fd5 Validate Skolem macro edge child metadata
c3f5366 Seed Skolem shadow replay with formula assumptions
fd29191 Separate Skolem CPS formula assumption contract
705bc78 Refresh July 18 expert report after structured Skolem metadata
9517a39 Structure Skolem macro edge metadata in Megalodon
2e97ac4 Refresh July 18 expert report after free-variable validation
c39585d Validate Skolem free-variable metadata semantically
7f0f8ca Audit Skolem macro-edge free-variable metadata
3ead504 Audit Skolem formula free-variable metadata
17d6732 Refresh July 18 expert report after top-level Skolem metadata
4191f89 Consume top-level Skolem quantifier metadata
ab59484 Refresh July 18 expert report with quantifier metadata
a61a69b Validate Skolem macro edge quantifier metadata
3c831c1 Refresh July 18 expert report with shape metadata status
3bee88b Validate Skolem macro edge shape metadata
48f565d Refresh July 18 expert report after contract diagnostic
03a8eb7 Add Skolem continuation contract diagnostic
e79ff94 Refresh July 18 expert report after binder audit
e522f73 Consume Skolem macro edge binders
3bc06bd Require decomposed Skolem macro edges
8a23806 Preserve Skolem macro edge quantifiers
81eb2c3 Audit structured Skolem macro edges
a60026f Refresh July 18 expert report after shadow diagnostics
25cf3c7 Add Skolem shadow replay debug switch
30b94b5 Trace Skolem CPS result contracts
4a464a6 Refresh July 18 report after Skolem diagnostics
d1957d1 Trace Skolem CPS context mismatches
c3bc278 Refresh July 18 expert report
99290c3 Document Skolem CPS fail-fast debugging
cf657f6 Add fail-fast Skolem CPS diagnostics
19558bf Align Skolem CPS sibling propositions
7107c8e Add parallel native aby target replay harness
4d30f20 Refresh July 18 expert report with late status
db490b1 Order Skolem substitutions by source binders
ad7c7a1 Refresh July 18 expert report
edab99e Bind Skolem CPS result continuations
11a7138 Advance Skolem CPS proposition alignment
2383b10 Refresh July 18 expert report with CPS blocker
a121c6f Advance Skolem CPS branch refresh
1a0bddf Refine Skolem CPS sibling fallbacks
bc6a83d Advance AVATAR and Skolem cleanup replay
0a6d828 Advance Skolem witness and AVATAR split replay
3881abb Avoid duplicate Skolem body substitution
eefe821 Thread Skolem helper substitutions
f1ba48d Read explicit Skolem macro edge metadata
346d1d6 Add Skolem parent-helper replay fallback
b42937d Validate Skolem witnesses after binder erasure
0f854b8 Close Skolem CPS disjunction candidates
5a4e3fe Avoid native delta collisions in live proof expansion
30361f9 Close Skolem CPS generated propositions
d35a81f Abstract Skolem CPS epsilon witnesses in proof terms
```

The most recent Vampire commits relevant to the report are:

```text
18253e552 Emit Skolem macro edge child metadata
a78e7edc0 Emit Skolem macro-edge free-variable metadata
6e1da7b22 Emit Skolem formula free-variable metadata
38f0c9f42 Emit top-level Skolem quantifier metadata
ac4479a93 Emit Skolem macro edge quantifier metadata
e9815f20f Emit Skolem macro edge shape metadata
9718483d4 Emit Skolem macro edge binders
c7c18a6d3 Structure Skolem macro edge metadata
b92fe08a1 Emit Skolem macro edge metadata
0ebdf2a67 Emit SAT subsumption primitive metadata
1b8b5cd42 Emit rectify variable maps
d48f82edf Emit subsumption fast-path primitive results
e3b4a8620 Emit predicate definition primitive metadata
8215c1fc8 Emit subsumption primitive step metadata
162c4fc31 Emit URR primitive rule payload metadata
99f6cd21c Represent URR expansion steps as primitive records
2462f0477 Track URR primitive expansion steps directly
bb3aa602e Reject unsupported Megalodon kernel rules
cc64aa131 Represent AVATAR refutation metadata explicitly
1fbfd3199 Represent AVATAR split metadata explicitly
f4f4ecf59 Represent split dependency metadata explicitly
e03e87a83 Represent AVATAR definition metadata explicitly
96cce436a Represent AVATAR component metadata explicitly
```

The source-binder-order fix is committed. It scans the raw `source_formula`
S-expression for nested `vampire_exists_prop` binders before the Megalodon
parser erases names to de Bruijn indices, and then reorders Vampire's Skolem
substitution list into source-binder order before native Skolem replay. That
fix is narrow and validated on the current focused pass cases.

The later accepted Megalodon commits refine the Skolem CPS debugging path
rather than claiming a new solved inference family. They add:

- a parallel focused native `aby` target replay harness;
- a sibling-proposition alignment fix for one-sided Skolem conjunction
  branches;
- fail-fast Skolem CPS diagnostics, so a rejected CPS candidate can stop at
  the proof-checking mismatch instead of falling through to slow fallback
  replay;
- README documentation for the fail-fast debugging mode.
- proof/proposition mismatch tracing for Skolem CPS branches, including
  first-difference paths and proof/term context depths.
- a debug-only switch,
  `MEGALODON_CERT_DISABLE_SKOLEM_SHADOW=1`, that disables the Skolem shadow
  replay continuation so the generic abstraction path can be compared against
  the shadow fast path.

An earlier local experiment that removed one term-depth shift from a captured
left-branch proposition was discarded. It preserved the two focused passing
lines and changed the line-233 mismatch, but it did not close the proof. It is
not part of the current branch state and should not be counted as accepted
progress.

A later local experiment that blindly shifted the unchanged sibling proof when
the target sibling proposition was exactly a one-level term shift was also
discarded. It moved the mismatch outward into the continuation application
instead of closing the proof. The conclusion is that the branch proof and the
continuation proposition must be aligned by construction, not by an isolated
shift patch.

## July 18 Late Refresh

This report has been refreshed after the latest Skolem-source-order,
sibling-proposition, parallel-harness, fail-fast, and context-mismatch
diagnostic work. The important update is that the previous suspected
witness-order problem was real enough to justify a committed fix, but that fix
does not finish Skolemization. The focused line-233 failure is now faster and
more precisely localized.

The committed source-order work changes the Skolem preprocessing path in a
principled way:

- it consumes Vampire-emitted source formula metadata rather than guessing
  binder order from Megalodon's already-erased term representation;
- it looks specifically for source-level nested existential binders used by
  the Skolem macro encoding;
- it reorders the substitution entries by those source binder names before
  proof construction;
- it leaves already-correct substitution orders unchanged;
- it logs the reorder under certificate debug mode, which is useful for
  auditing line-233-style failures.

Validation for the committed state:

```text
TMPDIR=/project/tmp ./makeopt
  pass

TMPDIR=/project/tmp VAMPIRE=/project/vampire-leancheck/vampire_rel_vampire/megalodon5_11083 \
  JOBS=2 WALL_SECONDS=100 tests/vampire_reconstruction/run_native_aby_targets_parallel.sh
  pass for targets 172:4 and 183:4

TMPDIR=/project/tmp tests/vampire_certificate/run_native_cert_v1_smoke.sh
  pass

MEGALODON_CERT_DEBUG=1 MEGALODON_CERT_FAIL_FAST_SKOLEM_CPS=1 ...
  line 233 fails fast with a proof/proposition mismatch, not by timing out
```

The line 233 diagnostic after this commit confirms that the substitution order
is now corrected:

```text
u35: native core skolem reordered substitution by source binders
     X0,X1,X2,X3,X4,X6 -> X0,X1,X6,X2,X4,X3
```

However, the proof still does not check. The remaining failure is a
branch-local proof/proposition alignment issue in the Skolem CPS construction.
The newest diagnostic identifies the problem more specifically: in a
one-sided conjunction branch with `right_count > 0`, the unchanged-left
component proposition computed for the result side is exactly `tmshift +1` of
the source-side component proposition. The first difference is inside the
component body:

```text
first diff path: root.body.left.arg
left:  _3
right: _4
```

The final rejected application has the same shape at a deeper context:

```text
expected ... _6 ...
actual   ... _5 ...
first diff root.body.left.arg
```

The fallback path still contains certificate-only `Eps_prop`, and the stronger
CPS candidate still fails with a proof application mismatch. This is not a
Vampire-search problem and should not be debugged by rerunning Vampire
portfolio strategies. It should be debugged either by making the Skolem macro
certificate more explicit or by simplifying the Megalodon-side transformation
checker around a smaller typed Skolem proof object.

The new fail-fast mode is important operationally. Before it, line 233 could
spend much of its time falling through failed candidates into fallback
refutation replay. With
`MEGALODON_CERT_FAIL_FAST_SKOLEM_CPS=1`, the run exits quickly at the first
bad Skolem CPS proof application. That supports the user's request to iterate
on Megalodon proof replay rather than repeatedly paying for Vampire or
portfolio-like work.

The right conclusion from the latest diagnostics is that captured branch
proofs and captured branch propositions need an explicit context discipline.
That discipline should be specified before more local Skolem fixes are added.
The next implementation step should decompose the exact conjunction
proposition expected by the current continuation and use that as the contract
for rebuilding the branch proof, or replace this whole CPS reconstruction with
a first-class Skolem macro certificate emitted by Vampire.

## July 18 Final Refresh

The latest committed diagnostic pass narrows the line-233 Skolem issue one
step further. Commit `30b94b5` added debug-only conjunction-contract checks
around the one-sided Skolem CPS conjunction branches and a
`result-to-target` contract check comparing:

```text
the current CPS result proposition
vs.
the originally checked result proposition shifted to the current term depth
and rewritten through the active witness replacements
```

The important outcome is that the one-sided rebuilt conjunctions now check
against their local contracts. The earlier visible `tmshift +1` sibling
symptom was not the final cause. The remaining bad application is in the
shadow replay continuation/result-to-target boundary: the continuation was
checked against the closed `result_checked_prop`, but the CPS reconstruction
later rebuilds and replaces witness symbols under branch binders. For the
representative `u35` step, those operations do not commute; the mismatch still
appears as expected `_6` versus actual `_5` under the branch body.

Commit `25cf3c7` adds
`MEGALODON_CERT_DISABLE_SKOLEM_SHADOW=1`, a debug-only switch that disables
the shadow replay continuation. A focused run on line 233 with this switch
does not reach the same fail-fast proof mismatch. Instead, it times out in the
slower fallback path and logs the first stored formula proof containing the
certificate-local choice witness `Eps_prop`. This confirms two things:

- the shadow continuation is currently necessary for fast progress on line
  233;
- the shadow continuation cannot be accepted as-is, because its proof contract
  is not stable under the branch-local witness replacement and closing order.

Validation after the switch:

```text
TMPDIR=/project/tmp VAMPIRE=/project/vampire-leancheck/vampire_rel_vampire/megalodon5_11083 \
  JOBS=2 WALL_SECONDS=100 tests/vampire_reconstruction/run_native_aby_targets_parallel.sh
  172:4 PASS
  183:4 PASS

TMPDIR=/project/tmp tests/vampire_certificate/run_native_cert_v1_smoke.sh
  pass
```

This changes the recommended Skolem work. The next useful implementation is
not another sibling-shift patch. It is either:

- make the shadow replay continuation replacement-aware and check it against
  the same proposition contract used by `result_to_target_builder`; or
- replace this Megalodon-side CPS reconstruction with a Vampire-emitted
  first-class Skolem macro proof object that explicitly records the branch
  contracts, ordered binders, witness terms, dependencies, sorts, and macro
  edges.

The second option is more aligned with the Prover9/Ivy audit direction. The
first option may be a pragmatic intermediate step if it is kept small and
fails closed.

## July 18 Current Refresh: Structured Skolem Macro Edges

After the shadow-replay diagnostics, the newest committed work moved one part of the
Skolem certificate closer to the Prover9/Ivy-style boundary. The earlier
Vampire code emitted Skolem macro-edge fields by building raw strings inside
`MegalodonChecker.cpp`. The Vampire branch now introduces a structured
record in `Shell/MegalodonChecker/MegalodonKernelSyntax.*`:

```text
RenderedKernelSkolemMacroEdge
  index
  parent_index
  unit
  formula
  binders
  source
  target
```

`MegalodonKernelStep` now carries a `skolemMacroEdges` vector, and
`kernelStepFields` renders the canonical field family:

```text
skolem_macro_edge_count=N
skolem_macro_edge_i_parent_index=...
skolem_macro_edge_i_unit=...
skolem_macro_edge_i_binder_count=...
skolem_macro_edge_i_binder_j_var=...
skolem_macro_edge_i_binder_j_type=...
skolem_macro_edge_i_formula=...
skolem_macro_edge_i_source=...
skolem_macro_edge_i_target=...
```

This is not yet a typed internal Vampire proof IR, but it removes one piece
of direct string assembly from the main checker and gives the Megalodon audit
a stable field contract. It is therefore directionally aligned with the audit
recommendation: Vampire should print exact proof data while it still has the
formula, parent, and macro-edge information.

The same Vampire change also improves helper-edge decomposition. For
Skolem helper parents, the code now strips leading universal quantifiers
before testing whether the helper body is an implication, so records such as
`forall ..., source -> target` can expose both the macro-edge source and
target. The follow-up binder commit then records the stripped universal
binders explicitly as `skolem_macro_edge_i_binder_j_var` and
`skolem_macro_edge_i_binder_j_type`. Megalodon consumes those explicit binder
types when rebuilding quantified helper edges and falls back to the older
formula-prefix recovery only for certificates that do not yet carry binder
metadata.

The Megalodon audit script now checks this field family. For every
`rule=skolemize` record it requires `skolem_macro_edge_count`. For every
reported edge index it requires parent index, unit, formula, source, target,
and binder count. For nonzero binder counts it also requires every indexed
binder variable and type. This is stronger than the first version of the
audit, which only required at least one decomposed edge in the corpus, and it
makes the helper-edge contract suitable for direct Megalodon consumption
rather than heuristic parent-formula recovery.

Validation for this committed state:

```text
TMPDIR=/project/tmp make -j10 vampire_rel
  passed in /project/vampire-leancheck
  latest binder-aware binary:
    /project/vampire-leancheck/vampire_rel_vampire/megalodon5_11084

TMPDIR=/project/tmp VAMPIRE=/project/vampire-leancheck/vampire_rel_vampire/megalodon5_11083 \
  LIMIT=5 MIN_PASS=5 JOBS=5 WALL_SECONDS=30 VAMPIRE_SECONDS=10 \
  PROBLEMS_FILE=/project/Megalodon/tests/vampire_certificate/source_linked_strict_100.list \
  AUDIT_NATIVE_PRIMITIVES=0 AUDIT_KERNEL_V1_METADATA=0 \
  WORK_DIR=/project/tmp/source_linked_curated5_skolem_macro_edges_qimp_170051 \
  tests/vampire_certificate/run_source_linked_corpus_parallel.sh
  PASS 5

TMPDIR=/project/tmp MIN_REWRITE_POSITION=1 \
  tests/vampire_certificate/run_kernel_v1_metadata_audit.sh \
  /project/tmp/source_linked_curated5_skolem_macro_edges_qimp_170051
  passed; kernel_v1 records: 280; skolemize records: 5
  artifacts: /project/tmp/kernel_v1_metadata_audit.ddg3lG

TMPDIR=/project/tmp MIN_EQUALITY_SYMMETRY=0 \
  tests/vampire_certificate/run_native_primitive_audit.sh \
  /project/tmp/source_linked_curated5_skolem_macro_edges_qimp_170051
  passed; skolem_formula records: 5
  artifacts: /project/tmp/native_primitive_audit.ndMorT

TMPDIR=/project/tmp VAMPIRE=/project/vampire-leancheck/vampire_rel_vampire/megalodon5_11084 \
  LIMIT=5 MIN_PASS=5 JOBS=5 WALL_SECONDS=30 VAMPIRE_SECONDS=10 \
  PROBLEMS_FILE=/project/Megalodon/tests/vampire_certificate/source_linked_strict_100.list \
  AUDIT_NATIVE_PRIMITIVES=0 AUDIT_KERNEL_V1_METADATA=0 \
  WORK_DIR=/project/tmp/source_linked_curated5_macro_binders_171640 \
  tests/vampire_certificate/run_source_linked_corpus_parallel.sh
  PASS 5

TMPDIR=/project/tmp MIN_REWRITE_POSITION=1 \
  tests/vampire_certificate/run_kernel_v1_metadata_audit.sh \
  /project/tmp/source_linked_curated5_macro_binders_171640
  passed; kernel_v1 records: 280; skolemize records: 5
  artifacts: /project/tmp/kernel_v1_metadata_audit.J0b5oP

TMPDIR=/project/tmp MIN_EQUALITY_SYMMETRY=0 \
  tests/vampire_certificate/run_native_primitive_audit.sh \
  /project/tmp/source_linked_curated5_macro_binders_171640
  passed; skolem_formula records: 5
  artifacts: /project/tmp/native_primitive_audit.pItrGm

TMPDIR=/project/tmp VAMPIRE=/project/vampire-leancheck/vampire_rel_vampire/megalodon5_11084 \
  LIMIT=20 MIN_PASS=1 JOBS=10 WALL_SECONDS=90 VAMPIRE_SECONDS=10 \
  PROBLEMS_FILE=/project/Megalodon/tests/vampire_certificate/source_linked_strict_100.list \
  AUDIT_NATIVE_PRIMITIVES=0 AUDIT_KERNEL_V1_METADATA=0 \
  WORK_DIR=/project/tmp/source_linked_20_macro_binders_171841 \
  tests/vampire_certificate/run_source_linked_corpus_parallel.sh
  PASS 20

TMPDIR=/project/tmp MIN_REWRITE_POSITION=1 \
  tests/vampire_certificate/run_kernel_v1_metadata_audit.sh \
  /project/tmp/source_linked_20_macro_binders_171841
  passed; kernel_v1 records: 738; skolemize records: 18
  artifacts: /project/tmp/kernel_v1_metadata_audit.4GWXey

TMPDIR=/project/tmp MIN_EQUALITY_SYMMETRY=0 \
  tests/vampire_certificate/run_native_primitive_audit.sh \
  /project/tmp/source_linked_20_macro_binders_171841
  passed; skolem_formula records: 18
  artifacts: /project/tmp/native_primitive_audit.HG6VhB
```

The curated run consists of:

```text
hammer.1007.43.th0.p  PASS
hammer.1009.43.th0.p  PASS
hammer.10208.46.th0.p PASS
hammer.10269.46.th0.p PASS
hammer.10304.46.th0.p PASS
```

This evidence is E4/E3 metadata and primitive-record evidence, not E1
original-context proof reconstruction. The important result is not that
Skolemization is solved; it is that the Skolem certificate surface is becoming
more explicit and auditable. The 20-case run is valuable as a wider regression
for the new field contract: it covered 738 `kernel_v1` records, including 18
Skolemization records, 21 Skolem macro edges, and 19 explicit helper binders
with no missing binder-count fields. It still does not prove that the
Skolemization transformation is fully reconstructed in Megalodon.

Three local Megalodon-side semantic experiments were also tried and reverted:

- applying active witness replacements to captured unchanged sibling proofs
  before conjunction introduction;
- applying those replacements to the shifted result-to-target continuation;
- shifting the shadow continuation by `term_depth + 1`.

All three left the representative line-233 proof-checking mismatch in the
same family, with the final bad application still showing the expected/actual
de Bruijn index disagreement. That is useful negative evidence. The current
failure is not a simple omitted replacement or isolated ambient shift. It
continues to point toward a missing first-class Skolem macro proof object or
an explicitly replacement-aware shadow-continuation contract.

## July 18 Prior Diagnostic Commit

The prior committed Megalodon change, `03a8eb7`, did not claim to solve a
new proof family. It adds a fail-fast diagnostic for the Skolem
continuation/result contract. This is useful because the main current blocker
is no longer Vampire search or certificate generation. It is a native
Megalodon proof-term mismatch in the Skolem replay of line 233.

The new diagnostic introduces a checked replacement contract around the
Skolem result-to-target continuation. When enabled with
`MEGALODON_CERT_FAIL_FAST_SKOLEM_CONTRACT=1`, it compares the current CPS
result proposition against the originally checked target proposition after
applying the active witness replacements. If the contract does not hold, the
run stops at that mismatch instead of falling through to slower fallback
replay.

The focused line-233 run now fails quickly and informatively at step `u35`.
The certificate has:

```text
megalodon_step_variable_sorts(35,["X5:prop"])
```

but the emitted step proposition also contains an internal binder:

```text
forall X5:prop, ...
```

That means the variable metadata can cause Megalodon to add an outer
quantifier for `X5` even when `X5` is already bound inside the result formula.
The visible result is still the familiar expected/actual de-Bruijn mismatch
inside the continuation application, but the new diagnostic makes the likely
cause sharper: formula-step variables must be treated as syntactically free
variables of the parsed formula, not as every variable name Vampire reports
for the step.

This is not a one-off benchmark-name issue and not a case for Python
guessing. It is a binder-accounting issue at the certificate/checker
boundary. A conservative Megalodon-side experiment was tried: filter formula
step variables through actual formula occurrence after parsing, so variables
bound by `ALLV` are not re-closed as outer step variables. That experiment is
not in the branch. It regressed the already-passing line 172 target, while
line 183 still passed, and was discarded. The useful conclusion is that
variable discipline must come from a richer Vampire-emitted binder contract,
not from a broad Megalodon-side syntactic filter.

Validation for the committed `03a8eb7` state:

```text
TMPDIR=/project/tmp ./makeopt
  pass

TMPDIR=/project/tmp VAMPIRE=/project/vampire-leancheck/vampire_rel_vampire/megalodon5_11084 \
  JOBS=2 WALL_SECONDS=100 tests/vampire_reconstruction/run_native_aby_targets_parallel.sh
  172:4 PASS
  183:4 PASS

TMPDIR=/project/tmp tests/vampire_certificate/run_native_cert_v1_smoke.sh
  pass

MEGALODON_CERT_DEBUG=1 MEGALODON_CERT_FAIL_FAST_SKOLEM_CPS=1 \
MEGALODON_CERT_FAIL_FAST_SKOLEM_CONTRACT=1 ...
  line 233 fails quickly at the Skolem continuation contract around u35
```

Additional audits on the committed binder-aware corpus passed:

```text
/project/tmp/source_linked_20_macro_binders_171841
  kernel_v1 records: 738
  skolemize records: 18
  native skolem_formula records: 18
```

These checks are still E3/E4 evidence. They show that the new diagnostics and
macro-edge/binder metadata do not regress the focused passing cases or the
small metadata corpus. They do not establish E1 original-context proof
reconstruction.

## July 18 Superseding Refresh: Shape Metadata and Current Blocker

The committed work before the final quantifier-metadata refresh was the
Skolem macro-edge shape metadata pair:

```text
Vampire:
  e9815f20f Emit Skolem macro edge shape metadata

Megalodon:
  3bee88b Validate Skolem macro edge shape metadata
```

This is a small but important architectural correction. The previous
Skolem-macro edge fields gave Megalodon the edge formula, source, target, and
binders. The new fields also report a compact structural summary for each of
those formula positions:

```text
skolem_macro_edge_i_formula_connective
skolem_macro_edge_i_formula_exists_count
skolem_macro_edge_i_formula_forall_count
skolem_macro_edge_i_formula_and_count
skolem_macro_edge_i_formula_or_count
skolem_macro_edge_i_formula_imp_count

skolem_macro_edge_i_source_...
skolem_macro_edge_i_target_...
```

The Vampire implementation computes these summaries while it still has the
internal formula objects. The Megalodon implementation recomputes the same
shape from the parsed formula terms and validates the metadata contract in
strict `kernel_v1` mode. This follows the audit's direction: when Vampire
knows the exact proof/formula structure, it should print it, and Megalodon
should verify it rather than infer it by a heuristic.

There is one caveat in the current validator. Exact counts are required for
`exists`, `forall`, `and`, and `or`. The top connective is exact unless
Vampire classifies the formula as a `literal` or `bool_term`; those are
accepted as leaves because Vampire's internal formula representation can
treat Boolean terms as atoms while Megalodon's rendered proposition may expose
internal implication structure. For the same reason, `imp_count` is currently
required to be nonnegative rather than exact. This is a deliberate
compatibility compromise, not the final desired proof contract. The final IR
should make the Boolean/formula boundary explicit enough that implication
shape can also be checked exactly.

The C++ side also had to count n-ary conjunctions and disjunctions as the
number of binary nodes exported to Megalodon, namely `child_count - 1`. This
was caught by the validator: the first version undercounted conjunctions in
focused examples. That is a useful example of the intended development style:
add exact Vampire-emitted metadata, make Megalodon validate it, and let
focused failures expose representation mismatches.

Validation for the committed shape-metadata state:

```text
Megalodon build:
  TMPDIR=/project/tmp ./makeopt
  pass

Vampire build:
  TMPDIR=/project/tmp make -j10 vampire_rel
  pass

Focused native replay harness:
  TMPDIR=/project/tmp \
  VAMPIRE=/project/vampire-leancheck/vampire_rel_vampire/megalodon5_11085 \
  JOBS=3 WALL_SECONDS=120 \
  tests/vampire_reconstruction/run_native_aby_targets_parallel.sh

  172:4 PASS
  183:4 PASS
  PASS 2

Native certificate smoke:
  TMPDIR=/project/tmp tests/vampire_certificate/run_native_cert_v1_smoke.sh
  native certificate v1 smoke test passed
```

The same committed state still has a focused Skolem CPS blocker on line 233 of
`examples/hammer/100thms_12_h.mg`. With fail-fast diagnostics enabled, the
representative run fails at step `u35`:

```text
u35: native preprocess Skolem CPS and-right-count unchanged-left
     proposition mismatch at term_depth=4 proof_depth=8
     right is tmshift +1 of left

u35: native preprocess Skolem CPS candidate did not check; failing fast
Failure at line 233 char 4:
  Vampire native certificate did not reconstruct current proof goal
```

Without `MEGALODON_CERT_FAIL_FAST_SKOLEM_CPS=1`, the same target reaches the
existing fallback path and reports:

```text
u35: native preprocess Skolem CPS candidate did not check; keeping original
     refutation
Vampire native certificate found native refutation for target.
returns native false: yes
```

That non-fail-fast run then timed out because it continued through expensive
debug/fallback work. It should not be counted as an E1 success. It is evidence
that the rest of the local refutation can be navigated when the strict Skolem
CPS proof is not required, but the actual proof-producing Skolem transport is
still wrong for this case.

Several local experiments were made after `3bee88b`; they were discarded and
are not counted as validated progress:

- a diagnostic comparison between captured and recomputed unchanged sibling
  propositions in one-sided conjunction branches;
- a trial helper that shifts an unchanged sibling proof when the result-side
  sibling proposition is exactly one term-shift above the source-side sibling
  proposition;
- a direct-shadow candidate behind
  `MEGALODON_CERT_TRY_DIRECT_SKOLEM_SHADOW=1`;
- a second direct-shadow experiment that also allows local choice witnesses
  through `MEGALODON_CERT_ALLOW_DIRECT_SKOLEM_CHOICE=1`.

The sibling-shift trial did not fix line 233. It moved the first visible
failure from a conjunction contract into a later implication application,
where the expected/actual proof terms differed by a de Bruijn index under the
branch body. This suggests that blind local shifting is the wrong abstraction.
The branch proof and continuation proposition have to be aligned by a single
explicit contract.

The direct-shadow trial also did not close the case. Without allowing choice
witnesses, it was rejected because the candidate still contained
certificate-local `Eps_prop` and the fixed choice theorem
`vampire_exists_prop_choice`. When choice witnesses were temporarily allowed,
the replay reached the empty clause but the candidate still contained
introduced Skolem symbols. That means direct shadow replay currently proves
the wrong boundary proposition for the final qualifying path: it can carry the
refutation through, but it does not eliminate the local Skolem symbols back to
the original formula context.

The practical conclusion is:

```text
Skolemization is now the clearest blocker. The certificate is richer and more
auditable, but the Megalodon-side CPS/shadow replay is still too implicit.
The next robust step is a first-class Vampire-emitted Skolem transformation
object with branch propositions, binders, dependencies, witness terms, sorts,
choice/epsilon use, and macro-edge contracts made explicit.
```

This is exactly where the Prover9/Ivy recommendation should be applied. The
remaining work should not be a growing OCaml search procedure that tries to
guess which term shift or branch replacement Vampire intended. Vampire should
emit the small proof object, and Megalodon should check it.

## July 18 Closing Refresh: Quantifier Metadata

The latest implementation work adds one more explicit contract to the Skolem
macro-edge certificate. The immediate motivation was a failed Megalodon-side
experiment: filtering Skolem result step variables by syntactic occurrence in
the parsed formula looked plausible, because line 233 had shown duplicated
internal binders, but it regressed the already-passing line 172 target. That
negative result matters. It shows that Megalodon should not infer Skolem
variable discipline from partial parsed-term heuristics. Vampire should emit
the binder/quantifier facts it used.

The Vampire branch now has:

```text
ac4479a93 Emit Skolem macro edge quantifier metadata
```

This adds a structured `RenderedKernelQuantifiedVariable` record to
`Shell/MegalodonChecker/MegalodonKernelSyntax.hpp` and attaches three
quantifier lists to each `RenderedKernelSkolemMacroEdge`:

```text
formulaQuantifiedVariables
sourceQuantifiedVariables
targetQuantifiedVariables
```

The renderer emits these canonical fields:

```text
skolem_macro_edge_i_formula_quantifier_count=N
skolem_macro_edge_i_formula_quantifier_j_kind=forall|exists
skolem_macro_edge_i_formula_quantifier_j_var=X...
skolem_macro_edge_i_formula_quantifier_j_type=(...)

skolem_macro_edge_i_source_quantifier_...
skolem_macro_edge_i_target_quantifier_...
```

The quantifier list is computed in Vampire while it still has internal
formula objects. It traverses universal and existential binders, implications,
binary/n-ary Boolean structure, and negation. This is still metadata, not a
complete Skolem proof object, but it is the correct kind of metadata:
Vampire-owned, typed, indexed, and attached to the exact helper formula,
source, and target fields that Megalodon consumes.

The matching Megalodon commit is:

```text
a61a69b Validate Skolem macro edge quantifier metadata
```

Megalodon now recomputes the quantifier sequence from the raw S-expression
for each Skolem macro-edge formula/source/target and validates:

- quantifier count;
- quantifier kind;
- variable name, including anonymous binders where applicable;
- binder type.

The validation is backward-compatible for older certificates: if the new
`*_quantifier_count` field is absent, the validator does not reject the
certificate only for that reason. The live metadata audit, however, now
requires the new quantifier fields on regenerated Skolem macro-edge records.
That gives future runs a concrete freshness check and prevents silently
falling back to old, under-informative Skolem certificates.

Validation for the final implementation state covered both builds and the
focused certificate paths:

```text
Vampire build:
  TMPDIR=/project/tmp make -j10 vampire_rel
  pass
  latest binary:
    /project/vampire-leancheck/vampire_rel_vampire/megalodon5_11086

Megalodon build:
  TMPDIR=/project/tmp ./makeopt
  pass

Native certificate smoke:
  TMPDIR=/project/tmp tests/vampire_certificate/run_native_cert_v1_smoke.sh
  native certificate v1 smoke test passed

Focused native replay:
  TMPDIR=/project/tmp \
  VAMPIRE=/project/vampire-leancheck/vampire_rel_vampire/megalodon5_11086 \
  JOBS=3 WALL_SECONDS=120 \
  tests/vampire_reconstruction/run_native_aby_targets_parallel.sh

  172:4 PASS
  183:4 PASS
  PASS 2

Five-problem source-linked corpus:
  TMPDIR=/project/tmp \
  VAMPIRE=/project/vampire-leancheck/vampire_rel_vampire/megalodon5_11086 \
  LIMIT=5 MIN_PASS=5 JOBS=5 WALL_SECONDS=30 VAMPIRE_SECONDS=10 \
  PROBLEMS_FILE=/project/Megalodon/tests/vampire_certificate/source_linked_strict_100.list \
  AUDIT_NATIVE_PRIMITIVES=0 AUDIT_KERNEL_V1_METADATA=0 \
  WORK_DIR=/project/tmp/source_linked_quant_fields_182727 \
  tests/vampire_certificate/run_source_linked_corpus_parallel.sh

  PASS 5

Fresh metadata audit over that corpus:
  TMPDIR=/project/tmp MIN_REWRITE_POSITION=1 \
  tests/vampire_certificate/run_kernel_v1_metadata_audit.sh \
  /project/tmp/source_linked_quant_fields_182727

  kernel_v1 records: 280
  skolemize 5
  pass

Native primitive audit over that corpus:
  TMPDIR=/project/tmp MIN_EQUALITY_SYMMETRY=0 \
  tests/vampire_certificate/run_native_primitive_audit.sh \
  /project/tmp/source_linked_quant_fields_182727

  skolem_formula 5
  pass
```

The same final implementation state still fails the line-233 strict Skolem
CPS target:

```text
MEGALODON_CERT_DEBUG=1
MEGALODON_CERT_FAIL_FAST_SKOLEM_CPS=1
... -vampireabytarget 233 4 ...

u35: native preprocess Skolem CPS using shadow replay continuation
u35: native preprocess Skolem CPS and-right-count unchanged-left
     proposition mismatch; right is tmshift +1 of left
u35: native preprocess Skolem CPS candidate did not check; failing fast
```

This is the honest final state for July 18. The new quantifier metadata
improves the certificate contract and reduces the need for Megalodon-side
guessing. It does not solve Skolemization. The next technically sound step is
still a first-class Vampire-emitted Skolem transformation proof object, or at
least a replacement-aware continuation contract whose binders, witnesses, and
choice dependencies are all explicit in the certificate.

## Background and Original Approach

The original assignment was to use the Vampire repository's Lean proof
reconstruction code as a starting point and port the idea to Megalodon.
Megalodon and Lean are morally similar in the relevant sense: both can check
explicit proof terms. Therefore the intended final direction was never merely
to print a human-readable script or to rely on an external Python translator.
The intended direction was:

```text
Vampire proof object
  -> rich certificate / proof term data
  -> Megalodon proof term
  -> Megalodon checker validates it
```

Early work did not follow that path strictly enough. It leaned on Python
translation, generated Megalodon scripts, `claim` skeletons, `admit`, `aby`,
and library-specific name recovery. Those artifacts were useful for exploring
the shape of the problem and for giving a human something readable, but they
were not a credible final solution. The user repeatedly clarified this point:

- final scripts cannot use `aby`, because `aby` is implemented as an admit;
- the proof must be connected to original Megalodon lemmas, definitions,
  local hypotheses, and conjectures;
- classical logic and `xm` are available and should be assumed;
- Megalodon library logic, including `or`, `False`, and equality, should be
  used rather than private replacements;
- broad tests must run in parallel and Vampire must not be rerun repeatedly on
  unsolved problems;
- Vampire should emit richer proof data rather than forcing Megalodon or
  Python to guess missing sorts, substitutions, literal positions, binders,
  or witness dependencies.

The audits strongly reinforced that correction. The current branch reflects a
substantial pivot, but not a completed one.

## Audit History

The audit reports were unusually important in this project because they caught
several ways the implementation could look successful while still being
logically non-qualifying.

The July 12 audit rejected the first Python/script-oriented direction as a
final architecture. It was useful as a skeleton generator, but it relied on
`admit`, `aby`, bridge assumptions, and heuristic source recovery. It did not
give an auditable kernel of reconstructed proof terms.

The July 13 audit pushed the project toward reading Prover9/Ivy and thinking
in terms of small proof objects. The key lesson was that complex prover
inferences should be lowered into a small set of simple, explicit proof
objects. The checker should verify the small objects, not reconstruct a large
prover's internal search retrospectively.

The July 14 and July 15 audits criticized continued broad growth in the
Megalodon-side importer and emphasized that source linking and Smolka-style
transformations were underdeveloped. They also pointed out that structural
metadata validation was being overcounted as proof reconstruction.

The July 16 audit was the most consequential. It accepted the native
`Syntax.pf` seed as real progress, but found that the preprocessing proof-term
frontier had recreated the old bridge assumption problem in a hidden form:
certificate-derived propositions were installed dynamically as `Known`
theorems and then applied. That is not reconstruction. It is only harder to
see than an explicit bridge axiom.

The accepted audit correction is:

```text
No counted proof-term path may install certificate-derived Known propositions.
Unsupported transformations must fail closed, or run only in explicitly marked
structural diagnostic mode.
```

The current branch has moved in that direction. There is now a
`MEGALODON_CERT_ALLOW_TRANSITIONAL_PREPROCESS_KNOWN=1` escape hatch for
diagnostics, and counted modes reject certificate-derived `vampire_*` knowns
except for the reviewed fixed logical basis. This is a material trust-boundary
improvement, though it does not by itself solve reconstruction.

## Prover9/Ivy Direction

The Prover9/Ivy analysis changed the desired architecture. The useful idea is
not to reproduce Prover9 literally, but to copy the design discipline:

```text
complex prover proof
  -> expanded sequence of simple inferences
  -> small proof object
  -> small independent checker
```

For this project, that means Vampire should expand or explain its macro
inferences before printing the certificate. Megalodon should receive explicit
primitive data:

- parent step ids;
- selected literals;
- selected equality sides;
- rewrite position;
- rewrite direction;
- explicit substitutions;
- variable and symbol sorts;
- source formula and result formula;
- introduced Skolem symbols and dependencies;
- AVATAR split and SAT proof details where applicable.

The Megalodon side should then check these objects with small, typed rules. It
should not search for missing pivots, infer binder mappings from raw strings,
guess Skolem witness order from formula shape, or hardwire library theorem
names.

The current Vampire branch is closer to this direction than before.
`Shell/MegalodonChecker/MegalodonKernelSyntax.*` defines a schema named
`prover9-small-kernel-v1`, supported rule contracts, primitive expansion
metadata, and rendered structures for source transforms, rectification,
AVATAR, SAT proof steps, subsumption, Skolem introduced symbols, and other
families. This is a real architectural improvement.

However, this is still not a fully separated primitive proof IR. Much of the
data is still emitted as compatibility metadata around existing Vampire proof
printing. Some macro families expose primitive-expansion summaries, but the
Megalodon checker does not yet consume a complete first-class primitive proof
stream end to end. The expert should view this as a strong start toward the
Prover9/Ivy path, not as evidence that the path is complete.

## Small Kernel Target

The stable clausal kernel should remain small. The current consensus is that
the core should include approximately:

- `input`, as a boundary from source/preprocessing, not as a logical inference;
- `instantiate` or `substitute`, with explicit substitution and sorts;
- `resolve`, binary resolution with explicit pivot data;
- `factor`, duplicate literal contraction;
- `equality_resolution`, contradiction from negative reflexive equality;
- `equality_factoring`, equality-specific factoring;
- `paramodulate`, equality rewriting at an explicit position and direction;
- `contradiction`, final empty-clause marker.

Some names require care:

- `rename` should be checked as alpha-conversion or represented as a special
  case of instantiation, not become an unrestricted logical rule;
- `truth_conflict` probably belongs in the FOOL/preprocessing layer unless real
  clausal certificates show it must be primitive;
- AVATAR should not be hidden inside arbitrary bridge implications. It should
  be checked through an explicit SAT/RUP-style layer or lowered to explicit
  primitive proof objects with a reviewed trust contract.

The current branch has native proof-term logic for pieces of this, but the
implementation is not yet isolated enough. Much of it is inside
`src/vampire_cert_v1.ml`, which also contains parsing, metadata validation,
legacy textual replay, source-map handling, preprocessing reconstruction, and
debug diagnostics. The audits' recommendation to stop monolithic growth has
not been fully realized.

## What Has Been Implemented

The implementation work falls into several streams.

### 1. Vampire Certificate Enrichment

The Vampire fork now emits much richer Megalodon-specific certificate
metadata. Important additions include:

- a `MegalodonKernelSyntax` C++ layer for schema names, supported rules, and
  rendering of structured metadata;
- supported-rule rejection for unsupported `kernel_v1` rule names;
- primitive-expansion chain metadata for URR and related macro inferences;
- explicit AVATAR component, split, refutation, definition, and split
  dependency metadata;
- SAT proof step metadata for parts of the AVATAR path;
- source formula transformation records;
- rectify variable maps and renaming metadata;
- CNF clause metadata;
- definition fold metadata;
- predicate definition metadata;
- subsumption-resolution pivot and fast-path primitive metadata;
- Skolem introduced-symbol metadata, witness sort metadata, dependency
  metadata, and macro-edge metadata.

The Skolem macro-edge addition is especially relevant. Previously Megalodon
was trying to infer helper edges by inspecting generic parent formulas. Vampire
now emits explicit fields such as:

```text
skolem_macro_edge_count
skolem_macro_edge_i_source
skolem_macro_edge_i_target
```

This is exactly the kind of "Vampire knows the information, so Vampire should
print it" correction requested during the project.

The latest committed refinement moves this field family into the
`MegalodonKernelSyntax` rendering boundary as a structured
`RenderedKernelSkolemMacroEdge` record. This is still rendered metadata rather
than a complete typed primitive IR, but it is better than embedding
Skolem-specific string construction directly in `MegalodonChecker.cpp`.
The corresponding Megalodon audit now requires indexed macro-edge fields for
Skolem records and catches missing parent/unit/formula entries.

### 2. Megalodon Native Proof-Term Checking

Megalodon now has a native certificate path that constructs `Syntax.pf` terms
and checks them with Megalodon's own proof checker. This path is separate from
the original generated-script translator and from Python-generated skeletons.

Implemented or partially implemented fragments include:

- clausal core proof-term checking for resolution-like fragments;
- selected equality reasoning including equality resolution, equality
  factoring, symmetry, and paramodulation fragments;
- source/context basis transport for live Vampire proofs;
- source obligation tracking;
- local definition and local hypothesis recognition in live source context;
- native set-reflexivity handling for equalities generated by Megalodon's
  `set` command;
- AVATAR component and restricted SAT binary-resolution replay;
- negative AVATAR split cleanup in focused cases;
- Skolem proof-term construction using classical choice and epsilon witnesses;
- Skolem helper formula replay;
- Skolem CPS continuation construction for existential elimination through
  conjunction and disjunction;
- debug and audit passes that reject leaked introduced Skolem symbols, split
  symbols, and certificate-only constants in counted modes.

This is not all complete, but it is real proof-term work rather than textual
script generation.

### 3. Trust-Boundary Hardening

The branch now distinguishes counted proof-term modes from transitional
diagnostic modes. The main trust hardening includes:

- strict native live mode no longer treats absence of a usable certificate as
  success;
- certificate-derived dynamic `Known` facts are rejected on counted
  proof-term paths by default;
- the old transitional preprocessing known behavior requires
  `MEGALODON_CERT_ALLOW_TRANSITIONAL_PREPROCESS_KNOWN=1`;
- final-refutation cleanup candidates are checked before being kept;
- proof-term traversal rejects unauthorized certificate-local constants whose
  names start with `vampire_`, except for fixed reviewed logical basis names;
- source facts are categorized so that unresolved source obligations are not
  silently counted as discharged proofs.

This was necessary after the July 16 audit. It is still not sufficient for E1,
but it prevents a major class of false positives.

### 4. Source Linking Work

Some source linking now exists. Megalodon can distinguish:

- global known facts;
- local hypotheses;
- local definitions;
- generated facts;
- unresolved facts;
- set-command reflexivity obligations.

The implementation can sometimes compose a certificate-derived proof back into
the live context and has focused successes on line 172 and line 183. It also
avoids one earlier unsoundness where certificate-helper delta entries could
overwrite live Megalodon definitions.

However, source linking is still incomplete. A general E1 proof needs a
well-defined source-context contract:

```ocaml
type source_proof =
  | GlobalKnown of string * Syntax.tm
  | LocalHyp of int * Syntax.tm
  | Definitional of Syntax.tm * Syntax.pf
  | Generated of Syntax.tm * Syntax.pf
  | SetReflexivity of Syntax.tm
  | Unresolved of string * Syntax.tm

type source_context = source_id -> source_proof
```

The actual type names do not matter. What matters is that every TPTP source
axiom has an auditable route back to the original Megalodon development:

- a theorem or lemma already in the environment;
- a local proof hypothesis at the correct de Bruijn depth;
- a local definition unfolded by conversion;
- a `set`-generated equality proved by reflexivity/conversion;
- a generated Smolka-style transformation proof;
- the negated conjecture handled only at the final refutation-to-goal
  composition boundary.

This remains a major unfinished part of the project.

### 5. Smolka-Style Transformation Work

The Smolka-style transformation layer is the right way to think about the path
from original higher-order formulas to the clausal forms consumed by the
kernel. It should be separate from the small clausal kernel. It includes:

- source formula copying;
- formula-term copying;
- definition input/fold/unfold;
- rectification;
- FOOL Boolean lifting;
- FOOL exhaustiveness;
- equality-to-truth and truth-to-proposition conversions;
- ENNF;
- CNF formula and literal projection;
- Skolemization;
- predicate definitions;
- possibly AVATAR boundary facts.

The branch contains partial metadata and partial proof-term handling for many
of these transformations. It does not yet contain a clean, complete,
Smolka-style transformation checker. Some transformations are replayed with
proof terms; others are structurally validated; others still depend on local
heuristics or focused-case logic.

The expert should pay particular attention to whether each transformation is
being proved or merely accepted because its shape matches expected metadata.
Shape validation is useful, but it is E4. For E1/E2, each transformation must
produce a checked proof term or be lowered into smaller checked
transformations.

## Current Focus: Skolemization

Skolemization is currently the most active technical area and the most visible
blocker.

The desired proof principle is classical. Since the Megalodon development is
classical and `xm` is available, the reconstruction can use choice/epsilon
principles. The final code should use Megalodon's library equality and logical
connectives rather than private substitutes.

The core problem is that Vampire's Skolemization step can replace nested
existentials across formulas with conjunctions, disjunctions, implications,
universals, and local branch structure. Megalodon must reconstruct a proof
that transports the source formula to the Skolemized result without leaking
introduced Skolem symbols or certificate-only epsilon helpers into the final
live proof.

Recent implemented pieces include:

- proof-level witness replacement;
- generated-CPS-proposition closure under the correct term depth;
- source witness sort fallback;
- binder-erasure witness validation;
- parent-helper implication replay;
- Skolem macro-edge metadata read from Vampire;
- structured Skolem macro-edge metadata rendering in Vampire's
  `MegalodonKernelSyntax` layer, implemented and audited;
- threading accumulated Skolem-symbol-to-epsilon-witness replacements through
  helper matching and implication construction;
- sibling fallback refinement for branch cases;
- final cleanup attempts for AVATAR split symbols and nullary `sK*` symbols;
- result-continuation binding for result step variables;
- source-formula binder-name scanning for nested `vampire_exists_prop`;
- source-binder-ordering of Skolem substitutions before native replay.

Focused line 172 and line 183 pass under strict native live replay. This is a
good sign: the implementation can reconstruct some real original-context
Skolem-heavy examples without `aby` or `admit`.

Line 233 remains the current focused blocker. Its recent history is:

- earlier failures exposed a Skolem substitution mismatch at step `u35`;
- after witness registration and negative AVATAR split replay, line 233
  reached a native refutation;
- after checked AVATAR/sK cleanup, visible `split_*` and nullary `sK*` symbols
  were no longer the first live validation failure;
- the fallback path still leaves a certificate-only `Eps_prop` helper;
- the stronger CPS candidate is rejected by Megalodon's proof checker;
- recent diagnostics show a branch-local proof/proposition mismatch, with
  expected and actual proof variables differing in a deep application path;
- witness ordering was one real cause: Vampire's raw substitution order for
  the representative step was observed as `X0,X1,X2,X3,X4,X6`, while the
  source existential order was `X0,X1,X6,X2,X4,X3`;
- that ordering issue is now addressed by the committed source-binder-order
  patch, but the deeper branch-local CPS proposition alignment issue remains.

The committed source-binder-order fix reads the raw `source_formula` metadata
to recover binder names before Megalodon's parser erases them to de Bruijn
indices, then reorders the Skolem substitution by source binder order. This is
directionally better than guessing from formula shape, because it uses
information Vampire already prints. It is still not a complete Skolemization
solution, because line 233 continues to expose context alignment and
certificate-local-choice issues after the ordering mismatch is removed.

The larger lesson is not "add one more Skolem heuristic." The lesson is that
Vampire should emit a first-class Skolem transformation proof object with
ordered binders, introduced symbols, witness terms, dependencies, sorts, and
macro edges. Megalodon should check that object directly.

The current structured macro-edge work is a step toward that object, not the
object itself. It records which helper parents form the macro boundary,
decomposes helper implications into source/target edges, and now emits
explicit helper binder names and types. It still does not give Megalodon a
complete Skolem proof contract with branch propositions, closing order, choice
principle instantiation, and proof continuation obligations.

## Equality

The equality situation has improved conceptually but remains worth expert
review. The final proof reconstruction should use Megalodon's equality, not a
private equality invented for the certificate path.

Some internal code still uses helper recognizers and wrappers around
Megalodon equality forms because the TPTP/Vampire certificate formulas arrive
through a normalized representation. That is acceptable as parsing and
orientation infrastructure, but it is not acceptable if it becomes a second
trusted equality theory.

The intended invariant is:

```text
All equality reasoning in counted proof terms eventually elaborates to the
Megalodon equality and Megalodon's equality eliminators/conversion principles.
```

The expert should check whether any remaining helper equality symbol is only a
parser/intermediate notation or whether it survives as a trusted logical
constant. Surviving private equality would be a serious design defect.

## Original Lemmas, Definitions, and TPTP Names

Source linking has two distinct problems:

1. recover the original Megalodon names, source positions, local contexts, and
   definitions from exported TPTP names;
2. prove or resolve every exported source axiom in Megalodon.

The name recovery problem is partly solved by studying the Megalodon exporter
and by using metadata. Mangled TPTP names can often be mapped back to original
Megalodon names. However, name recovery alone is not proof reconstruction. It
only identifies where the proof obligation should come from.

The second problem is the real trust boundary. Each TPTP axiom must be one of:

- an original lemma/theorem already known in Megalodon;
- a local claim/hypothesis in the active proof context;
- an unfolding or definitional equality;
- a generated equality from `set`, proved by reflexivity/conversion;
- a generated transformation fact with an explicit proof term;
- a conjecture-negation assumption introduced only for refutation and then
  discharged at the final goal boundary.

The user specifically noted that some TPTP axioms come from Megalodon's `set`
command and may not correspond to claims. These should not be looked up as
lemmas. They should be proved directly by reflexivity or conversion. The
branch has code in that direction, but it should be tested on deliberately
chosen examples with local `set`s and definition-heavy contexts.

At present, the focused line 172 and line 183 successes show that source
composition can work in some cases. They do not prove that arbitrary original
Megalodon developments are handled.

## Python Translator Status

The Python translator and generated script path should be considered legacy
exploration and debugging infrastructure. It can still be useful for:

- quickly inspecting proof skeletons;
- producing claim/admit outlines for human reading;
- comparing source names and TPTP names;
- triaging broad certificate corpora.

It should not be part of the qualifying path. A proof that depends on Python
to guess inference details, reconstruct terms, or generate trusted proof
steps is not the desired Vampire-to-Megalodon reconstruction.

This was a major criticism in the audits, and it remains an important guardrail
because Python is a tempting place to patch over missing certificate data.
Missing data should be emitted by Vampire, not guessed in Python.

## Testing Work

Several kinds of tests and artifacts have been used:

- generated THF problems under `examples/hammer`;
- Vampire proof outputs under `examples/hammer/out` and `examples/hammer/out1`;
- focused `-vampireabytarget` runs against
  `examples/hammer/100thms_12_h.mg`;
- native certificate smoke tests under `tests/vampire_certificate`;
- broad strict live certificate runs with parallelism;
- cached closed certificate corpora and layer audits;
- debug runs for individual failing lines such as 172, 183, and 233.

The correct testing discipline is now clear:

- export all relevant problems once;
- run Vampire in parallel, typically with 10 to 20 jobs depending on the
  machine;
- cap Vampire at 10 seconds per problem;
- record which THF problems Vampire can solve once, then iterate only on that
  solvable subset;
- avoid repeated expensive Vampire portfolio runs when debugging Megalodon
  proof replay;
- run focused Megalodon replay on cached/current certificates while developing;
- run broad tests only at milestone boundaries or regression points;
- keep temporary generated material under `/project/tmp`.

The project has sometimes violated this discipline in practice, especially
early on, by running sequential or repeated unsuccessful Vampire tests. The
later focused workflow is much better.

The current focused regression set should include at least:

- line 172 strict native live replay, expected pass;
- line 183 strict native live replay, expected pass;
- line 233 strict native live replay, current expected fail until Skolem/CPS
  is fixed;
- the native certificate smoke script;
- a source-linking set with at least one global theorem, one local hypothesis,
  one local definition, one `set` equality, and one conjecture-negation
  discharge;
- a small live exported-certificate E2 set that excludes transitional knowns.

The newest focused validation artifacts are:

```text
/project/tmp/source_linked_curated5_macro_binders_171640
  PASS 5
  skolem_formula primitive records: 5
  kernel_v1 records: 280
  kernel_v1 skolemize records: 5

/project/tmp/source_linked_20_macro_binders_171841
  PASS 20
  skolem_formula primitive records: 18
  kernel_v1 records: 738
  kernel_v1 skolemize records: 18
  Skolem macro edges: 21
  explicit helper binders: 19
  missing binder-count fields: 0
```

The lightweight audits run on those artifacts passed:

```text
/project/tmp/kernel_v1_metadata_audit.J0b5oP
/project/tmp/native_primitive_audit.pItrGm
/project/tmp/kernel_v1_metadata_audit.4GWXey
/project/tmp/native_primitive_audit.HG6VhB
```

This should be treated as a focused metadata/primitive-record regression for
Skolem macro-edge emission. It is not a broad reconstruction result and should
not replace the held-out 100-theorem E1 gate.

Before claiming broad progress again, the branch should define a held-out
suite of at least 100 original-context theorems and report E1/E2/E3/E4 counts
separately.

## Current Line-233 Diagnostic

The current line-233 command shape is:

```sh
work=/project/tmp/line233_<tag>_$(date +%H%M%S)
mkdir -p "$work/out"
MEGALODON_CERT_DEBUG=1 MEGALODON_CERT_FAIL_FAST_SKOLEM_CPS=1 \
TMPDIR=/project/tmp timeout 80s \
  /project/Megalodon/bin/megalodon -v 4 -allowincompleteqed \
  -vampireaby /project/vampire-leancheck/vampire_rel_vampire/megalodon5_11084 \
  -vampireabytimeout 10 -vampireabyproof megalodon \
  -vampireabynative -vampireabynativestrict \
  -vampireabytarget 233 4 -vampireabytargetstop \
  -vampireabyoutdir "$work/out" \
  /project/Megalodon/examples/hammer/100thms_12_h.mg \
  >"$work/run.out" 2>"$work/run.err"
```

Recent focused line-233 diagnostics showed:

- Vampire finds a native refutation for the target;
- live fallback validation rejects a certificate-only `Eps_prop`;
- Skolem CPS debug reports active witness refreshes in branch/base cases;
- final CPS candidate fails proof checking with a deep first-difference path;
- the committed source-order patch corrects the visible raw substitution order
  disagreement;
- the committed sibling-proposition patch preserves the line 172 and line 183
  strict native passes, but does not close line 233;
- fail-fast mode now turns the line 233 problem into a quick proof-checking
  diagnostic rather than a long timeout;
- conjunction-contract diagnostics now show that the one-sided rebuilt
  conjunctions satisfy their immediate local contracts;
- the remaining visible mismatch is in the Skolem shadow replay
  continuation/result-to-target proof contract, not in Vampire search and not
  in a simple witness ordering issue;
- disabling shadow replay avoids the immediate mismatch but falls back to the
  slow certificate-local `Eps_prop` path and times out, so shadow replay is
  currently necessary but insufficiently disciplined.

This is a good focused failure because it is no longer wasting time in Vampire
search. It is inside Megalodon proof replay. It also illustrates why the
project needs first-class Vampire-emitted proof objects for Skolemization:
source binder order, witness order, dependency order, and branch proposition
binding must be explicit.

## Main Problems

### Problem 1: The Project Is Not Yet E1

The original goal is original-context reconstruction. A proof checked only
against exported THF, or a structurally valid certificate, is not enough.

The branch has focused live successes, but not a representative 100-theorem
E1 suite. Any expert assessment should keep this distinction central.

### Problem 2: Skolemization Is Still Too Ad Hoc

The Skolem/CPS work is sophisticated, but much of it is still in the form of
local replay logic in `vampire_cert_v1.ml`. It handles real cases and exposes
real proof obligations, but it is hard to audit and brittle around branch-local
witnesses.

The better endpoint is a Vampire-emitted Skolem transformation proof object:

- ordered source binders;
- introduced symbols;
- replaced variables;
- witness terms;
- dependency terms;
- sorts;
- macro-edge source and target formulas;
- branch structure;
- choice principle used.

Megalodon should check that object, not rediscover it.

### Problem 3: Smolka-Style Transformations Are Not Yet Modular

FOOL, ENNF, rectification, CNF projection, predicate definitions,
definition-folding, and Skolemization are separate transformation families.
They should not be mixed into the clausal kernel, and they should not be
accepted by broad bridge propositions.

The current code has useful metadata and partial proof-term replay, but the
transformation checker is not yet a clean component.

### Problem 4: The Megalodon Importer Is Still a Monolith

The audits recommended stopping growth in the OCaml importer and moving toward
smaller modules such as:

- `vampire_kernel_syntax.ml`;
- `vampire_kernel_check.ml`;
- `vampire_kernel_elab.ml`;
- `vampire_source_context.ml`;
- `vampire_transform_check.ml`.

Some files and boundaries exist conceptually, but the main work still happens
inside `src/vampire_cert_v1.ml`. This is a maintainability and auditability
risk.

### Problem 5: Vampire IR Is Still Transitional

Vampire now has an explicit syntax/rendering boundary, which is good. But the
expert should check how much of it is a real internal primitive proof IR and
how much is structured printing around existing proof steps.

The desired Vampire-side invariant is:

```text
macro inference handler -> primitive step records -> canonical printer
```

Direct string assembly and compatibility-only metadata should shrink over
time. Unsupported rules should fail closed before certificate emission.

### Problem 6: Source Context Is Still Underspecified

The final theorem proof depends on more than the external certificate. It
depends on the original Megalodon context. The system still needs a clearer
contract for:

- global theorem hashes and names;
- local hypothesis indices;
- local `set` definitions;
- local definitions and conversion;
- theorem binder scopes;
- conjecture negation and final refutation discharge;
- generated transformation facts.

Line 172 and 183 show that this can work, but they are not enough to establish
generality.

## Suggested Architecture From Here

The recommended architecture is:

```text
Megalodon source theorem/context
  -> THF export with source ids and enough context metadata
  -> Vampire search
  -> Vampire primitive certificate IR
  -> canonical certificate S-expression
  -> Megalodon certificate parser
  -> small structural checker
  -> native Syntax.pf elaborator
  -> source-context composition
  -> checked proof of original Megalodon theorem
```

The implementation should enforce the following boundaries.

### Boundary A: Source Context

This module knows how exported sources map back to the original Megalodon
development. It should prove `set` equalities by reflexivity/conversion and
resolve local/global facts without inventing new assumptions.

### Boundary B: Transformation Checker

This module checks Smolka-style formula transformations. It is allowed to use
classical principles and library logic. It should produce checked proof terms,
not dynamic known propositions.

### Boundary C: Clausal Kernel

This module checks the small clausal kernel: instantiate, resolve, factor,
equality resolution, equality factoring, paramodulation, and contradiction.
It should consume explicit pivots, positions, directions, substitutions, and
sorts from Vampire.

### Boundary D: AVATAR/SAT Layer

This module handles AVATAR explicitly, either through a small SAT/RUP checker
or through a documented lowering to primitive proof objects. It should not be
a bridge theorem.

### Boundary E: Final Composition

This module converts a refutation of the negated conjecture into a proof of
the original theorem goal in the current Megalodon proof context.

The code should be organized so that each boundary can be reviewed
independently.

## Recommended Milestones

The milestone order matters. The project should not chase broad pass counts
until the trust boundaries are clean.

### Milestone 1: Freeze Counted Trusted-Known Paths

Keep the current policy that counted proof-term paths reject certificate-local
knowns. Add regression tests that intentionally try to install a
certificate-derived known proposition and verify rejection.

Exit criterion:

```text
No counted E1/E2/E3 path can use certificate-derived Known propositions.
```

### Milestone 2: Finish the Vampire Primitive IR Boundary

Move from metadata-as-rendered-fields toward first-class primitive records for
the frequent macro families:

- resolution;
- unit-resulting resolution;
- subsumption resolution;
- superposition/paramodulation;
- substitution/instantiation;
- factoring;
- Skolemization.

Exit criterion:

```text
Vampire emits primitive records with explicit parents, substitutions, sorts,
literals, positions, directions, and conclusions. Unsupported records fail
closed.
```

### Milestone 3: Extract the Megalodon Kernel Checker

Move the small kernel elaboration out of the monolithic importer without
adding new broad replay behavior during extraction.

Exit criterion:

```text
The small kernel can parse and check primitive certificates in isolated modules
and produce native Syntax.pf proof terms.
```

### Milestone 4: Make Skolemization a First-Class Transformation

Stop relying on branch-local witness-order heuristics. Use Vampire-emitted
Skolem transformation data.

Exit criterion:

```text
Desired exit criterion: line 172 and line 183 remain passing, and line 233
newly passes strict native live replay without certificate-only Eps_prop/sK/split
leaks, and without transitional knowns.
```

### Milestone 5: Implement Source-Context Composition on a Designed Set

Build a small hand-picked E1 suite containing:

- one global theorem;
- one local hypothesis;
- one local definition;
- one `set` equality;
- one definition unfold/fold;
- one Skolemized theorem;
- one AVATAR proof;
- one equality-heavy proof;
- one superposition/paramodulation proof;
- one conjecture-negation final discharge.

Exit criterion:

```text
At least 10 examples pass as true E1.
```

### Milestone 6: Run the 100-Theorem Gate

Only after the above milestones, run the broad suite:

- export once;
- run Vampire in parallel with timeout <=10 seconds;
- reconstruct from cached/generated certificates;
- check in Megalodon in parallel;
- report E1/E2/E3/E4 separately;
- do not count diagnostic modes.

Exit criterion:

```text
100 representative original-context E1 proofs pass.
```

## What the Expert Should Review

The expert review should focus on these questions.

1. Is the `MegalodonKernelSyntax` layer in Vampire a good basis for a real
   primitive IR, or is it still too close to formatted metadata?

2. Does any counted Megalodon proof-term path still install
   certificate-derived known propositions under another name?

3. Are the current native proof constructors using Megalodon's real equality,
   `False`, `or`, implication, universal quantification, and classical
   principles, or do any private logical constants survive into trusted proof
   terms?

4. Is the Skolem CPS approach worth finishing as an intermediate path, or
   should it be stopped in favor of a first-class Vampire-emitted Skolem proof
   object?

5. What is the smallest credible Smolka-style transformation checker that can
   support the current hammer corpus?

6. What concrete source-context API should Megalodon expose during export so
   reconstruction can return to the original theorem cleanly?

7. Which currently passing focused tests are E1, which are E2/E3, and which
   are only E4?

8. How should AVATAR be represented: SAT/RUP proof, primitive lowering, or a
   separately checked AVATAR layer?

9. What code should be deleted or frozen to avoid another round of monolithic
   importer growth?

10. Are the current reports overclaiming any evidence?

## July 18 Final Addendum: Top-Level Skolem Quantifier Metadata

After the previous July 18 refresh, one more scoped implementation step was
completed and committed in both repositories:

```text
Vampire:
  38f0c9f42 Emit top-level Skolem quantifier metadata

Megalodon:
  4191f89 Consume top-level Skolem quantifier metadata
```

The change is small in code size but important for the direction of the
project. The earlier Skolem macro-edge contract exposed quantified-variable
metadata for each helper edge. The new Vampire commit also emits quantifier
sequences for the top-level Skolem source formula and result formula:

```text
source_formula_quantifier_count
source_formula_quantifier_i_kind
source_formula_quantifier_i_var
source_formula_quantifier_i_type

result_formula_quantifier_count
result_formula_quantifier_i_kind
result_formula_quantifier_i_var
result_formula_quantifier_i_type
```

This matters because Megalodon was still using a raw S-expression scan of
`source_formula` to recover the order of source existential binders. That scan
was a local heuristic. The Megalodon commit now consumes the Vampire-emitted
`source_formula_quantifier_*` fields first and only falls back to the old scan
for backwards compatibility with older certificates. The metadata audit also
requires and validates the new top-level fields when Skolem records are
present.

This is directly aligned with the audit recommendation to stop rediscovering
facts in Python or large Megalodon heuristics when Vampire already knows the
typed formula structure. It does not yet create the full Prover9/Ivy-style
Skolem primitive proof object, but it moves one more binder-order decision
from Megalodon guessing into Vampire's certificate payload, where it can be
audited.

The validation performed after these commits was:

```text
Vampire build:
  TMPDIR=/project/tmp make -j10 vampire_rel
  pass

Megalodon build:
  TMPDIR=/project/tmp ./makeopt
  pass

Focused native replay:
  TMPDIR=/project/tmp \
  VAMPIRE=/project/vampire-leancheck/vampire_rel_vampire/megalodon5_11087 \
  JOBS=3 WALL_SECONDS=120 \
  tests/vampire_reconstruction/run_native_aby_targets_parallel.sh

  172:4 PASS
  183:4 PASS
  PASS 2

Native certificate smoke:
  TMPDIR=/project/tmp tests/vampire_certificate/run_native_cert_v1_smoke.sh
  native certificate v1 smoke test passed

Source-linked five-case corpus:
  TMPDIR=/project/tmp \
  VAMPIRE=/project/vampire-leancheck/vampire_rel_vampire/megalodon5_11087 \
  LIMIT=5 MIN_PASS=5 JOBS=5 WALL_SECONDS=30 VAMPIRE_SECONDS=10 \
  PROBLEMS_FILE=/project/Megalodon/tests/vampire_certificate/source_linked_strict_100.list \
  AUDIT_NATIVE_PRIMITIVES=0 AUDIT_KERNEL_V1_METADATA=0 \
  WORK_DIR=/project/tmp/source_linked_top_quant_report_184211 \
  tests/vampire_certificate/run_source_linked_corpus_parallel.sh

  hammer.1007.43.th0.p  PASS
  hammer.1009.43.th0.p  PASS
  hammer.10208.46.th0.p PASS
  hammer.10269.46.th0.p PASS
  hammer.10304.46.th0.p PASS
  PASS 5

Metadata audit on that corpus:
  TMPDIR=/project/tmp MIN_REWRITE_POSITION=1 \
  tests/vampire_certificate/run_kernel_v1_metadata_audit.sh \
  /project/tmp/source_linked_top_quant_report_184211

  kernel_v1 records: 280
  skolemize records: 5
  pass

Primitive audit on that corpus:
  TMPDIR=/project/tmp MIN_EQUALITY_SYMMETRY=0 \
  tests/vampire_certificate/run_native_primitive_audit.sh \
  /project/tmp/source_linked_top_quant_report_184211

  skolem_formula records: 5
  pass
```

The known line-233 blocker was also reproduced after this change:

```text
MEGALODON_CERT_DEBUG=1 MEGALODON_CERT_FAIL_FAST_SKOLEM_CPS=1 \
TMPDIR=/project/tmp timeout 80s /project/Megalodon/bin/megalodon \
  -v 4 -allowincompleteqed \
  -vampireaby /project/vampire-leancheck/vampire_rel_vampire/megalodon5_11087 \
  -vampireabytimeout 10 \
  -vampireabyproof megalodon \
  -vampireabynative \
  -vampireabynativestrict \
  -vampireabytarget 233 4 \
  -vampireabytargetstop \
  -vampireabyoutdir /project/tmp/line233_top_quant_after_184251/out \
  /project/Megalodon/examples/hammer/100thms_12_h.mg

  rc=1
```

The failure remains the same family:

```text
u35: native preprocess Skolem shadow replay reached empty clause
u35: native preprocess Skolem CPS using shadow replay continuation
u35: native preprocess Skolem CPS result-to-target
     replaced-checked/current proposition mismatch
native preprocess final refutation proof debug:
  Proof term for an implication applied to a proof term for the wrong proposition
expected ... _6 ...
actual   ... _5 ...
u35: native preprocess Skolem CPS candidate did not check; failing fast
```

The latest metadata therefore does not solve line 233. It does, however, make
the remaining diagnosis cleaner. For the failing `u35` step, Vampire now
prints the top-level quantifier sequences explicitly. The source formula has
the order:

```text
exists X0, exists X1, exists X6, forall X5, exists X2, exists X4, exists X3
```

and the result formula has:

```text
forall X5
```

The surviving mismatch is not an unknown source-existential order anymore. It
is a branch/continuation contract problem: the current CPS construction
checks a result-to-target continuation under one binder/proof context and
then applies it under another after witness replacement and conjunction
composition. This is exactly the kind of case where the next step should be a
first-class Skolem macro certificate object rather than more broad
Megalodon-side proof-term surgery.

## July 18 Final Late Addendum: Skolem Formula Free-Variable Metadata

After the top-level quantifier metadata commits, one further scoped
implementation step added Vampire-emitted free-variable metadata for the
top-level source and result formulas of Skolemization records:

```text
source_formula_free_variable_count
source_formula_free_variable_i_var
source_formula_free_variable_i_type

result_formula_free_variable_count
result_formula_free_variable_i_var
result_formula_free_variable_i_type
```

The Vampire-side change adds a typed-variable record to
`MegalodonKernelSyntax`, computes free variables with Vampire's
`SortHelper::collectVariableSorts(..., ignoreBound=true)`, sorts them by
Vampire variable id, and renders them through the existing structured
`kernel_v1` boundary. The Megalodon-side change validates the indexed
`*_var` and `*_type` fields in strict metadata audit mode.

This is a deliberately narrow piece of Vampire-side information export. It is
not a new Megalodon-side reconstruction heuristic. Its purpose is to tell
Megalodon which Vampire step variables are genuinely free in the source/result
formula and which variables merely occur as binders under quantifiers. That
distinction is needed for the remaining Skolem CPS continuation issue.

This direction was chosen after a discarded Megalodon-only experiment. That
experiment tried to filter Skolem result step variables by syntactic occurrence
inside the result formula. It regressed the known passing line-172 focused
target, because a variable can be both a binder in one subformula and free in
another. The lesson is important: Megalodon should not infer these facts from
fragile local scans when Vampire can emit the typed free-variable set directly.

Intermediate validation for the top-level free-variable metadata state, before
the later macro-edge free-variable and semantic-validation commits:

```text
Vampire build:
  TMPDIR=/project/tmp make -j10 vampire_rel
  pass

Megalodon build:
  TMPDIR=/project/tmp ./makeopt
  pass

Focused native replay:
  TMPDIR=/project/tmp \
  VAMPIRE=/project/vampire-leancheck/vampire_rel_vampire/megalodon5_11088 \
  JOBS=3 WALL_SECONDS=120 \
  tests/vampire_reconstruction/run_native_aby_targets_parallel.sh

  172:4 PASS
  183:4 PASS
  PASS 2

Native certificate smoke:
  TMPDIR=/project/tmp tests/vampire_certificate/run_native_cert_v1_smoke.sh
  native certificate v1 smoke test passed

Source-linked five-case corpus:
  TMPDIR=/project/tmp \
  VAMPIRE=/project/vampire-leancheck/vampire_rel_vampire/megalodon5_11088 \
  LIMIT=5 MIN_PASS=5 JOBS=5 WALL_SECONDS=30 VAMPIRE_SECONDS=10 \
  PROBLEMS_FILE=/project/Megalodon/tests/vampire_certificate/source_linked_strict_100.list \
  AUDIT_NATIVE_PRIMITIVES=0 AUDIT_KERNEL_V1_METADATA=0 \
  WORK_DIR=/project/tmp/source_linked_free_vars_185456 \
  tests/vampire_certificate/run_source_linked_corpus_parallel.sh

  hammer.1007.43.th0.p  PASS
  hammer.1009.43.th0.p  PASS
  hammer.10208.46.th0.p PASS
  hammer.10269.46.th0.p PASS
  hammer.10304.46.th0.p PASS
  PASS 5

Metadata audit on that corpus:
  TMPDIR=/project/tmp MIN_REWRITE_POSITION=1 \
  tests/vampire_certificate/run_kernel_v1_metadata_audit.sh \
  /project/tmp/source_linked_free_vars_185456

  kernel_v1 records: 280
  skolemize records: 5
  pass

Primitive audit on that corpus:
  TMPDIR=/project/tmp MIN_EQUALITY_SYMMETRY=0 \
  tests/vampire_certificate/run_native_primitive_audit.sh \
  /project/tmp/source_linked_free_vars_185456

  skolem_formula records: 5
  pass
```

The known line-233 blocker was reproduced again with the new Vampire binary:

```text
MEGALODON_CERT_DEBUG=1 MEGALODON_CERT_FAIL_FAST_SKOLEM_CPS=1 \
TMPDIR=/project/tmp timeout 80s /project/Megalodon/bin/megalodon \
  -v 4 -allowincompleteqed \
  -vampireaby /project/vampire-leancheck/vampire_rel_vampire/megalodon5_11088 \
  -vampireabytimeout 10 \
  -vampireabyproof megalodon \
  -vampireabynative \
  -vampireabynativestrict \
  -vampireabytarget 233 4 \
  -vampireabytargetstop \
  -vampireabyoutdir /project/tmp/line233_free_vars_185529/out \
  /project/Megalodon/examples/hammer/100thms_12_h.mg

  rc=1
```

For the failing `u35` Skolem step, the new metadata reports:

```text
source_formula_free_variable_count=0
result_formula_free_variable_count=0
```

This is a useful negative result. It shows that the variable currently
appearing in the failed proof/proposition comparison is not a free step
variable of the top-level source or result formula. It is a bound-variable /
continuation-context problem. The next implementation step should therefore
use this metadata to control the Skolem continuation contract, not to mutate
the ordinary Skolem formula proof path globally.

## July 18 Closing Addendum: Committed Free-Variable Metadata

The final local state for July 18 commits the free-variable metadata work on
both sides and extends it beyond the top-level Skolem source/result formulas
to the decomposed Skolem macro edges.

The Vampire-side commits are:

```text
6e1da7b22 Emit Skolem formula free-variable metadata
a78e7edc0 Emit Skolem macro-edge free-variable metadata
```

These commits add a typed rendered-variable structure to
`MegalodonKernelSyntax` and emit explicitly indexed free-variable lists for:

```text
source_formula
result_formula
skolem_macro_edge_i_formula
skolem_macro_edge_i_source
skolem_macro_edge_i_target
```

The implementation uses Vampire's own sort information through
`Kernel::SortHelper::collectVariableSorts(formula, collectedSorts,
ignoreBound=true)`, then sorts variables by Vampire variable id and renders
both the variable name and type through the same `kernel_v1` metadata boundary
used by the rest of the Megalodon certificate printer. This is intentionally
not a Megalodon-side guessing pass.

The Megalodon-side commits are:

```text
9517a39 Structure Skolem macro edge metadata in Megalodon
3ead504 Audit Skolem formula free-variable metadata
7f0f8ca Audit Skolem macro-edge free-variable metadata
c39585d Validate Skolem free-variable metadata semantically
```

The last commit is important. The metadata audit no longer only checks that
the indexed fields are present and contiguous. It also parses the raw
certificate S-expressions, computes the free Vampire variables while respecting
`ALLV`, `LAMV`, `VLAMV`, and the encoded existential form
`(AP (TMH "vampire_exists_prop") (LAMV|VLAMV ...))`, and compares the result
against the emitted metadata. Variable types are still checked syntactically
as present and parseable; the semantic comparison is currently on names and
ordering, not inferred type equality.

This is a useful trust-boundary improvement. It means the checker is not
blindly accepting a Vampire-emitted free-variable list that could silently
disagree with the formula payload. It is still not a Skolem proof replay
solution. Its value is to make the next Skolem macro object precise enough
that Megalodon can distinguish theorem-wrapper variables, branch-local
bound variables, and genuinely free step variables.

The final Megalodon commit in this refresh structures the Skolem macro-edge
metadata as typed OCaml records rather than repeatedly re-reading flat fields
at each use site. The important local types are:

```text
native_kernel_typed_variable
native_kernel_quantifier
native_skolem_macro_edge
```

This does not reduce the size of the importer yet, but it makes the
Skolemization surface less ad hoc and gives the next checker/extraction step a
clearer local object to consume. It is aligned with the audit's recommendation
only as an incremental cleanup: the desired endpoint is still a separate
small-kernel module and first-class Vampire primitive/macro proof objects, not
further growth of the monolithic importer.

The accepted validation for this final committed state was:

```text
Vampire build:
  TMPDIR=/project/tmp make -j10 vampire_rel
  pass
  binary: /project/vampire-leancheck/vampire_rel_vampire/megalodon5_11089

Megalodon build:
  TMPDIR=/project/tmp ./makeopt
  pass

Focused native replay:
  TMPDIR=/project/tmp \
  VAMPIRE=/project/vampire-leancheck/vampire_rel_vampire/megalodon5_11089 \
  JOBS=3 WALL_SECONDS=120 \
  tests/vampire_reconstruction/run_native_aby_targets_parallel.sh

  172:4 PASS
  183:4 PASS
  PASS 2
  artifacts: /project/tmp/native_aby_targets.EMzpK4

Source-linked five-case corpus:
  TMPDIR=/project/tmp \
  VAMPIRE=/project/vampire-leancheck/vampire_rel_vampire/megalodon5_11089 \
  LIMIT=5 MIN_PASS=5 JOBS=5 WALL_SECONDS=30 VAMPIRE_SECONDS=10 \
  PROBLEMS_FILE=/project/Megalodon/tests/vampire_certificate/source_linked_strict_100.list \
  AUDIT_NATIVE_PRIMITIVES=0 AUDIT_KERNEL_V1_METADATA=0 \
  WORK_DIR=/project/tmp/source_linked_edge_freevars_190930 \
  tests/vampire_certificate/run_source_linked_corpus_parallel.sh

  hammer.1007.43.th0.p  PASS
  hammer.1009.43.th0.p  PASS
  hammer.10208.46.th0.p PASS
  hammer.10269.46.th0.p PASS
  hammer.10304.46.th0.p PASS
  PASS 5

Metadata audit on that corpus:
  TMPDIR=/project/tmp MIN_REWRITE_POSITION=1 \
  tests/vampire_certificate/run_kernel_v1_metadata_audit.sh \
  /project/tmp/source_linked_edge_freevars_190930

  kernel_v1 records: 280
  skolemize records: 5
  pass
  artifacts: /project/tmp/kernel_v1_metadata_audit.HfYFkc

Primitive audit on that corpus:
  TMPDIR=/project/tmp MIN_EQUALITY_SYMMETRY=0 \
  tests/vampire_certificate/run_native_primitive_audit.sh \
  /project/tmp/source_linked_edge_freevars_190930

  skolem_formula records: 5
  pass
  artifacts: /project/tmp/native_primitive_audit.krfufY

Native certificate smoke:
  TMPDIR=/project/tmp tests/vampire_certificate/run_native_cert_v1_smoke.sh
  pass
  artifacts: /project/tmp/native_cert_v1.kvNF9t
```

A local replay experiment using the new top-level free-variable metadata to
drop theorem-level wrappers in the Skolem CPS proposition was tested and
reverted. More precisely, Vampire was briefly changed so formula units used
`collectVariableSorts(..., ignoreBound=true)` for global
`step_variable_sorts`. That produced local binary
`/project/vampire-leancheck/vampire_rel_vampire/megalodon5_11090`, but the
experiment is not in the source branch and should not be treated as canonical.
The focused replay result was:

```text
172:4 FAIL
183:4 PASS
```

The line-233 diagnostic still failed in the original-goal reconstruction path.
The generated certificate showed that `u35` no longer emitted
`step_variable_sorts`, and the top-level source/result formula free-variable
counts were still both zero. This confirmed that changing the global
`step_variable_sorts` semantics breaks existing source-goal glue without
solving the Skolem branch contract problem.

An earlier Megalodon-only replay experiment using the same idea also failed.
In that line-233 diagnostic run, the shadow replay failed earlier at `u43`
with:

```text
native preprocess Skolem shadow built an ill-formed proof term:
Proof term does not prove a universally quantified proposition but is applied
to a term
```

That failed experiment is a strong design signal. The free-variable metadata
should be consumed through a first-class Skolem macro-edge proof object and a
branch contract, not by globally changing theorem-wrapper application in the
current CPS/shadow pipeline.

The current concrete line-233 blocker with canonical binary `11089` remains
step `u35`. The shadow replay reaches the empty clause, but the Skolem CPS
candidate does not check. The key final proposition mismatch is:

```text
expected: forall _:(prop -> (prop -> prop)),
          (((_0 (forall _:prop, (_0 -> _0))) _6) ->
           ((_0 _6) (forall _:prop, (_0 -> _0))))

actual:   forall _:(prop -> (prop -> prop)),
          (((_0 (forall _:prop, (_0 -> _0))) _5) ->
           ((_0 _5) (forall _:prop, (_0 -> _0))))
```

For that step, structured metadata says:

```text
step_variable_sorts: X5:prop
source_formula_free_variable_count: 0
result_formula_free_variable_count: 0
skolem_macro_edge_count: 5
```

The source formula has seven reported quantifiers, including six existential
binders and one universal binder. The result formula has one universal binder.
All five macro edges report zero free variables in their source/target
formulas. The practical conclusion is that the failed proof is not missing a
free result variable. It is mis-accounting for a branch-local bound variable
while applying the CPS continuation. The next proof-producing work should
therefore be local to the Skolem CPS/macro-edge contract, not a global
variable-sort policy change.

## July 18 Closing Addendum: Separate Skolem CPS Formula Assumptions

After the structured Skolem metadata refresh, one more committed Megalodon
change was made:

```text
fd29191 Separate Skolem CPS formula assumption contract
```

This change addresses a specific trust-boundary and binder-contract issue in
the Skolem CPS replay code. The earlier implementation treated the checked
Skolem result proposition and the formula-level Skolem assumption proposition
as if they were the same contract. On simple examples that was harmless. On
line 233, that conflation was visible in the debug trace as a
`result-to-target replaced-checked/current proposition mismatch`. The new
commit separates these two notions:

```text
result_checked_prop
  the legacy checked proposition for the Skolem result step, using the normal
  step-variable wrapper policy;

result_assumption_prop
  the proposition used when the Skolem formula is assumed inside the local
  refutation replay, with step variables recomputed from the formula-level
  result payload.
```

The implementation adds a formula-level preprocessing helper:

```text
native_preprocess_step_formula_prop_with_step_variables
native_preprocess_step_formula_prop
native_core_skolem_result_formula_step_variables
```

and extends `native_core_skolem_refutation_cps_proof` with optional
formula-assumption arguments:

```text
?result_assumption_prop
?result_assumption_step_variables
```

This is not a new trusted axiom. It is a contract separation inside the native
proof-term construction path. Its purpose is to stop using a theorem-wrapper
view of the Skolem step where the local shadow proof really needs a
formula-assumption view.

Validation for the committed `fd29191` state:

```text
Megalodon build:
  TMPDIR=/project/tmp ./makeopt
  pass

Focused native replay:
  TMPDIR=/project/tmp \
  VAMPIRE=/project/vampire-leancheck/vampire_rel_vampire/megalodon5_11089 \
  JOBS=3 WALL_SECONDS=120 \
  tests/vampire_reconstruction/run_native_aby_targets_parallel.sh

  172:4 PASS
  183:4 PASS
  PASS 2
  artifacts: /project/tmp/native_aby_targets.GejNq3

Source-linked five-case corpus:
  TMPDIR=/project/tmp \
  VAMPIRE=/project/vampire-leancheck/vampire_rel_vampire/megalodon5_11089 \
  LIMIT=5 MIN_PASS=5 JOBS=5 WALL_SECONDS=30 VAMPIRE_SECONDS=10 \
  PROBLEMS_FILE=/project/Megalodon/tests/vampire_certificate/source_linked_strict_100.list \
  AUDIT_NATIVE_PRIMITIVES=0 AUDIT_KERNEL_V1_METADATA=0 \
  WORK_DIR=/project/tmp/source_linked_cps_contract_194147 \
  tests/vampire_certificate/run_source_linked_corpus_parallel.sh

  hammer.1007.43.th0.p  PASS
  hammer.1009.43.th0.p  PASS
  hammer.10208.46.th0.p PASS
  hammer.10269.46.th0.p PASS
  hammer.10304.46.th0.p PASS
  PASS 5

Metadata audit:
  TMPDIR=/project/tmp MIN_REWRITE_POSITION=1 \
  tests/vampire_certificate/run_kernel_v1_metadata_audit.sh \
  /project/tmp/source_linked_cps_contract_194147

  kernel_v1 records: 280
  skolemize records: 5
  pass
  artifacts: /project/tmp/kernel_v1_metadata_audit.i79huB

Primitive audit:
  TMPDIR=/project/tmp MIN_EQUALITY_SYMMETRY=0 \
  tests/vampire_certificate/run_native_primitive_audit.sh \
  /project/tmp/source_linked_cps_contract_194147

  pass
  artifacts: /project/tmp/native_primitive_audit.Ttllvd

Native certificate smoke:
  TMPDIR=/project/tmp tests/vampire_certificate/run_native_cert_v1_smoke.sh
  pass
  artifacts: /project/tmp/native_cert_v1.iecUbY
```

The same committed state was also tested on the canonical line-233 Skolem
case:

```text
work=/project/tmp/line233_cps_contract_193828
TMPDIR=/project/tmp MEGALODON_CERT_DEBUG=1 \
MEGALODON_CERT_FAIL_FAST_SKOLEM_CPS=1 \
timeout 90s /project/Megalodon/bin/megalodon \
  -v 4 -allowincompleteqed \
  -vampireaby /project/vampire-leancheck/vampire_rel_vampire/megalodon5_11089 \
  -vampireabytimeout 10 \
  -vampireabyproof megalodon \
  -vampireabynative \
  -vampireabynativestrict \
  -vampireabytarget 233 4 \
  -vampireabytargetstop \
  -vampireabyoutdir "$work/out" \
  /project/Megalodon/examples/hammer/100thms_12_h.mg
```

That run still failed, but the failure moved in a useful way. The explicit
`result-to-target replaced-checked/current proposition mismatch` diagnostic
disappeared, while the final check still rejected the candidate because the
proof applies a branch-local proposition at one binder depth too shallow:

```text
expected: forall _:(prop -> (prop -> prop)),
          (((_0 (forall _:prop, (_0 -> _0))) _6) ->
           ((_0 _6) (forall _:prop, (_0 -> _0))))

actual:   forall _:(prop -> (prop -> prop)),
          (((_0 (forall _:prop, (_0 -> _0))) _5) ->
           ((_0 _5) (forall _:prop, (_0 -> _0))))
```

So `fd29191` should be considered validated but incomplete. It removes one
incorrect conflation in the Skolem CPS contract and preserves the previously
passing focused targets. It does not solve line 233.

There is also a current uncommitted Megalodon experiment in
`src/vampire_cert_v1.ml`. It changes the Skolem shadow replay so that the
shadow context is seeded with the formula-level Skolem assumption rather than
the legacy checked proposition, and it avoids using the Skolem parent's legacy
step variables when the parent of a `cnf_formula_clause` is the Skolem step
itself. A later local edit in the same experiment corrected the wrapper used
when a shadow-derived `result_to_target_body` already has the formula-level
assumption as its source.

The experiment compiled and has now passed the focused regression gates:

```text
TMPDIR=/project/tmp ./makeopt
pass

Focused native replay:
  TMPDIR=/project/tmp \
  VAMPIRE=/project/vampire-leancheck/vampire_rel_vampire/megalodon5_11089 \
  JOBS=3 WALL_SECONDS=120 \
  tests/vampire_reconstruction/run_native_aby_targets_parallel.sh

  172:4 PASS
  183:4 PASS
  PASS 2
  artifacts: /project/tmp/native_aby_targets.g6RT6m

Source-linked five-case corpus:
  TMPDIR=/project/tmp \
  VAMPIRE=/project/vampire-leancheck/vampire_rel_vampire/megalodon5_11089 \
  LIMIT=5 MIN_PASS=5 JOBS=5 WALL_SECONDS=30 VAMPIRE_SECONDS=10 \
  PROBLEMS_FILE=/project/Megalodon/tests/vampire_certificate/source_linked_strict_100.list \
  AUDIT_NATIVE_PRIMITIVES=0 AUDIT_KERNEL_V1_METADATA=0 \
  WORK_DIR=/project/tmp/source_linked_formula_shadow_195338 \
  tests/vampire_certificate/run_source_linked_corpus_parallel.sh

  hammer.1007.43.th0.p  PASS
  hammer.1009.43.th0.p  PASS
  hammer.10208.46.th0.p PASS
  hammer.10269.46.th0.p PASS
  hammer.10304.46.th0.p PASS
  PASS 5

Metadata audit:
  TMPDIR=/project/tmp MIN_REWRITE_POSITION=1 \
  tests/vampire_certificate/run_kernel_v1_metadata_audit.sh \
  /project/tmp/source_linked_formula_shadow_195338

  kernel_v1 records: 280
  skolemize records: 5
  pass
  artifacts: /project/tmp/kernel_v1_metadata_audit.YTbnyL

Primitive audit:
  TMPDIR=/project/tmp MIN_EQUALITY_SYMMETRY=0 \
  tests/vampire_certificate/run_native_primitive_audit.sh \
  /project/tmp/source_linked_formula_shadow_195338

  pass
  artifacts: /project/tmp/native_primitive_audit.XRyeHT
```

The focused line-233 experiments were:

```text
/project/tmp/line233_formula_shadow_194337
  interrupted after about one minute;
  shadow replay failed earlier at u43 because cnf_formula_clause still tried
  to instantiate the Skolem parent through legacy step variables.

/project/tmp/line233_formula_shadow_parentvars_194651
  failed after the parent-step-variable guard;
  shadow replay reached farther, but the final refutation proof was applied
  with the wrong wrapper shape.

/project/tmp/line233_formula_shadow_unwrapped_194808
  failed after the wrapper correction;
  shadow replay reached the empty clause, but the final candidate still failed
  with the same `_6` versus `_5` branch-local binder mismatch.
```

The experiment is therefore validated as a small branch increment, but it is
not a line-233 solution. A follow-up attempt to repair the unchanged
conjunction side by adding an extra component proof shift was tested in:

```text
/project/tmp/line233_component_shift_195656
```

That moved the failure from applying a proof of `_5` where `_6` was expected
to the dual mismatch, where the Church-elimination proof had been shifted too
far. A second follow-up attempt to rebuild the unchanged component from the
source-aligned proposition was tested in:

```text
/project/tmp/line233_source_aligned_195848
```

That broke the local conjunction contract and still failed. Both follow-up
variants were rejected and removed from the source tree. The remaining source
change is the formula-level shadow assumption contract, not the component
shift experiment.

The technical interpretation is now sharper than in the earlier July 18
report:

```text
Vampire is now emitting enough Skolem metadata to stop guessing source-binder
order and free-variable occurrence. Megalodon can validate that metadata
semantically. The remaining line-233 failure is not a missing metadata field
about free variables. It is a local proof-term construction error in the
Skolem CPS branch contract, most likely in how a Church-encoded branch motive
or unchanged conjunction side is shifted under nested binders.
```

This conclusion supports the audit direction rather than contradicting it.
The next durable step should not be a larger Python-style reconstruction
heuristic, and it should not be another broad Megalodon-side guessing pass.
Either the current local CPS repair must become a very small, fail-closed
proof-term fix, or Vampire should emit a first-class Skolem macro proof object
that states the branch contracts explicitly enough for Megalodon to elaborate
them mechanically.

## July 18 Postscript: Skolem Macro-Edge Child Metadata

After committing the formula-level shadow assumption work, the Vampire branch
was extended again in the audit-recommended direction. Each structured Skolem
macro edge now carries immediate child decomposition metadata for its
formula/source/target payloads:

```text
skolem_macro_edge_N_formula_child_count
skolem_macro_edge_N_formula_child_I_role
skolem_macro_edge_N_formula_child_I_formula
skolem_macro_edge_N_source_child_count
skolem_macro_edge_N_source_child_I_role
skolem_macro_edge_N_source_child_I_formula
skolem_macro_edge_N_target_child_count
skolem_macro_edge_N_target_child_I_role
skolem_macro_edge_N_target_child_I_formula
```

Roles are emitted from Vampire's formula tree, for example `left`, `right`,
`arg_0`, `arg_1`, or `body`. Megalodon now parses these child lists into the
typed `native_skolem_macro_edge` record and validates the child count, role,
and child formula against the raw formula S-expression. The validator accounts
for Vampire quantifier nodes that group several binders even though the
certificate S-expression renders them as nested `ALLV` or existential
encodings.

This still does not solve the line-233 Skolem CPS proof term. It does,
however, move another part of the branch decomposition contract from
Megalodon-side reconstruction into Vampire-emitted certificate data with
Megalodon-side semantic validation.

Validation for this postscript state used rebuilt Vampire binary:

```text
/project/vampire-leancheck/vampire_rel_vampire/megalodon5_11090
```

The checks were:

```text
Vampire build:
  TMPDIR=/project/tmp make -j10 vampire_rel
  pass

Megalodon build:
  TMPDIR=/project/tmp ./makeopt
  pass

Source-linked five-case corpus:
  TMPDIR=/project/tmp \
  VAMPIRE=/project/vampire-leancheck/vampire_rel_vampire/megalodon5_11090 \
  LIMIT=5 MIN_PASS=5 JOBS=5 WALL_SECONDS=30 VAMPIRE_SECONDS=10 \
  PROBLEMS_FILE=/project/Megalodon/tests/vampire_certificate/source_linked_strict_100.list \
  AUDIT_NATIVE_PRIMITIVES=0 AUDIT_KERNEL_V1_METADATA=0 \
  WORK_DIR=/project/tmp/source_linked_child_edges2_200722 \
  tests/vampire_certificate/run_source_linked_corpus_parallel.sh

  PASS 5

Metadata audit with child-field minimum:
  TMPDIR=/project/tmp MIN_REWRITE_POSITION=1 MIN_SKOLEM_MACRO_EDGE_CHILD=1 \
  tests/vampire_certificate/run_kernel_v1_metadata_audit.sh \
  /project/tmp/source_linked_child_edges2_200722

  kernel_v1 records: 280
  skolemize records: 5
  pass
  artifacts: /project/tmp/kernel_v1_metadata_audit.TekS0Y

Primitive audit:
  TMPDIR=/project/tmp MIN_EQUALITY_SYMMETRY=0 \
  tests/vampire_certificate/run_native_primitive_audit.sh \
  /project/tmp/source_linked_child_edges2_200722

  pass
  artifacts: /project/tmp/native_primitive_audit.7U4KHc

Focused native replay:
  TMPDIR=/project/tmp \
  VAMPIRE=/project/vampire-leancheck/vampire_rel_vampire/megalodon5_11090 \
  JOBS=3 WALL_SECONDS=120 \
  tests/vampire_reconstruction/run_native_aby_targets_parallel.sh

  172:4 PASS
  183:4 PASS
  PASS 2
  artifacts: /project/tmp/native_aby_targets.sX5wlN
```

## Honest Current Status

The project is substantially better than it was at the first audit. It now has
real native proof-term checking, stricter trust policy, richer Vampire
metadata, focused original-context successes, and a much clearer understanding
of why Python/script reconstruction is not the final path.

It is still not finished. The current implementation remains too ad hoc around
Skolemization and too concentrated in the large Megalodon importer. The
Prover9/Ivy direction is accepted and partially implemented, but not yet the
dominant end-to-end path. The 100-theorem E1 target remains open.

At this refresh, the code-size warning from the audits is still relevant:
`src/vampire_cert_v1.ml` is approximately 31,183 lines, while the Vampire
Megalodon exporter files are approximately 25,658 lines for
`MegalodonChecker.cpp`, 1,466 lines for `MegalodonKernelSyntax.cpp`, and 718
lines for `MegalodonKernelSyntax.hpp`. Some of that growth is now in the
right direction, especially the C++ metadata boundary and the OCaml typed
metadata records, but it is still too much mass in too few files. Future
changes should favor extraction and primitive proof-object design over adding
more broad replay cases in place.

The most useful immediate work is not another broad pass-count run. It is:

1. prioritize a first-class Vampire-emitted Skolem proof object with explicit
   branch contracts, ordered binders, dependencies, witness terms, sorts, and
   macro edges; keep any Skolem CPS continuation repair small and
   fail-closed;
2. make Skolemization consume explicit Vampire-emitted proof data;
3. isolate the small kernel and source-context checker;
4. define a small E1 suite that exercises original lemmas, local hypotheses,
   definitions, `set` equalities, Skolemization, equality, and AVATAR;
5. only then return to a broad 100-theorem gate.

## Final Expert-Review Position

This refresh should be read as a correction to the project direction, not as
an end-of-project report. The week of work produced useful infrastructure, but
also demonstrated why the early strategy was too optimistic. Translating
Vampire's existing proof output after the fact, especially through Python or
through a large Megalodon-side heuristic interpreter, does not expose enough
stable proof structure for a trustworthy general reconstruction path. The
current branch has therefore moved toward the audit's recommendation: make
Vampire emit richer proof objects while it still has the exact inference data,
then make Megalodon check a deliberately small calculus.

The most defensible completed work is the certificate boundary work:

- Vampire now emits substantially richer `kernel_v1` records.
- The rendering contract has begun moving into
  `Shell/MegalodonChecker/MegalodonKernelSyntax.{hpp,cpp}`.
- Megalodon validates many of the new fields semantically rather than merely
  accepting their presence.
- The trust reset after the July 16 audit is in force: certificate-derived
  `Known` propositions are no longer acceptable on counted native proof-term
  paths.
- Focused native proof-term replay succeeds on small real targets such as
  `172:4` and `183:4`.

The most important unfinished work is also clear:

- The system still does not have an E1 suite of 100 original-context proofs.
- The source/preprocessing transformation layer is not yet a clean
  Smolka-style proof-producing module.
- Skolemization is still the active bottleneck, especially the line-233 CPS
  branch contract around step `u35`.
- The OCaml importer remains too large and too responsible for proof
  reconstruction decisions.
- The Vampire-side `PrimitiveExpansion` structure is only a seed, not yet the
  Ivy-like primitive certificate object that the audit asks for.

The latest Skolem child-metadata work is a good example of the desired
direction. Vampire emits child roles and child formulas for the relevant
Skolem macro-edge payloads; Megalodon checks that this data matches the
formula tree. That is better than guessing in the importer. However, it is
still only metadata validation. It does not yet say: "here is the primitive
proof sequence that justifies this Skolem transformation."

For that reason, the recommended next implementation step is not to grow the
current Skolem CPS repair much further unless the fix is small, local, and
fail-closed. The more durable step is to define the explicit Skolem macro
certificate:

```text
source formula
target formula
introduced Skolem symbols
dependency variables and sorts
choice/epsilon definitions
macro-edge decomposition
branch-local propositions
continuation source and target propositions
primitive proof script for the transformation
```

Megalodon should then validate and elaborate that object into proof terms
using classical principles already present in the library, including `xm` and
the ordinary Megalodon equality and logical constants. It should not introduce
a private equality, private logic, certificate-local theorem, `aby`, `admit`,
or incomplete QED.

For original-context linking, the current state is partial rather than absent.
Global facts and several source-origin classes are recognized, and generated
`set` equalities are understood as obligations that should be solved by
reflexivity rather than mapped to user claims. But the full API still needs to
be made explicit. The checker should distinguish:

```text
global theorem/definition facts from the loaded development
local hypotheses in scope at the call site
the current conjecture and its negation as exported to Vampire
generated set equalities discharged by reflexivity
generated transformation facts discharged by Smolka-style proofs
Skolem definitions discharged by conservative choice/epsilon definitions
```

The proposed near-term test suite should stay small and fast until the proof
objects are real. A credible next gate is ten original-context examples after
`xm` in the library, selected to cover global lemmas, local assumptions,
definitions, `set` equalities, equality reasoning, one Skolem example, one
definition-fold example, and one AVATAR example. Only after that gate passes
without trusted certificate-derived facts should the project return to the
100-theorem E1 target.

The expert should especially review three design decisions:

1. Whether the Skolem CPS path should be finished as a transitional
   implementation or replaced immediately by a Vampire-emitted primitive
   Skolem object.
2. Whether the current `kernel_v1` primitive vocabulary is small enough, or
   whether it should be reduced further to a Prover9/Ivy-like core before more
   inferences are added.
3. Whether the source-context API should be extracted before the next proof
   family is implemented, so original-context composition becomes a first-class
   invariant rather than a final integration step.

## Appendix: Non-Negotiable Project Constraints

The following constraints should remain in force:

- use `/project/tmp` for temporary generated artifacts;
- use THF input;
- use per-problem Vampire timeout no greater than 10 seconds;
- run broad Vampire and Megalodon checks in parallel;
- avoid repeated Vampire runs on known-unsolved problems;
- treat `xm` as available and work classically;
- use Megalodon's library logic and equality;
- no final `aby`;
- no final `admit`;
- no incomplete QED on the qualifying path;
- no hidden bridge premise;
- no certificate-derived trusted theorem on counted paths;
- no Python proof reconstruction on the qualifying path;
- classify evidence honestly as E1, E2, E3, or E4.
