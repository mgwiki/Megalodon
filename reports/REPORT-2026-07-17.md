# Vampire to Megalodon Proof Reconstruction: July 17 Project Report

Date: 2026-07-17

This report summarizes the state of the Vampire-to-Megalodon proof
reconstruction project for external review. It incorporates the previous daily
reports, the audit reports and responses, the Prover9/Ivy analysis, the
Smolka-style transformation discussion, and the current local
`vampire/megalodon5` branches.

The report is intentionally written as an audit handoff rather than as a
progress announcement. It distinguishes engineering integration evidence from
actual proof reconstruction evidence. Several earlier reports and intermediate
frontier numbers were over-optimistic; the later audits corrected the trust
boundary, and the current report follows those corrections.

## Repository State

At report time:

- Megalodon repository: `/project/Megalodon`
- Megalodon branch: `vampire/megalodon5`
- Megalodon technical-state head before this report refresh: `30e2495`
  (`Replay rectification with Vampire variable maps`)
- Vampire proof-export fork: `/project/vampire-leancheck`
- Vampire branch: `vampire/megalodon5`
- Vampire head: `1b8b5cd42` (`Emit rectify variable maps`)
- Requested report path: `/project/reports/REPORT-2026-07-17.md`
- Related tracked report path: `/project/Megalodon/reports/REPORT-2026-07-17.md`
- Temporary/test artifact root: `/project/tmp`

The Megalodon working tree contains many untracked generated Hammer problems,
Vampire outputs, and local run artifacts under `examples/hammer`. The Vampire
working tree contains untracked build artifacts such as `obj/`,
`vampire_rel_detached_10761`, `vampire_rel_vampire/`, and `version.cpp`.
Those are not counted as source changes. The evidence in this report is tied
to committed heads and named `/project/tmp` artifacts where possible.

## Executive Summary

The project is not finished. It is now much better aligned with the audit
recommendations than it was earlier in the week, but it is still a prototype
with focused successes, not a general 100-theorem Vampire-to-Megalodon proof
reconstructor.

The intended finished pipeline remains:

```text
original Megalodon theorem and proof context
  -> THF/TPTP export
  -> Vampire proof search, timeout <= 10s
  -> Vampire-emitted explicit certificate
  -> Megalodon native proof-term checking
  -> proof of the original Megalodon goal
```

The final path must not use `admit`, `aby`, incomplete QED, bridge
assumptions, Python proof reconstruction, certificate-derived `Known`
theorems, or private replacements for Megalodon's equality and logical
constants. The project assumes a classical Megalodon context: `xm`,
double-negation principles, `False`, disjunction, proposition extensionality
where needed, and Megalodon's equality are expected to be available from the
loaded development.

The most important architectural conclusion is unchanged from the July 15 and
July 16 audits:

- Python was useful for exploration, but it is not a qualifying path.
- A broad Megalodon-side OCaml replay engine is also not the final target if
  it guesses proof details Vampire already knows.
- The target architecture is Prover9/Ivy-style: Vampire expands complex
  internal inferences into small explicit proof objects while it still has the
  exact selected literals, substitutions, positions, sorts, introduced
  symbols, and parent clauses; Megalodon checks those objects and elaborates
  native proof terms.
- Source and preprocessing transformations must be proof-producing. They
  cannot be justified in counted modes by dynamically installing the
  proposition to be proved as a `Known` theorem.

There has been real progress:

- Vampire now has a `MegalodonKernelSyntax` boundary and a first
  `MegalodonKernelStep` builder, with rendered-domain wrappers for clauses,
  literals, substitutions, positions, Skolem metadata, source transforms,
  CNF metadata, definition folds, AVATAR records, URR traces, and subsumption
  primitive metadata.
- Megalodon has a native certificate parser/checker, a shared
  `vampire_kernel_syntax.ml` vocabulary, native proof-term checking for a
  clausal core, source-context resolution infrastructure, and fail-closed
  guards against certificate-derived `Known` propositions in counted
  proof-term modes.
- The latest Megalodon work fixed a real live preprocessing case,
  `hammer.10644.15`, by handling native rectification over formula
  propositions and by giving the FOOL proof constructor a typed fallback
  witness for a parent variable dropped by the target formula.
- The newest Vampire/Megalodon pair moved the representative
  predicate-definition blocker beyond the contract-only stage: Vampire emits
  `predicate_definition_intro` metadata with the introduced symbol,
  definiendum symbol, body formula, result formula, body variables, and
  classical proof-shape fields; Megalodon validates that metadata strictly and
  now elaborates the representative predicate-definition and fold steps as
  native proof terms rather than as certificate-derived `Known` theorems.
- Recent Megalodon work also replays the representative AVATAR
  split/component/refutation layer natively for the cached
  `hammer.11925.125` certificate. It supports both directions of split
  definitions, complementary component clauses, split-clause replacement, and
  RUP-style split-clause resolution chains with factoring of duplicate split
  literals.
- The latest Megalodon commit fixed the regenerated `hammer.11925.125`
  `truth_conflict` failure by opening the quantified parent proof in the
  result context before replaying the contradiction.
- The latest Megalodon commit after that, `b53a783`, moves
  `predicate_definition_fold_chain` out of the remaining bridge-like path for
  the representative cached family. The checker now computes the actual
  intermediate formula path through a fold chain and elaborates each fold step
  as a native proof term. It no longer just proves reachability in a formula
  graph and then relies on a generated
  `vampire_predicate_definition_fold_chain_*` known theorem.
- The latest Megalodon commit, `84d6a4e`, then moved the compact
  `skolem_formula` blocker in `hammer.10806.144/u311` out of the frontier.
- The latest Megalodon commit after that, `776eb49`, records the compact
  binder follow-up: a broad Megalodon-side rebinding pass can advance the
  representative `hammer.1007.43/u83` rectification failure, but it regresses
  already-passing compact-Skolem/FOOL/ENNF checks. The durable fix should be
  Vampire-side explicit binder identities, alpha-renaming metadata, or a
  lowered primitive rectification object, not an OCaml-side heuristic that
  guesses binder names after the fact.
  Native Skolem replay now accepts compact certificate steps whose explicit
  source formula field is absent but whose checked parent formula is the
  source formula, and it handles compact generated Skolem definitions in the
  representative definition-heavy cached family. The focused regression checks
  45 native preprocess proof-term steps and source-bindings for
  `hammer.10806.144`.
- The newest Vampire/Megalodon pair then implements that durable fix for the
  representative rectification family. Vampire emits an explicit
  `rectify_variable_map` field for non-identity alpha-renaming pairs, and
  Megalodon consumes that field when opening the parent proof under the target
  binders. The fresh regenerated `hammer.1007.43` certificate now checks 68
  native preprocess proof-term steps without compact anonymous `ALL` binders
  and without a Megalodon-side name-guessing heuristic.
- Megalodon now has a separate fail-closed `vampire` proof command. It uses
  the same live certificate path as the old certified `aby` integration, but
  it cannot silently fall back to `aby`, `admit`, incomplete proof states,
  native fallback reconstruction, or target skipping. It is accepted only
  when the native Vampire certificate path reconstructs a proof term.
- The THF export path now preserves the proof-command origin. Certified
  `vampire` commands emit `% megalodon_origin ... (kind "vampire")` and
  `vampire.*.thf.p` artifacts, while legacy certified `aby` commands still
  emit `(kind "aby")` and `aby.*` artifacts. This makes audit logs distinguish
  the new no-`aby` command path from legacy automation.
- The latest Vampire commit fixed a real source-context smoke failure in
  `forward subsumption resolution`: the fast path now emits primitive
  `substitute`/`resolve` records with final result clauses, parent counts,
  result clauses, and pivots instead of returning only a rendered native step.
  The 12-target real Vampire source-context smoke now passes after previously
  failing seven targets on missing
  `primitive_expansion_final_result_clause`.
- The live strict 100-case THF/Vampire/certificate run passes structurally:
  `/project/tmp/live_strict_100_megalodon5_current/summary.tsv` reports
  `PASS 100`.
- The current live Skolem proof-term frontier is no longer wholly blocked by
  stale certificates or direct transitional `Known` assumptions:
  `/project/tmp/live-skolem-frontier-script.183108/counts.txt` originally
  reported `SKOLEM_PF_PASS 24` out of 35 eligible Skolem-heavy cases, and the
  checker-only recheck after the rectification/FOOL fix reports
  `SKOLEM_PF_PASS 32`, with one deliberately refused transitional
  predicate-definition blocker and two stale no-certificate artifacts.
- After predicate-definition, AVATAR replay, and the truth-conflict
  proof-opening fix, a fresh parallel live frontier at
  `/project/tmp/live-skolem-frontier-after-truth-conflict.201400` reports
  `SKOLEM_PF_PASS 33` and `TIMEOUT 2`, with no proof-term checker failure
  in that 35-case frontier. The newly passing case is the regenerated
  `hammer.11925.125` Vampire proof that had exposed the `u454`
  quantified-proof opening/type issue after the AVATAR work.
- After the fold-chain proof-term change, a cached preprocess proof-term audit
  over the currently supported preprocess fragment reports
  `PREPROCESS_STRUCTURAL_PASS 84` for all 84 eligible cases in
  `/project/tmp/preprocess_pf_audit_after_fold_chain.211008`. This is the
  strongest current evidence that the common rectification/FOOL/ENNF/CNF
  preprocessing slice is no longer relying on certificate-derived `Known`
  constants in that audit. The same run still classifies excluded first
  blockers separately: 48 `skolem_formula`, 13 `definition_input`, one
  `inequality_name_intro`, one `avatar_definition`, and one
  `avatar_component`.
- A definition-layer cached closed audit at
  `/project/tmp/definition_audit_after_fold_chain.211053` reports
  `DEFINITION_ELIGIBLE 12`, `CLOSED_PASS 12`, and
  `DEFINITION_CLOSED_PASS 12`. After `84d6a4e`, a proof-term replay over
  those 12 cases also reports `PREPROCESS_STRUCTURAL_PASS 12`. This is a real
  improvement over the earlier 11/12 frontier: the compact-source
  Skolemization case is now checked natively rather than rejected for lacking
  an explicit source field.
- The next broad live preprocessing frontier is now measured after the
  rectification variable-map fix, not before it. A 100-case THF run under
  `/project/tmp/live_preprocess_after_varmap_20260717_222323`, using
  10-way parallelism and 10-second Vampire caps, reports
  `PREPROCESS_STRUCTURAL_PASS 83`, `CHECK_FAILED 9`, and `NO_CERT 8`. The
  largest remaining checker class is now Skolemization under conjunctive
  contexts and related dependency-context checks, not the old
  `hammer.1007.43/u83` binder identity problem.

The critical qualifications are:

- `PASS 100` is E4 structural/integration evidence, not proof
  reconstruction. It shows that Vampire can find proofs and Megalodon can
  parse/validate the certificates and metadata for 100 THF problems. It does
  not mean those 100 proofs have been reconstructed as Megalodon proof terms.
- The 33/35 fresh live Skolem proof-term frontier is E2/E3-style focused
  evidence, not the final E1 original-context target. The two remaining
  cases are Vampire timeouts, not currently Megalodon checker failures.
- Original-context proof composition is still only partially implemented.
- Predicate-definition, AVATAR split/refutation, and the current
  truth-conflict proof-opening path have focused native implementations for
  representative `hammer.11925.125` certificates: one cached certificate for
  the predicate-definition/AVATAR path, and one regenerated certificate for
  the truth-conflict path. This is not yet a theorem that all possible Vampire
  predicate-definition, AVATAR, or truth-conflict shapes are handled; it is a
  focused proof-producing frontier success that should be kept under
  regression.
- The 12-target source-context smoke is stronger source-link integration
  evidence than a pure metadata fixture because it uses real Vampire output
  and checks known/local/conjecture classification on selected source
  obligations. It is still E4/E1-probe evidence, not completed original
  theorem reconstruction.
- The newer 12-target `SOURCE_COMMAND=vampire` smoke is stronger still as an
  interface test: it rewrites selected source commands from `aby` to
  `vampire`, runs real Vampire with 10-second timeouts, checks the shifted
  parser locations, checks `kind "vampire"` origin metadata, and checks that
  no `aby.*.thf.p` artifact is used for those selected commands. It remains
  interface/source-context evidence, not final E1 proof reconstruction.
- The fold-chain result is real proof-term progress for a cached family, but
  it is not yet original-context proof reconstruction. It should be counted as
  E2/E3-style focused proof-term evidence and regression coverage for the
  source/preprocessing layer.
- The rectification variable-map result is real proof-term progress because
  it moves a live regenerated certificate using new Vampire-emitted detail.
  It is still not E1 by itself: the remaining source assumptions are recorded
  and classified, but the proof has not yet been composed into the original
  theorem command as a complete source-context proof.
- The small-kernel path is present, but extraction is incomplete. Most logic
  still lives in the large historical `src/vampire_cert_v1.ml` and
  `Shell/MegalodonChecker/MegalodonChecker.cpp` modules.

The current best description is: the project has moved from "broad replay and
bridges" toward a real proof-object architecture, but the qualifying line is
still ahead.

## Prior Reports and Audits

The current report should be read after:

- `REPORT-2026-07-12.md`
- `REPORT-2026-07-13.md`
- `REPORT-2026-07-14.md`
- `REPORT-2026-07-15.md`
- `REPORT-2026-07-16.md`
- `audit-REPORT-2026-07-12.md`
- `audit-REPORT-2026-07-13.md`
- `audit-REPORT-2026-07-14.md`
- `audit-REPORT-2026-07-15.md`
- `audit-REPORT-2026-07-16.md`
- `audit-response-2026-07-12.md` through
  `audit-response-2026-07-17.md`
- `PROVER9-IVY-ANALYSIS-2026-07-13.md`
- `VAMPIRE-MEGALODON-KERNEL-IR-PLAN-2026-07-17.md`
- `VAMPIRE-MEGALODON-DESIGN-UPDATE-2026-07-17.md`
- `vampire-megalodon-certificate-spec.md`

The audit trajectory matters because it changed what is counted as progress.

The July 14 audit found that earlier strict runs still allowed bridge
premises and that source linking was label-based rather than
proposition-based. That made some earlier "passes" integration evidence, not
reconstruction evidence.

The July 15 audit accepted that closed/source-checked evidence was improving,
but criticized the project for growing a large Megalodon-side replay engine.
It recommended a Prover9/Ivy-style path: keep the current machinery as a
regression oracle, but move proof detail into Vampire and make Megalodon a
small checker.

The July 16 audit found the most important hidden trust bug in the native
preprocessing path: many transformations were proved by dynamically adding a
certificate-derived proposition to the `Known` theorem table and then applying
it. That is a bridge assumption in disguise. The current branch now treats
such behavior as structural diagnostics only. Counted native proof-term paths
must reject certificate-derived `Known` names except for a fixed reviewed
logical basis.

The audit response and design documents from July 16-17 are accepted here as
binding project policy. The correct next milestone is not "raise a broad
frontier number by adding more Megalodon-side heuristics"; it is "produce
small explicit Vampire certificates and check them natively without hidden
assumptions, then compose them with the original source context."

## Evidence Classes Used in This Report

The project now uses evidence classes rather than ambiguous "pass" labels:

- E1: original Megalodon theorem reconstructed in its live source context,
  with no `admit`, no `aby`, no bridge assumptions, and no
  certificate-derived `Known`.
- E2: closed native `Syntax.pf` proof of a live Vampire certificate, still at
  the exported THF/certificate boundary.
- E3: focused or synthetic native proof-term checks for the small kernel or a
  limited transformation fragment.
- E4: structural certificate validation, metadata validation, source-map
  validation, integration runs, and transitional diagnostics.

The live strict 100-case run is E4. The old preprocessing frontiers that
required `MEGALODON_CERT_ALLOW_TRANSITIONAL_PREPROCESS_KNOWN=1` are E4. The
native core proof-term tests are E3 unless they are live exported proofs with
closed proof terms. Focused live Skolem proof-term checks are closer to E2,
but they are still not E1 until they are composed into the original Megalodon
theorem context.

This distinction is the main correction from earlier reporting.

## Late July 17 Update Since the Earlier Refresh

Two important implementation commits landed after the earlier July 17 report
refresh. The first is Megalodon commit `b53a783`:

```text
b53a783 Replay predicate definition fold chains natively
```

This commit matters because it removes another bridge-like fallback from a
real preprocessing family. The old fold-chain checker could establish that a
sequence of predicate-definition folds connected the source and target
formulas, but native proof-term replay still relied on dynamic
`vampire_predicate_definition_fold_chain_*` facts for some chain shapes. The
new path computes the actual intermediate formula path and replays each fold
step using native proof constructors. At the replacement point it unfolds the
generated predicate definition delta and wraps the result with the necessary
true-equality conversion when the target formula asks for `true = body`.

The focused regression added by that commit is deliberately source-bound:

```text
tests/vampire_certificate/native_cert_v1_predicate_definition_fold_chain_valid.sexp
tests/vampire_certificate/native_cert_v1_predicate_definition_fold_chain_valid.th0.p
```

The smoke suite checks that this fixture reaches:

```text
Vampire certificate v1 native preprocess proof term checked 8 steps
```

and rejects the old generated fold-chain known names. The focused manual
check also reports source bindings and remaining assumptions rather than
silently treating source linkage as solved.

The cached audit immediately after the fold-chain work gives the current best
frontier picture:

```text
/project/tmp/preprocess_pf_audit_after_fold_chain.211008/counts.txt
PREPROCESS_ELIGIBLE 84
EXCLUDED_OR_CORE_ONLY 88
MIN_PREPROCESS 10

/project/tmp/preprocess_pf_audit_after_fold_chain.211008/preprocess_pf_check/counts.txt
PREPROCESS_STRUCTURAL_PASS 84
```

The `84/84` result is not an E1 claim, but it is meaningful: for the selected
preprocess fragment, the native proof-term checker no longer stops on the
former fold-chain bridge. The excluded first blockers are now explicit:

```text
48 skolem_formula
13 definition_input
1 inequality_name_intro
1 avatar_definition
1 avatar_component
```

The same run also confirms that the closed textual emitter still has one
separate failure in `hammer.11703.242` involving `bridge_cnf__u607`; that is
an emitter limitation, not the native proof-term frontier.

A definition-layer audit after the same change reports:

```text
/project/tmp/definition_audit_after_fold_chain.211053/counts.txt
DEFINITION_ELIGIBLE 12
EXCLUDED_OR_LOWER_LAYER_ONLY 160
MIN_DEFINITION 10

/project/tmp/definition_audit_after_fold_chain.211053/closed_check/counts.txt
CLOSED_PASS 12

/project/tmp/definition_audit_after_fold_chain.211053/definition_counts.txt
DEFINITION_CLOSED_PASS 12
```

Before the compact Skolem fix, those 12 definition cases checked as native
preprocess proof terms with 11 passes and one failure:

```text
/project/tmp/definition_pf_audit_after_fold_chain.211117/counts.txt
PREPROCESS_STRUCTURAL_PASS 11
PREPROCESS_STRUCTURAL_FAIL 1

hammer.10806.144
u311: native preprocess proof-term skolemization needs an explicit source formula
```

Commit `84d6a4e` addresses that target. The certificate step is a compact
`skolem_formula` whose S-expression does not carry an explicit source formula
argument, but the checked parent formula is the source formula and the Vampire
`kernel_v1` metadata also contains `source_formula` and `parent_0_formula`.
The native proof-term path now follows that contract instead of failing merely
because the compact source field is absent. The clean regression after the
fix reports:

```text
/project/tmp/definition_pf_audit_after_clean_rebuild.213458/counts.txt
PREPROCESS_STRUCTURAL_PASS 12
```

The focused compact-Skolem regression also checks:

```text
tests/vampire_certificate/closed_cases/hammer.10806.144.native.sexp
Vampire certificate v1 native preprocess proof term checked 45 steps
Vampire certificate v1 source bindings checked 2 assumptions
```

The next concrete frontier exposed by that fix is a 35-case cached
Skolem-selected proof-term run:

```text
/project/tmp/skolem_pf_frontier_after_compact_skolem.212928/counts.txt
FORMULA_ORIENTATION_UNSUPPORTED 35
```

The representative first failure is `hammer.1007.43/u83`, a
`rectify_formula` step where the source and target differ only by the displayed
name of a compact anonymous `ALL` binder. This is the right next engineering
target because it is a general binder/alpha-conversion representation issue.
It should be solved either by richer Vampire binder output or by a small,
proof-term-safe compact-binder normalization at the certificate boundary, not
by adding theorem-specific matching.

This update does not change the strategic diagnosis. It strengthens the
proof-term evidence for predicate-definition fold chains and compact
Skolemization, and it sharpens the next preprocessing proof-term blocker, but
original-context E1 reconstruction is still unfinished.

After that update, a focused Megalodon-side attempt was made to normalize
compact anonymous binders during certificate parsing. It confirmed the shape
of the problem but was reverted. The attempted broad rebinding moved
`hammer.1007.43/u83` and a neighboring compact-binder rectification forward,
but it changed formulas in already-passing cached cases such as
`hammer.10806.144/u271`, `u292`, and `u311`. That regression is important: it
shows that compact binder repair is not a safe place for Megalodon to guess.

The current Vampire source already contains the better representation at the
certificate boundary. The native S-expression renderer emits named universal
binders as `ALLV`, and higher-order lambda binders as `VLAMV`; the Megalodon
certificate parser already has corresponding parsing support. Therefore the
cached `hammer.1007.43` certificate that still contains anonymous compact
`ALL` and `LAM` forms should be treated as stale evidence.

## Latest Rectification Variable-Map Update

The most recent implementation work follows the conclusion above. Rather than
continue to normalize compact binders inside Megalodon, Vampire now emits
explicit alpha-renaming data for rectification steps.

The Vampire commit is:

```text
1b8b5cd42 Emit rectify variable maps
```

It adds `rectify_variable_map` metadata to the `kernel_v1` record when Vampire
can collect a consistent non-identity pairing between quantified variables in
the source and target formulas. The map is deliberately narrow. In the fresh
`hammer.1007.43` certificate, Vampire emits examples such as:

```text
rectify_variable_map=(subst ("X3" (TMH "X4")))
rectify_variable_map=(subst ("X3" (TMH "X4")) ("X4" (TMH "X3")))
```

That means Vampire, not Megalodon, records that a displayed `X3` binder in the
parent formula is meant to be opened as the target `X4` binder, or that two
displayed binder names have been swapped by rectification. This is the
Prover9/Ivy-style direction recommended by the audits: the prover emits the
proof-relevant detail while it still knows the transformation.

The Megalodon commit is:

```text
30e2495 Replay rectification with Vampire variable maps
```

It parses the optional `rectify_variable_map` field and passes it into the
native formula-orientation proof constructor. During parent proof
instantiation, Megalodon first checks this Vampire-emitted map. If a parent
variable name maps to a target binder name, the checker resolves that target
name against the result-bound de Bruijn context and opens the parent proof
with the correct proof-term variable. If the metadata is absent, the previous
strict matching path remains in force; the checker does not introduce a broad
alpha-renaming search.

The representative validation is the fresh regenerated live case:

```text
/project/tmp/live_1007_varmap_20260717_222020
```

Megalodon reports:

```text
Vampire certificate v1 source map checked 5 sources.
Vampire certificate v1 closed checked 68 steps.
Vampire certificate v1 native preprocess proof term checked 68 steps.
Vampire certificate v1 native preprocess source bindings checked 5 assumptions.
Vampire certificate v1 native preprocess source assumptions remaining by kind known=2 local=1 definition=0 generated=0 conjecture=2 unresolved=0.
Everything looks good.
```

This is a meaningful proof-producing fix. It also has the right trust shape:
the checker validates a concrete map printed by Vampire and then constructs a
native proof term; it does not install a generated theorem and it does not
guess alpha-equivalence globally.

A focused smoke test was added:

```text
tests/vampire_certificate/run_native_live_rectify_varmap_smoke.sh
```

The smoke regenerates `hammer.1007.43` with the current Vampire binary under
`/project/tmp`, checks that the emitted certificate contains the expected
`rectify_variable_map` pairs, asserts that compact anonymous `(ALL ...)`
binders are not present in the fresh certificate, and then runs
`-vampirecertv1preprocesspfcheck`. This keeps the fix tied to a live Vampire
certificate rather than a stale cached artifact.

The broad live frontier after the fix is:

```text
/project/tmp/live_preprocess_after_varmap_20260717_222323/counts.txt
PREPROCESS_STRUCTURAL_PASS 83
CHECK_FAILED 9
NO_CERT 8
```

The nine checker failures are now:

```text
hammer.1009.43   formula orientation beyond equality symmetry/true elimination/matching structure
hammer.10640.42  missing primitive_expansion_final_result_clause
hammer.10785.27  Skolem body mentions a non-dependency context variable
hammer.1110.18   Skolemization under unsupported conjunction context
hammer.11108.78  avatar_component has no matching split definition
hammer.1114.18   Skolemization under unsupported conjunction context
hammer.11145.52  Skolem body mentions a non-dependency context variable
hammer.1118.18   Skolemization under unsupported conjunction context
hammer.1122.18   Skolemization under unsupported conjunction context
```

The practical next proof-term target has therefore moved. The old
`hammer.1007.43/u83` compact-binder rectification problem has a principled
Vampire-side contract fix. The current largest repeated class is
Skolemization inside conjunction contexts. In the representative
`hammer.1110.18/u85`, Vampire transforms:

```text
vampire_and A (exists X, P X)
```

into:

```text
vampire_and A (P sK0)
```

and emits a choice/instantiation parent. The Megalodon proof constructor
currently handles existential, universal, and disjunctive contexts, but not
this conjunctive context. The right next implementation is a general
conjunction-context proof rule: project the unchanged conjunct, recursively
apply the Skolem proof to the existential conjunct, and reassemble the
conjunction. This should be implemented as a proof-producing constructor, not
as a theorem-specific shortcut.

## Original Objective and Operational Constraints

The original task was to download Vampire and Megalodon, find or adapt the
Vampire proof reconstruction support originally aimed at Lean, and port proof
reconstruction to Megalodon. The expected final system should:

1. Export Megalodon goals to THF/TPTP.
2. Run Vampire on THF problems, not FOF approximations.
3. Use a short Vampire timeout, currently capped at 10 seconds per problem.
4. Run export, Vampire proof search, and certificate checking in parallel,
   normally 10 to 20 jobs for broad batches.
5. Avoid repeatedly rerunning known-unsolved Vampire searches.
6. Ask Vampire to emit proof objects rich enough for reconstruction.
7. Check those proof objects in Megalodon using native proof terms.
8. Link source facts back to original Megalodon lemmas, definitions, local
   hypotheses, set-generated equalities, and conjectures.
9. Use Megalodon's existing equality, `False`, disjunction, and classical
   library facts.
10. Avoid theorem-specific hardwiring and library-specific tricks.
11. Avoid `admit`, `aby`, or `-allowincompleteqed` in the resulting proof.
12. Validate on at least 100 representative Megalodon theorems and their
   Vampire proofs.

Additional local constraints from the conversation are also followed:

- `/project/tmp` is the temporary directory for generated proof/test artifacts.
- Vampire compilation should use parallel build commands such as
  `make -j10` where possible.
- Test harnesses should run in parallel and should not perform slow sequential
  export/search/check cycles when a batch artifact can be reused.
- Problems before the library's excluded-middle point are not a priority
  because the reconstruction assumes classical reasoning.
- The result should be general over libraries; it should not hardwire
  theorem names like `famunion` or optimize for the current benchmark list.

## Current Architecture

The intended architecture now has four layers.

### Vampire Certificate and Primitive IR Layer

Vampire is responsible for emitting proof details while they are still
available internally. For a complex inference, the certificate should not
merely say "superposition" or "unit_resulting_resolution" and leave
Megalodon to reverse-engineer the missing local facts. Vampire should emit:

- parent step identifiers;
- selected literals and pivot indices;
- equality side and rewrite direction;
- rewrite positions;
- substitutions before and after macro steps;
- variable and symbol sorts;
- introduced Skolem symbols and their dependencies;
- source formulas before and after transformations;
- result formulas and clauses;
- AVATAR split/component/refutation metadata where relevant;
- primitive expansion chains for macro inferences.

The Vampire fork now contains
`Shell/MegalodonChecker/MegalodonKernelSyntax.{hpp,cpp}`. This is not yet the
complete primitive proof IR requested by the audits, but it is a real boundary
around the old monolithic exporter. The current line counts show the state:

```text
25155 Shell/MegalodonChecker/MegalodonChecker.cpp
 1311 Shell/MegalodonChecker/MegalodonKernelSyntax.cpp
  635 Shell/MegalodonChecker/MegalodonKernelSyntax.hpp
```

The extracted C++ boundary currently defines records such as:

- `PrimitiveExpansion`;
- `RenderedKernelUnitRef`;
- `RenderedKernelTerm`;
- `RenderedKernelFormula`;
- `RenderedKernelLiteral`;
- `RenderedKernelClause`;
- `RenderedKernelSubstitution`;
- `RenderedKernelPosition`;
- `RenderedKernelConclusion`;
- `RenderedKernelParent`;
- `RenderedKernelLiteralSelection`;
- `RenderedKernelRewrite`;
- `RenderedKernelUrrTraceStep`;
- `RenderedKernelAvatarComponent`;
- `RenderedKernelAvatarDefinition`;
- `RenderedKernelSplitDependency`;
- `RenderedKernelAvatarRefutation`;
- `RenderedKernelSkolemIntroducedSymbol`;
- `RenderedKernelPredicateDefinitionVariable`;
- `RenderedKernelPredicateDefinition`;
- `RenderedKernelSourceFormulaTransform`;
- `RenderedKernelRectifyRenamings`;
- `RenderedKernelCnfClause`;
- `RenderedKernelDefinitionFold`;
- `MegalodonKernelStep`.

The key limitation is that many of these records still wrap rendered strings
rather than typed Vampire terms, literals, substitutions, and positions. That
is an improvement over open-coded field emission, but it is not yet the
Prover9/Ivy-style primitive object. The next Vampire step should replace
rendered wrappers with typed internal objects and make macro inferences build
explicit primitive step lists before serialization.

Recent Vampire commits on `vampire/megalodon5` include:

```text
d48f82edf Emit subsumption fast-path primitive results
e3b4a8620 Emit predicate definition primitive metadata
8215c1fc8 Emit subsumption primitive step metadata
162c4fc31 Emit URR primitive rule payload metadata
69b89cba4 Emit URR primitive final result metadata
6f03fa872 Emit URR primitive parent metadata
99f6cd21c Represent URR expansion steps as primitive records
2462f0477 Track URR primitive expansion steps directly
47ab68635 Require URR primitive chain final id
d75fc3539 Extract primitive expansion chain metadata
d426e8d70 Expose URR primitive expansion chain
bb3aa602e Reject unsupported Megalodon kernel rules
```

This is directionally correct: URR, subsumption resolution, and now predicate
definition are moving from opaque macro names toward explicit primitive
metadata. The newest subsumption fast-path commit is a useful example of the
audit-guided correction: Megalodon should not parse a rendered native proof
string to recover primitive data that Vampire already has. Vampire now records
the primitive chain before printing it.

### Megalodon Certificate Checker Layer

Most Megalodon-side work still lives in `src/vampire_cert_v1.ml`, which is
too large:

```text
26302 src/vampire_cert_v1.ml
   54 src/vampire_kernel_syntax.ml
```

The extracted `src/vampire_kernel_syntax.ml` is currently small. It owns the
shared `kernel_v1` rule vocabulary and primitive-contract table. That is a
useful start, but not yet the modular checker/elaborator requested by the
audits.

The desired module split remains:

- `vampire_kernel_syntax.ml`: syntax, stable rule names, primitive contracts;
- `vampire_kernel_check.ml`: structural and type checks for the primitive
  certificate;
- `vampire_kernel_elab.ml`: native `Syntax.pf` elaboration for primitive
  steps;
- `vampire_source_context.ml`: source-context lookup and proof resolution;
- a compatibility module for parsing the historical certificate format.

The current monolithic file does useful work, but future broad additions to it
should be treated as technical debt unless they are small compatibility fixes
or extraction steps.

Current native checker capabilities include:

- strict S-expression certificate parsing;
- source-map validation;
- supported source formula comparison;
- source-obligation auditing;
- source-context auditing against loaded Megalodon knowns and locals;
- native core proof-term checking;
- native preprocessing proof-term checking for selected fragments;
- explicit rejection of certificate-derived `Known` propositions in counted
  proof-term paths;
- proof-producing handling of several generated set/reflexivity obligations;
- partial final composition back to live Megalodon `aby` targets.

### Source Context Layer

Source context is the bridge between exported THF identifiers and the original
Megalodon development. A checked exported refutation is not sufficient if its
premises are not connected back to real Megalodon facts.

The intended API shape remains:

```ocaml
type source_proof =
  | GlobalKnown of string * Syntax.tm
  | LocalHyp of int * Syntax.tm
  | Definitional of Syntax.tm * Syntax.pf
  | Generated of Syntax.tm * Syntax.pf
  | Unresolved of string * Syntax.tm

type source_context = source_id -> source_proof
```

The current implementation handles several source categories:

- global known facts resolved by hash/original name/TPTP name, then checked
  semantically;
- local hypotheses in the live proof context;
- local types and proof-local definitions;
- source-map aliases for mangled exported names;
- generated `set` equalities, proved by reflexivity/conversion when possible;
- conjecture facts, discharged at the final refutation-to-goal boundary;
- unresolved facts, which fail strict qualifying modes.

This is one of the most important improvements over early source-map work.
The checker is no longer allowed to accept an arbitrary source formula merely
because a label exists. It must compare the exported proposition against a
real source proposition or supply a proof-producing generated fact.

Remaining source-context gaps include:

- full local definition proof production when a local `set` equality is
  actually used by the refutation;
- robust original-name recovery for every mangled THF name shape;
- full final composition for larger local contexts with several definitions,
  hypotheses, and negated conjecture variants;
- avoiding any reliance on the old `aby` command as part of final evidence.

### Smolka-Style Transformation Layer

Source-to-clause transformations are not ordinary clausal kernel rules. They
are a separate proof-producing layer, roughly following the Smolka-style
view:

- source formula input;
- formula-term input/copying;
- definition input/fold/unfold;
- rectification;
- FOOL Boolean lifting and exhaustiveness;
- ENNF;
- CNF literal/formula projection;
- Skolemization using explicit introduced symbols, dependencies, sorts, and
  a choice principle;
- optional later AVATAR/SAT layers.

The current branch has made progress on these transformations, especially on
rectification/FOOL/ENNF/CNF and Skolemization metadata, but they are not
complete. The important trust invariant is now explicit: a transformation
either produces a checked native proof term or is rejected in counted mode.
It cannot silently insert a certificate-derived theorem such as
`vampire_skolem_formula_u123` or `vampire_predicate_definition_u123` into the
known table.

The latest Skolem work is the clearest example of this layer beginning to
work properly.

## Recent Megalodon Work

Recent Megalodon commits on `vampire/megalodon5` include:

```text
b53a783 Replay predicate definition fold chains natively
1258841 Refresh July 17 expert report
cbb9122 Exercise real Vampire targets through vampire command
b906bd1 Preserve Vampire proof command origins
b460bba Add certified Vampire proof command
72954de Refresh July 17 reconstruction report
0a5b06c Refresh July 17 expert report
2a354e9 Open truth-conflict parents in result context
b10dcf6 Refresh July 17 expert report
b8166bc Replay AVATAR split refutations natively
db26318 Refresh July 17 reconstruction report
d0cf6e9 Elaborate predicate definition proofs natively
2f52399 Refresh July 17 audit report
aa5fbdd Validate predicate definition primitive contract
28da765 Update July 17 audit handoff report
0bd3c08 Handle native rectification formula propositions
dd324ce Fix clause resolve proof context
1a5096a Handle nested Skolem orientation
10a6d53 Expand dependent Skolem proof frontier
5655c7f Reconstruct live Skolem preprocessing natively
a445ee3 Reconstruct preprocess macros natively
acc1434 Stabilize preprocess native audit boundary
3a44524 Treat core textual closed emission as diagnostic
71ae594 Accept unreferenced AVATAR split component counts
d2ebbd5 Validate subsumption primitive chain payloads
```

The most important changes are summarized below.

### Trust Reset for Preprocessing Proof Terms

The July 16 audit found that the native preprocessing proof-term frontier was
using certificate-derived `Known` propositions. The current branch makes that
behavior fail closed in counted proof-term modes. The old behavior is only
available through:

```sh
MEGALODON_CERT_ALLOW_TRANSITIONAL_PREPROCESS_KNOWN=1
```

and is classified as diagnostic E4 evidence.

The checker now traverses checked proof terms and rejects generated
certificate-derived proof constants with names beginning with `vampire_`,
except for a fixed reviewed logical basis such as choice and
not-forall/existence lemmas. This makes the trust boundary mechanically harder
to bypass.

### Core Textual Emission Reclassified

Earlier textual closed emission could be useful, but it is not the target
when native proof terms are available. Commit `3a44524` explicitly
reclassified core textual closed emission as diagnostic. That prevents
generated `.mg` scripts from being counted as the primary reconstruction
evidence.

### Preprocess Macro Reconstruction

Commit `a445ee3` reconstructed several preprocessing macros natively rather
than through transitional knowns. The significant families are:

- `rectify_formula`;
- `fool_formula`;
- `ennf_formula`;
- associated `_subst` follow-up steps.

The associated audit moved a selected cached preprocessing corpus into
`PREPROCESS_STRUCTURAL_PASS 84` without the transitional-known opt-in. This
is still not the final target, but it removed one large class of hidden
bridges.

Commit `0bd3c08` fixed the current representative live rectification blocker,
`hammer.10644.15`. The first failure was that native rectification matching
treated a formula-level proposition as an unsupported term-level shape. The
fix added a native formula rectification matcher and routed both native core
and native preprocess rectification through it. That exposed the next real
obligation in the same certificate: a FOOL formula step whose target dropped
one parent variable. The FOOL constructor now receives the certificate symbol
table and can instantiate the dropped parent variable with a typed witness
found in the source or target formula. This is a general formula/proof-context
fix, not a special case for the benchmark name.

The focused replay of `hammer.10644.15` now checks:

```text
Vampire certificate v1 native preprocess proof term checked 41 steps
```

and the cached Skolem frontier moved from 31 to 32 proof-term passes.

### Skolemization

The Skolemization work is currently the most active proof-term frontier.

Commit `5655c7f` made a live direct/nested Skolem preprocessing fixture check
as a real native proof term. It added explicit substitution replay through a
chain of existential binders and sequential conservative definitions for
nullary Skolem symbols.

Commit `10a6d53` expanded the dependent Skolem frontier. It delays generated
Skolem expansion until a symbol application supplies enough arguments for the
definition's leading lambda arity. It also added normalization of generated
Skolem definitions for formula-orientation matching while keeping the proof
term on Megalodon's real equality.

Commit `1a5096a` handled nested Skolem orientation. It generalized sequential
Skolem definitions from nullary/top-level occurrences to a preorder walk of
the source formula, including walks under `forall`, `vampire_or`,
`vampire_and`, and implication. It can eliminate nested
`vampire_exists_prop` in the forward direction using the existing choice
principle and then continue structural conversion.

This work is not theorem-specific hardwiring. It uses Vampire-emitted
substitution lists, introduced symbol metadata, and source formulas. The
remaining problems are in general proof-term opening/orientation and related
preprocessing fragments, not in recognizing a single benchmark theorem.

### Subsumption and URR Metadata

Megalodon now validates subsumption primitive chain payloads emitted by
Vampire. The representative check ensures that a `subsumption_resolution`
macro carrying primitive expansion metadata has coherent pivot and chain
fields. This is not yet a complete proof-term implementation for every
subsumption case, but it is aligned with the Prover9/Ivy plan: emit the chain
from Vampire and make Megalodon verify the explicit payload.

Focused URR evidence also exists:

```text
/project/tmp/urr_core_pf_audit_20260717_102726/summary.tsv
hammer.11453.77    URR_CORE_PF_PASS
hammer.11703.242   URR_CORE_PF_PASS
```

That is a narrow proof-term slice, not a full macro-lowering solution.

### Clause Resolution Proof Context

Commit `dd324ce` fixed a proof-context bug in native clause/clause
resolution. The bad branch occurred when the selected pivot was the only
literal in the left clause. The elaborator shifted the right parent proof by
one proof-hypothesis slot before consuming it, even though the surrounding
proof already had exactly the required context. This produced ill-formed
proof terms such as:

```text
Proof hypothesis index 12 is out of bounds for context length 12
```

The fix is small but important: it removes the extra proof shift in the
left-last-pivot case and adds a dedicated regression fixture,
`native_cert_v1_resolve_left_last_pivot_valid`. This was a general
clause-resolution proof-opening bug, not a benchmark-name special case. The
cached seven-case failure cluster now passes.

### AVATAR Status

AVATAR support has moved beyond structural metadata for at least one serious
representative proof. The branch now accepts unreferenced AVATAR split
component counts in the validator, Vampire emits more explicit AVATAR records
for components, definitions, split dependencies, split steps, and refutations,
and Megalodon can replay the cached `hammer.11925.125` AVATAR layer as native
proof terms.

The implemented replay is still narrow but no longer just a bridge. It:

- identifies split literals and split clauses;
- proves component clauses in both directions of a split definition;
- handles complementary component literals through contrapositive reasoning;
- replaces component literals by split literals inside clauses;
- replays SAT/RUP refutation steps as split-clause resolution chains;
- factors duplicate split literals after each resolution.

The focused cached certificate now reaches:

```text
Vampire certificate v1 native preprocess proof term checked 295 steps
```

This is meaningful proof-producing progress. It still does not make AVATAR
part of the first minimal clausal kernel. AVATAR should remain a separate
checked layer, ideally with explicit SAT/RUP-style proof objects emitted from
Vampire and a small Megalodon checker for split clauses. The current replay
should be used as regression evidence while that smaller layer is designed.

## Current Test Evidence

### Live Strict 100: E4 Structural Evidence

The current live strict artifact is:

```text
/project/tmp/live_strict_100_megalodon5_current/summary.tsv
```

It reports:

```text
PASS 100
```

This run used THF problems, real Vampire proof search, and native certificate
validation. It is valuable because it shows that, on a 100-problem live
corpus, Vampire can generate certificates and Megalodon can parse and
validate the certificate/source/metadata shape.

It is not proof reconstruction evidence. The run is E4 because it does not
check native proof terms for all inference steps and does not produce E1
original-context proofs for the 100 theorems.

The report must not say "100 proofs reconstructed." It should say "100
certificates structurally generated and validated."

### Live Skolem Proof-Term Frontier

The most relevant current proof-term frontier is the fresh live regenerated
run after the truth-conflict proof-opening fix:

```text
/project/tmp/live-skolem-frontier-after-truth-conflict.201400
```

Its counts are:

```text
SKOLEM_PF_PASS 33
TIMEOUT 2
```

The pass cases are:

```text
hammer.1007.43
hammer.10609.51
hammer.10644.15
hammer.10654.21
hammer.10693.23
hammer.10699.43
hammer.10703.23
hammer.10730.22
hammer.10765.22
hammer.10781.23
hammer.10783.22
hammer.10787.22
hammer.10789.22
hammer.10970.20
hammer.1098.23
hammer.10990.20
hammer.11020.22
hammer.11154.58
hammer.11220.21
hammer.11228.26
hammer.11245.21
hammer.11271.23
hammer.11277.24
hammer.11301.23
hammer.11339.23
hammer.11362.26
hammer.11374.48
hammer.11399.51
hammer.11413.29
hammer.11525.23
hammer.11602.25
hammer.11745.21
hammer.11925.125
```

The two Vampire timeouts are:

```text
hammer.11870.37
hammer.11888.36
```

This run was invoked with 10-way parallelism, `VAMPIRE_SECONDS=10`, and
`WALL_SECONDS=13`. It is the cleanest current E2/E3-style frontier result:
every non-timeout case in this Skolem-heavy 35-case set reconstructs as a
native proof term without the transitional-known opt-in.

This 33/35 result was reached through several incremental fixes:

- the original live run
  `/project/tmp/live-skolem-frontier-script.183108` had 24 passes, 7
  ill-formed proof terms, 1 unsupported rectification case, 1 refused
  transitional predicate-definition case, and 2 timeouts;
- after the left-last-pivot clause resolution fix, cached recheck
  `/project/tmp/live-skolem-frontier-recheck-after-resolve-fix.184218`
  moved the former 7 ill-formed proof terms to passes;
- after the rectification/FOOL fix, cached recheck
  `/project/tmp/live-skolem-frontier-recheck-after-rectify-fool.fKCRqR`
  reached 32 passes plus one refused transitional predicate-definition case;
- after predicate-definition and cached AVATAR replay, fresh live run
  `/project/tmp/live-skolem-frontier-after-avatar.200508` reached 32 passes,
  2 timeouts, and one new `hammer.11925.125` proof-opening failure at `u454`;
- after `2a354e9`, the fresh regenerated `hammer.11925.125` proof also
  checks, producing the current 33-pass frontier.

The focused `hammer.11925.125` proof-term check after `2a354e9` reports:

```text
Vampire certificate v1 source origin /project/Megalodon/examples/hammer/100thms_12_h.mg line 11925 char 125 (aby).
Vampire certificate v1 source map checked 11 sources.
Vampire certificate v1 closed checked 186 steps.
Vampire certificate v1 native preprocess proof term checked 186 steps.
Vampire certificate v1 native preprocess source bindings checked 11 assumptions.
Vampire certificate v1 native preprocess source assumptions remaining 11.
Vampire certificate v1 native preprocess source assumptions remaining by kind known=7 local=2 definition=0 generated=0 conjecture=2 unresolved=0.
Vampire certificate v1 native preprocess source propositions recorded 11 assumptions.
Everything looks good.
```

The qualification is important: the proof-term checker still reports the
source assumptions as remaining. That means this is not yet an E1 proof of
the original Megalodon theorem. It is proof-producing evidence at the
exported certificate/preprocessing boundary. The next milestone is composing
such native proof terms back into the original source context without using
`aby`, `admit`, or certificate-derived `Known`.

### Predicate-Definition and AVATAR Proof Terms

The newest focused changes addressed the remaining cached proof-producing
frontier blockers in `hammer.11925.125`.

Before this change, `predicate_definition` was still too close to a
structural or bridge step. That was incompatible with the audit direction:
Megalodon should not infer or guess a definition proof from a rendered formula,
and it should not admit the step through a dynamically installed
`vampire_predicate_definition_u269` known theorem.

Vampire commit `e3b4a8620` emits a richer `kernel_v1` record for predicate
definitions. For the representative live case `u269`, the generated
certificate includes:

```text
rule=predicate_definition
primitive_expansion=prefix
primitive_expansion_prefix=u269
primitive_expansion_requires=predicate_definition_intro
introduced_symbol=p__
sort=set->set->prop
definiendum_symbol=sP0
body_formula=...
result_formula=...
body_variable_sort_count=3
body_variable_sort_0=X0:set
body_variable_sort_1=X1:set->prop
body_variable_sort_2=X2:set
proof_shape=classical_definitional_split
classical_principle=xm
positive_branch=definition_body
negative_branch=negated_definiendum
```

Commit `aa5fbdd` made Megalodon consume this as a primitive contract:
`predicate_definition` is no longer in the structural rule set, and the shared
kernel vocabulary requires `predicate_definition_intro`. Strict validation
checks the required fields, parses the body formula, checks that the metadata
is attached to a certificate `PredicateDefinition` step, checks that
`definiendum_symbol` and `result_formula` match the certificate step, and
checks the expected classical proof-shape annotations.

Commit `d0cf6e9` then implemented the native proof-term path for the
representative predicate-definition shape. The key implementation points are:

- generated predicate symbols such as `sP0` are treated as conservative delta
  symbols, not as quantified proof variables;
- the emitted body formula and binder sorts are validated before the delta
  body is built;
- predicate definitions are added to the certificate delta context;
- the universal definitional disjunction is proved with the fixed classical
  basis (`xm`), Megalodon's disjunction, and equality-to-true reasoning;
- the matching predicate-definition fold is replayed through the native
  formula-orientation proof path rather than through a transitional `Known`
  theorem;
- the true-equality shortcut used for definition folds is restricted to
  emitted definition symbols so it does not interfere with Skolem formula
  orientation.

Commit `b8166bc` then implemented the native proof-term path for the cached
representative AVATAR layer. The key implementation points are:

- split literals are identified generically from Vampire split names, not from
  source theorem names;
- component clauses can be derived in both directions from a split definition;
- complement component literals are handled by a contrapositive step using
  the classical basis;
- split clauses are compared by multiset so identity split clauses may contain
  multiple split literals;
- multi-definition `avatar_split` steps can replace component literals by
  positive split literals or complement component literals by negative split
  literals;
- `avatar_refutation` RUP replay resolves split clauses in order and factors
  duplicate split literals after each resolution.

Commit `b53a783` extends the same proof-producing policy to
`predicate_definition_fold_chain`. This is a useful example of the desired
audit discipline. The checker first validates the fold-chain structurally,
but the counted native path then asks for the actual sequence of intermediate
formulas and elaborates each fold as a real proof step. The implementation
descends through matching `forall`, implication, `vampire_or`, `vampire_and`,
and `vampire_exists_prop` structure. At the fold point it uses the generated
predicate definition delta and a restricted true-equality wrapper when the
certificate formula encodes the folded atom as equality to `true`. The old
dynamic `vampire_predicate_definition_fold_chain_*` known facts are no longer
used in the regression fixture.

The focused validation artifacts are in:

```text
/project/tmp/predicate-definition-vampire.1NRqFk
/project/tmp/predicate-definition-metadata-audit.194853
/project/tmp/avatar-metadata-audit.1005998
/project/tmp/live-skolem-frontier-after-avatar.200508
/project/tmp/live-skolem-frontier-after-truth-conflict.201400
/project/tmp/preprocess_pf_audit_after_fold_chain.211008
/project/tmp/definition_audit_after_fold_chain.211053
/project/tmp/definition_pf_audit_after_fold_chain.211117
```

The fresh strict certificate check for `hammer.11925.125` passes:

```text
Vampire certificate v1 source origin /project/Megalodon/examples/hammer/100thms_12_h.mg line 11925 char 125 (aby).
Vampire certificate v1 source map checked 11 sources.
Vampire certificate v1 strict checked 304 steps.
Everything looks good.
```

The focused cached proof-term check now gets past:

```text
u269 predicate_definition
u270 predicate_definition_fold
u273 skolemize
u370 avatar_definition
u371 avatar_component
u372 avatar_component
u383 avatar_split
u384 avatar_refutation
```

and ends with:

```text
Vampire certificate v1 native preprocess proof term checked 295 steps.
```

The separate fold-chain smoke fixture ends with:

```text
Vampire certificate v1 native preprocess proof term checked 8 steps.
```

The focused metadata audit for that certificate reports:

```text
kernel_v1 records: 213
kernel_v1 rewrite-position records: 30
avatar_component 9
avatar_definition 8
avatar_refutation 1
avatar_split 6
cnf_clause 16
equality_resolution 27
fool_formula 8
formula_copy 5
formula_normalize 8
instantiation 23
predicate_definition 1
predicate_definition_fold 1
rectify_formula 10
rewrite 8
skolemize 2
split_dependency 58
superposition 22
```

The remaining qualification is that the 295-step AVATAR result is focused
cached evidence. The fresh regenerated proof for the same source file exposed
a later `truth_conflict` proof-opening issue at `u454`; commit `2a354e9`
fixed that issue for the current regenerated certificate, and the subsequent
live frontier now reports 33 proof-term passes and 2 Vampire timeouts.

### Real Vampire Source-Context Smoke After Subsumption Fix

A later real-Vampire source-context smoke exposed a different class of
problem. The selected 12-target suite is not a broad 100-case proof-term
frontier; it is a deliberately small source-context probe over real Vampire
THF proof output. Its value is that it checks whether generated certificates
carry enough primitive detail to support source-bound reconstruction rather
than only structural parsing.

The first run, at:

```text
/project/tmp/real_vampire_targets_e1_probe.202243/summary.tsv
```

had five passes and seven failures:

```text
10823 PASS
11228 PASS
11453 PASS
11560 PASS
11703 PASS
10809 FAIL_1
11203 FAIL_1
11555 FAIL_1
11572 FAIL_1
11578 FAIL_1
11696 FAIL_1
11706 FAIL_1
```

All seven failures had the same strict validation cause:

```text
strict certificate v1 requires primitive_expansion_final_result_clause
```

The failing steps were `forward subsumption resolution` cases. The root cause
was in Vampire, not in Megalodon reconstruction: the subsumption fast path
returned a rendered native proof step but did not populate the primitive
expansion vector when strict metadata was requested. Megalodon therefore saw
only weak macro metadata, not the primitive final result clause and
per-substep parent/result/pivot fields needed for a small-kernel checker.

Vampire commit `d48f82edf` fixed this by making the fast path construct
primitive `substitute` and `resolve` records before printing. It also removed
the old condition that disabled the fast path when primitive metadata was
requested. The regenerated certificate for `hammer.10809.92` now contains
fields such as:

```text
primitive_expansion_final_result_clause=(clause)
primitive_expansion_step_0_parent_count=2
primitive_expansion_step_0_result_clause=(clause)
primitive_expansion_step_0_pivot_left=0
primitive_expansion_step_0_pivot_right=0
```

The follow-up parallel run, using the new Vampire binary, passed all 12
targets:

```text
/project/tmp/real_vampire_targets_after_subsumption_metadata.202729/summary.tsv

10809 PASS steps=11 sources=3 known=0 local=1 local_definition=0 conjecture=2 unresolved=0
10823 PASS steps=9  sources=3 known=0 local=1 local_definition=0 conjecture=2 unresolved=0
11203 PASS steps=22 sources=3 known=1 local=0 local_definition=0 conjecture=2 unresolved=0
11228 PASS steps=37 sources=3 known=1 local=0 local_definition=0 conjecture=2 unresolved=0
11453 PASS steps=65 sources=8 known=6 local=0 local_definition=0 conjecture=2 unresolved=0
11555 PASS steps=11 sources=3 known=1 local=0 local_definition=0 conjecture=2 unresolved=0
11560 PASS steps=5  sources=2 known=0 local=0 local_definition=0 conjecture=2 unresolved=0
11572 PASS steps=11 sources=3 known=0 local=1 local_definition=0 conjecture=2 unresolved=0
11578 PASS steps=15 sources=4 known=0 local=2 local_definition=0 conjecture=2 unresolved=0
11696 PASS steps=11 sources=3 known=0 local=1 local_definition=0 conjecture=2 unresolved=0
11703 PASS steps=44 sources=7 known=1 local=4 local_definition=0 conjecture=2 unresolved=0
11706 PASS steps=13 sources=3 known=0 local=1 local_definition=0 conjecture=2 unresolved=0
```

This result should be classified carefully. It is good evidence that the
Vampire-side primitive metadata path is the right place to fix missing proof
detail, and it checks real source-context categories including known facts,
local hypotheses, and conjecture facts. It is not yet an E1 proof of those
original theorems because final theorem composition without `aby` remains
unfinished.

### Certified `vampire` Source Command

After the subsumption metadata fix, Megalodon grew a separate proof command:

```text
vampire Hp.
```

This command is intentionally stricter than the historical `aby` surface. It
does not mean "try Vampire, then accept incomplete automation." The command
routes through the live Vampire certificate path and fails unless all of the
following hold:

- `-vampireaby` is supplied;
- `-vampireabyproof megalodon` is selected;
- native certificate reconstruction is enabled and succeeds;
- target-stop filtering does not skip the command;
- the result is a native proof term, not a textual script, `aby`, `admit`, or
  fallback reconstruction.

The origin export was also parameterized so that the artifact stream records
which surface command produced it. A certified `vampire` command now emits
`vampire.<line>.<char>.<digest>.thf.p` and includes:

```text
% megalodon_origin ... (kind "vampire")
```

The legacy certified `aby` path still emits `aby.*` artifacts with
`(kind "aby")`. This matters for auditability: no-`aby` tests can now assert
that the proof obligation actually entered the new `vampire` command path.

The current real-Vampire target harness supports both surfaces through
`SOURCE_COMMAND=aby|vampire`. For the `vampire` mode it rewrites the selected
target line from `aby` to `vampire`, adjusts the expected parser character
offset, runs the same real Vampire proof search with a 10-second timeout, and
checks:

- real source-context resolution counts;
- native certificate reconstruction at the shifted source location;
- command-specific certification log lines;
- target-stop after reconstructed proof term;
- exactly one THF/proof output pair;
- native certificate block presence;
- `vampire.*.thf.p` artifact naming;
- `kind "vampire"` origin metadata.

The latest 12-target `SOURCE_COMMAND=vampire` parallel run is:

```text
/project/tmp/real_vampire_targets_vampire_command_summary.205228

10809 PASS line=10809 char=96  steps=11 sources=3 known=0 local=1 local_definition=0 conjecture=2 unresolved=0
10823 PASS line=10823 char=96  steps=9  sources=3 known=0 local=1 local_definition=0 conjecture=2 unresolved=0
11203 PASS line=11203 char=29  steps=22 sources=3 known=1 local=0 local_definition=0 conjecture=2 unresolved=0
11228 PASS line=11228 char=30  steps=37 sources=3 known=1 local=0 local_definition=0 conjecture=2 unresolved=0
11453 PASS line=11453 char=81  steps=65 sources=8 known=6 local=0 local_definition=0 conjecture=2 unresolved=0
11555 PASS line=11555 char=46  steps=11 sources=3 known=1 local=0 local_definition=0 conjecture=2 unresolved=0
11560 PASS line=11560 char=35  steps=5  sources=2 known=0 local=0 local_definition=0 conjecture=2 unresolved=0
11572 PASS line=11572 char=92  steps=11 sources=3 known=0 local=1 local_definition=0 conjecture=2 unresolved=0
11578 PASS line=11578 char=61  steps=15 sources=4 known=0 local=2 local_definition=0 conjecture=2 unresolved=0
11696 PASS line=11696 char=243 steps=11 sources=3 known=0 local=1 local_definition=0 conjecture=2 unresolved=0
11703 PASS line=11703 char=246 steps=44 sources=7 known=1 local=4 local_definition=0 conjecture=2 unresolved=0
11706 PASS line=11706 char=274 steps=13 sources=3 known=0 local=1 local_definition=0 conjecture=2 unresolved=0
```

The matching legacy-surface regression run is:

```text
/project/tmp/real_vampire_targets_aby_after_summary.205304
```

This is an important interface milestone because it shows that real live
Vampire proof output can be connected to a no-`aby` Megalodon command in
selected source contexts. It is still not counted as E1 proof reconstruction:
the harness stops after the selected reconstructed proof term and does not
yet compose a completed proof of the original theorem in arbitrary source
developments.

### Cached and Layered Audits

The branch also has staged cached audits for core, preprocessing,
Skolemization, definitions, AVATAR, inequality, and rewrite/definition layers.
These are useful for identifying the first unsupported inference in
certificate order. They should be treated as regression and frontier
diagnostics unless the invoked mode explicitly checks native proof terms
without transitional knowns.

The cached Skolem-selected cases are now known to contain stale certificates
from before the current Vampire `VLAMV`/explicit-source Skolem output. For
Skolem evidence, the live regenerated frontier is more meaningful than old
cached results.

### Build and Smoke Evidence

Recent committed work was validated with combinations of:

```sh
TMPDIR=/project/tmp ./makeopt
TMPDIR=/project/tmp tests/vampire_certificate/run_native_cert_v1_smoke.sh
TMPDIR=/project/tmp tests/vampire_certificate/run_native_live_skolem_pf_smoke.sh
TMPDIR=/project/tmp WORK_DIR=/project/tmp/live-skolem-frontier-script.183108 JOBS=10 MIN_PASS=24 \
  tests/vampire_certificate/run_native_live_skolem_pf_frontier.sh
```

The latest predicate-definition, AVATAR, and truth-conflict proof-term changes
were validated with:

```sh
TMPDIR=/project/tmp ./makeopt
TMPDIR=/project/tmp tests/vampire_certificate/run_native_cert_v1_smoke.sh
TMPDIR=/project/tmp tests/vampire_certificate/run_native_live_skolem_pf_smoke.sh
OUT=$(cat /project/tmp/latest_predicate_definition_vampire_dir)
/project/Megalodon/bin/megalodon -vampirecertv1preprocesspfcheck -vampirecertv1strict \
  -vampirecertv1 "$OUT/native.sexp" \
  -vampirecertv1source /project/Megalodon/tests/vampire_certificate/closed_cases/hammer.11925.125.th0.p \
  "$OUT/empty.mg"
WORK_DIR=/project/tmp/avatar-metadata-audit.1005998 \
  TMPDIR=/project/tmp MIN_KERNEL_V1=1 MIN_REWRITE_POSITION=0 \
  tests/vampire_certificate/run_kernel_v1_metadata_audit.sh "$OUT/native.sexp"
TMPDIR=/project/tmp WORK_DIR=/project/tmp/live-skolem-frontier-after-avatar.200508 \
  JOBS=10 MIN_PASS=32 VAMPIRE_SECONDS=10 WALL_SECONDS=13 \
  tests/vampire_certificate/run_native_live_skolem_pf_frontier.sh
TMPDIR=/project/tmp WORK_DIR=/project/tmp/live-skolem-frontier-after-truth-conflict.201400 \
  JOBS=10 MIN_PASS=33 VAMPIRE_SECONDS=10 WALL_SECONDS=13 \
  tests/vampire_certificate/run_native_live_skolem_pf_frontier.sh
```

and cached checker-only replays:

```text
/project/tmp/resolve-cluster-after-fix.184159/summary.tsv
/project/tmp/live-skolem-frontier-recheck-after-resolve-fix.184218/counts.txt
/project/tmp/live-skolem-frontier-recheck-after-rectify-fool.fKCRqR/counts.txt
/project/tmp/live-skolem-frontier-after-avatar.200508/counts.txt
/project/tmp/live-skolem-frontier-after-truth-conflict.201400/counts.txt
```

The focused cached proof-term run for `hammer.11925.125` now passes through
the former AVATAR component/refutation blocker. The fresh regenerated proof
for that same source now also passes after the truth-conflict parent-opening
fix.

Vampire-side extraction commits were validated with parallel builds such as:

```sh
TMPDIR=/project/tmp make -j10 vampire_rel
```

and small live THF runs followed by primitive metadata audits.

The latest subsumption fast-path metadata fix was validated with:

```sh
TMPDIR=/project/tmp make -j10 vampire_rel
TMPDIR=/project/tmp VAMPIRE=/project/vampire-leancheck/vampire_rel_vampire/megalodon5_11079 \
  MEGALODON_VAMPIRE_TIMEOUT=10 TARGET_LINE=10809 TARGET_CHAR=92 TARGET_LABEL=10809 \
  EXPECTED_STEPS=11 EXPECTED_SOURCES=3 \
  EXPECTED_CONTEXT='known=0 local=1 local_definition=0 conjecture=2 unresolved=0' \
  WORK_DIR=/project/tmp/real_vampire_10809_after_subsumption_metadata.202717 \
  /project/Megalodon/tests/vampire_certificate/run_real_vampire_11703_target_smoke.sh
TMPDIR=/project/tmp VAMPIRE=/project/vampire-leancheck/vampire_rel_vampire/megalodon5_11079 \
  JOBS=10 MEGALODON_VAMPIRE_TIMEOUT=10 \
  WORK_DIR=/project/tmp/real_vampire_targets_after_subsumption_metadata.202729 \
  /project/Megalodon/tests/vampire_certificate/run_real_vampire_targets_smoke.sh
```

The separate `vampire` proof-command path and command-origin export were
validated with:

```sh
TMPDIR=/project/tmp ./makeopt
TMPDIR=/project/tmp tests/vampire_certificate/run_native_cert_v1_smoke.sh
TMPDIR=/project/tmp tests/vampire_certificate/run_source_map_export_smoke.sh
TMPDIR=/project/tmp VAMPIRE=/project/vampire-leancheck/vampire_rel_vampire/megalodon5_11079 \
  SOURCE_COMMAND=vampire JOBS=10 MEGALODON_VAMPIRE_TIMEOUT=10 \
  WORK_DIR=/project/tmp/real_vampire_targets_vampire_command_summary.205228 \
  /project/Megalodon/tests/vampire_certificate/run_real_vampire_targets_smoke.sh
TMPDIR=/project/tmp VAMPIRE=/project/vampire-leancheck/vampire_rel_vampire/megalodon5_11079 \
  JOBS=10 MEGALODON_VAMPIRE_TIMEOUT=10 \
  WORK_DIR=/project/tmp/real_vampire_targets_aby_after_summary.205304 \
  /project/Megalodon/tests/vampire_certificate/run_real_vampire_targets_smoke.sh
```

The latest fold-chain proof-term work was validated with:

```sh
TMPDIR=/project/tmp ./makeopt
TMPDIR=/project/tmp tests/vampire_certificate/run_native_cert_v1_smoke.sh
TMPDIR=/project/tmp /project/Megalodon/bin/megalodon \
  -vampirecertv1preprocesspfcheck \
  -vampirecertv1strict \
  -vampirecertv1 tests/vampire_certificate/native_cert_v1_predicate_definition_fold_chain_valid.sexp \
  -vampirecertv1source tests/vampire_certificate/native_cert_v1_predicate_definition_fold_chain_valid.th0.p \
  /project/tmp/latest_native_cert_v1/native_cert_v1_dummy.mg
TMPDIR=/project/tmp JOBS=10 MIN_PREPROCESS=10 \
  WORK_DIR=/project/tmp/preprocess_pf_audit_after_fold_chain.211008 \
  tests/vampire_certificate/run_native_cert_v1_preprocess_closed_audit.sh
TMPDIR=/project/tmp JOBS=10 MIN_DEFINITION=10 \
  WORK_DIR=/project/tmp/definition_audit_after_fold_chain.211053 \
  tests/vampire_certificate/run_native_cert_v1_definition_closed_audit.sh
TMPDIR=/project/tmp JOBS=10 MIN_PREPROCESS_STRUCTURAL=12 \
  CASE_LIST=/project/tmp/definition_audit_after_fold_chain.211053/definition_closed_cases.list \
  WORK_DIR=/project/tmp/definition_pf_audit_after_clean_rebuild.213458 \
  tests/vampire_certificate/run_native_cert_v1_preprocess_pf_audit.sh
```

All three audits pass their stated minima after the compact Skolem fix. The
former `hammer.10806.144/u311` compact-source Skolemization failure is now a
regression test rather than the active blocker.

The latest rectification variable-map work was validated with:

```sh
TMPDIR=/project/tmp make -j10 vampire_rel
TMPDIR=/project/tmp /project/Megalodon/tests/vampire_certificate/run_native_live_rectify_varmap_smoke.sh
TMPDIR=/project/tmp WORK_DIR=/project/tmp/live_preprocess_after_varmap_20260717_222323 \
  LIMIT=100 JOBS=10 VAMPIRE_SECONDS=10 WALL_SECONDS=12 \
  VAMPIRE=/project/vampire-leancheck/vampire_rel_vampire/megalodon5_11080 \
  /project/Megalodon/tests/vampire_certificate/run_native_live_preprocess_pf_frontier.sh
```

The broad run reports 83 native preprocess proof-term passes, nine checker
failures, and eight no-certificate cases. This report relies on that artifact,
the earlier named artifacts, and the committed validation history.

## Linking to Original Megalodon Lemmas, Definitions, and Theorems

This is one of the central requirements, and it is only partially done.

### What Works

The source-context resolver can load a Megalodon file, inspect known facts and
local proof context, and resolve some certificate source facts back to actual
Megalodon proofs.

The current system can:

- resolve global knowns by hash/original name/TPTP name;
- verify that a resolved known proves the exported source proposition;
- handle source-map aliases produced by name mangling;
- match local hypotheses in simple and shifted contexts;
- account for local type declarations;
- account for local `set` definitions as source-map facts;
- prove generated set-reflexivity equalities by reflexivity/conversion when
  possible;
- omit source facts from the proof spine when they are already discharged by
  source-context proofs;
- leave negated conjecture facts for the final refutation-to-goal boundary.

This is materially better than a label-only source map. It reduces the risk
that the certificate attaches an arbitrary formula to a valid source name.

### What Is Still Missing

The source glue is not yet a complete E1 implementation for arbitrary larger
developments.

Open areas include:

- full proof production for local definitions when used by the refutation;
- full proof production for `set` equalities beyond simple reflexivity or
  conversion cases;
- robust handling of every export name-mangling pattern;
- final composition of a closed native certificate into the exact original
  Megalodon target without depending on `aby`;
- handling larger local contexts where hypotheses, local definitions, and
  generated facts interleave;
- systematic proof of all source-to-clause transformation facts before the
  clausal kernel begins.

The project has therefore made progress on "linking" but has not completed
the original-context proof.

## Equality and Logical Constants

Earlier code paths risked introducing private equality/logical encodings.
The current policy is that the resulting proof must use Megalodon's equality
and logical constants.

The certificate still has an internal representation of equality and clauses,
because it must parse Vampire/TPTP terms. That is not the same as introducing
a new trusted equality. The counted proof-term path should translate
certificate equality into Megalodon's equality and use Megalodon's native
proof rules and library facts.

The same applies to:

- `False`;
- disjunction;
- implication;
- classical principles such as `xm` and double negation;
- proposition extensionality where explicitly used;
- choice principles for Skolemization.

The remaining risk is not policy; it is implementation coverage. Some
normalization/matching logic still lives in the broad checker. The future
small-kernel elaborator should make the equality boundary explicit and small.

## Skolemization Status

Skolemization has moved from "mostly blocked or transitional" to "partially
proof-producing on live regenerated cases."

The current Skolem path uses:

- Vampire-emitted `skolem_formula` steps;
- explicit source formulas;
- explicit substitution lists;
- introduced-symbol metadata;
- dependency names and sorts where available;
- conservative generated definitions for Skolem symbols;
- Megalodon choice principles;
- normalization of generated Skolem definitions for formula matching.

The latest work handles:

- direct existential Skolemization;
- nested existential orientation in several formula contexts;
- nullary Skolem symbols;
- dependent Skolem applications where expansion must wait for enough
  arguments;
- multiple Skolem introductions discovered by walking the source formula in
  preorder.
- compact `skolem_formula` records whose explicit source formula is absent
  but whose checked parent formula is the source formula;
- compact generated Skolem definitions in the representative
  `hammer.10806.144` definition-heavy cached family;
- rectification steps that require explicit Vampire-emitted variable maps,
  represented by the fresh `hammer.1007.43` run.

The main remaining Skolem-adjacent blockers are:

- Skolemization under conjunction contexts, represented by
  `hammer.1110.18/u85`, `hammer.1114.18/u85`, `hammer.1118.18/u85`, and
  `hammer.1122.18/u85`;
- Skolem body/dependency-context checks, represented by `hammer.10785.27`
  and `hammer.11145.52`;
- one remaining formula-orientation shape beyond equality symmetry, true
  equality elimination, and matching logical structure, represented by
  `hammer.1009.43/u98`;
- proof-term opening/shifting in later clausal steps after Skolemization,
  where the local fixes for resolution and truth-conflict should be extracted
  into a general `open_step_theorem` operation;
- broader predicate-definition shapes beyond the current focused live
  representative;
- broader source/preprocessing proof chains that mix Skolemization with
  AVATAR, definitions, and CNF in more complex ways.

The latest broad live preprocessing frontier after the rectification
variable-map fix is 83/100 native preprocess proof-term passes, with nine
checker failures and eight no-certificate cases. It is encouraging, but it is
not the end state because source assumptions are still reported as remaining
and E1 original-context composition is incomplete.

## Prover9/Ivy Path and Small Kernel

The project is now broadly on the Prover9/Ivy path, but not far enough along.

The desired small clausal kernel is:

- `input`: boundary from a proved source/preprocessing clause;
- `instantiate`: explicit substitution;
- `resolve`: binary resolution;
- `factor`: duplicate literal contraction;
- `equality_resolution`: contradiction from a negative reflexive equality;
- `equality_factoring`: equality-specific factoring;
- `paramodulate`: equality rewrite at an explicit position;
- `contradiction`: final empty-clause marker.

Additional names need careful placement:

- `rename` should be alpha-conversion or a special case of instantiation, not
  a broad logical rule.
- `truth_conflict` probably belongs in the FOOL/preprocessing layer unless
  real clausal proofs force it into the kernel.
- AVATAR should be a separate SAT/RUP-style layer or explicit split/refutation
proof layer, not part of the first kernel milestone.

### Truth-Conflict Proof Opening

Commit `2a354e9` fixed the fresh regenerated `hammer.11925.125` failure that
appeared after the AVATAR layer started replaying natively. The failing step
was `u454`, a `truth_conflict` step whose parent proof was still quantified
over step variables. The old replay path tried to use the stored parent proof
as if it were already opened in the result context, and Megalodon rejected the
application with:

```text
Proof term for a universally quantified proposition applied to a term of the wrong type
```

The fix added a wrapper that:

- closes parent and result clauses under the result step variables;
- opens the parent theorem body in the result context;
- runs the existing native truth-conflict proof constructor;
- rebinds result step variables afterward.

Both core and preprocessing `TruthConflict` replay now use that wrapper. This
is a general proof-context fix, not a special case for the theorem name. It
also reinforces the small-kernel design point: stored step theorems need one
well-specified opening operation, and individual inference replay functions
should not each invent their own shifting/opening discipline.

The most important missing operation is:

```ocaml
open_step_theorem :
  global_context ->
  parent_step_variables ->
  result_step_variables ->
  substitution ->
  stored_proof ->
  opened_proof
```

This operation must define exactly how a stored step theorem is opened in the
context of a later step:

- retained variables are opened without stale shifts;
- substituted variables are closed in the result context;
- dropped variables require explicit substitutions or fail closed;
- proof-hypothesis indices are shifted correctly;
- globals and local result variables have a documented shadowing rule.

The former seven-case Skolem-frontier `resolve` cluster is strong evidence
that this operation still needs to be specified and extracted cleanly. The
cluster is fixed, but the fix was still a local correction inside the large
checker rather than the reusable operation the small-kernel design needs.

## Problems and Technical Debt

### The Monoliths Are Still Too Large

The biggest code-structure problem remains module size and responsibility
mixing.

On the Megalodon side, `src/vampire_cert_v1.ml` still contains parsing,
source maps, structural validation, proof-term checking, preprocessing logic,
textual emission, source-context logic, and multiple test-facing modes.

On the Vampire side, `MegalodonChecker.cpp` is still the main proof exporter.
`MegalodonKernelSyntax` is a good extraction, but it is still mostly a
rendered-record boundary.

The next work should reduce these modules, not add another several thousand
lines to them.

### E4 Pass Counts Are Easy to Misread

The project now has many useful runs that say `PASS`, but only some are proof
reconstruction passes. The report and harness names must be disciplined:

- `PASS 100` in strict live mode is certificate integration.
- `PREPROCESS_STRUCTURAL_PASS` is structural/preprocessing diagnostic.
- `SKOLEM_PF_PASS` is a focused native proof-term frontier.
- E1 requires original-context proof composition without `aby`/`admit`/bridges.

This distinction should be visible in every future report.

### Python Must Stay Quarantined

Python utilities are acceptable for corpus inspection, summaries, and
debugging. They should not grow new logical reconstruction power, and no
Python-generated proof should count in the qualifying path.

### Smolka Transformations Are Still Partial

The branch has meaningful pieces for formula transforms, rectification, FOOL,
ENNF, CNF, and Skolemization. It does not yet have a complete proof-producing
source-to-clause pipeline.

The safe ordering is:

1. source formula proof;
2. rectification proof;
3. FOOL proof;
4. ENNF proof;
5. CNF projection proof;
6. Skolem proof;
7. clausal kernel proof.

Every boundary must produce a proof term or fail closed.

### AVATAR Is Not Yet a Kernel Story

AVATAR metadata is improving, but final proof production is still a separate
problem. The safest direction is an explicit SAT/RUP object for the Boolean
part and a proof-producing bridge from split clauses back to the clausal
kernel. The current AVATAR support should remain outside the first small
kernel milestone.

## Recommended Next Milestones

The next milestones should be strict and narrow.

1. Implement Skolemization under conjunctive contexts.
   The immediate repeated target is the `hammer.1110.18`,
   `hammer.1114.18`, `hammer.1118.18`, and `hammer.1122.18` family, all at
   step `u85`. The shape is `vampire_and A (exists X, P X)` becoming
   `vampire_and A (P sK)`. This should be replayed by eliminating the
   conjunction, applying the existing choice/Skolem proof to the existential
   conjunct, and reintroducing the conjunction. The implementation should be
   general over left/right conjunct positions and must not introduce
   certificate-derived `Known`.

2. Extract `open_step_theorem`.
   Do not add more local proof-shifting special cases. Specify the operation
   once and reuse it for `resolve`, `factor`, `instantiate`,
   `equality_resolution`, `equality_factoring`, `paramodulate`, `flip`, and
   `truth_conflict`. The `u454` truth-conflict fix should become an instance
   of this operation rather than remain a local wrapper.

3. Keep predicate-definition, fold-chain, AVATAR, and truth-conflict proof terms under
   regression and
   broaden only from new certificate evidence.
   The current representative `predicate_definition_intro` path is native and
   should remain free of certificate-derived `Known`. The current fold-chain,
   split/refutation replay and truth-conflict opening path should also stay
   proof-producing. The next broadening step should be driven by new
   Vampire-emitted primitive metadata for additional shapes, not by
   Megalodon-side guessing.

4. Turn one live proof into a complete E1 proof without `aby`.
   Choose a small but real original-context target with source facts and at
   least one generated set/reflexivity or local-definition fact.

5. Convert the Vampire rendered wrapper records into typed primitive records.
   The current `MegalodonKernelStep` builder is a useful migration boundary.
   The next step is typed terms, literals, substitutions, positions, and
   parent references, not more string metadata.

6. Move Megalodon kernel checking out of `vampire_cert_v1.ml`.
   Create `vampire_kernel_check.ml`, `vampire_kernel_elab.ml`, and
   `vampire_source_context.ml`, then migrate functionality without adding
   new replay power during the move.

7. Prove ten live exported certificates as E2.
   These should be real Vampire proofs, closed native proof terms, no
   transitional knowns, no textual-emitter fallback.

8. Compose ten live original-context theorems as E1.
   Only after this should the project rerun the 100-theorem held-out gate as
   proof reconstruction rather than structural validation.

9. Keep broad tests parallel and bounded.
   Use `/project/tmp`, `JOBS=10` or higher where suitable, and Vampire
   timeouts of at most 10 seconds. Reuse generated artifacts when debugging
   checker failures.

## Outlook

The project is now pointed in the right direction. The main risk is relapse:
adding more Megalodon-side guessing to raise short-term pass counts. That
would repeat the earlier failure mode and make the system harder to audit.

The promising path is narrower:

- make Vampire print exact primitive proof data;
- keep the small kernel small;
- make Megalodon check native proof terms only from explicit data;
- treat source/preprocessing transformations as proof-producing layers;
- compose back into the original Megalodon context;
- use clear evidence labels.

With the clustered Skolem/resolve proof-opening bug fixed, the compact-source
Skolemization blocker fixed, the current predicate-definition and fold-chain
cases elaborated natively, the cached AVATAR component/refutation layer
replayed, the fresh truth-conflict `u454` failure fixed, the subsumption
fast-path metadata failure moved to Vampire where it belongs, and
`hammer.1007.43` rectification repaired through Vampire-emitted variable
maps, the next iteration should turn these frontier successes into cleaner
architecture and E1 evidence. The immediate tactical step is Skolemization
under conjunction contexts; the immediate architectural step is still to
extract the proof-opening rule and then compose native proof terms back into
original Megalodon theorem contexts. If instead the next iteration broadens
the OCaml reconstructor again with heuristic matching, the audits' main
concern will remain unresolved.

My current assessment is:

- E4 integration is strong: 100 live THF certificates can be generated and
  structurally validated, a 12-target real Vampire source-context probe now
  passes after the subsumption primitive metadata fix, and the same selected
  targets pass through the explicit no-`aby` `vampire` proof-command surface
  with `kind "vampire"` origin metadata.
- E3/E2 proof-term evidence is real but still narrow: native core, URR slices,
  33 Skolem-heavy fresh live frontier passes, one focused cached
  predicate-definition/fold/AVATAR representative that checks 295 native
  preprocess proof-term steps, a separate fold-chain regression that checks 8
  native preprocess proof-term steps, 84 cached eligible preprocess-fragment
  proof-term passes after the fold-chain fix, 12 cached definition-layer
  proof-term passes after the compact-Skolem fix, a focused
  `hammer.10806.144` compact-Skolem regression that checks 45 native
  preprocess proof-term steps, and a regenerated `hammer.11925.125`
  truth-conflict proof that now checks 186 native preprocess proof-term steps,
  plus a fresh regenerated `hammer.1007.43` rectification-variable-map proof
  that checks 68 native preprocess proof-term steps.
- E1 original-context reconstruction is partially implemented and not yet
  delivered at scale.
- The project should continue, but under the stricter small-kernel and
  source-context plan described above.
