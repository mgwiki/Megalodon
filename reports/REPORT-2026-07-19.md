# Vampire to Megalodon Proof Reconstruction: Expert Status Report

Date: 2026-07-19

Requested path: `/project/reports/REPORT-2026-07-19.md`

Versioned copy: `/project/Megalodon/reports/REPORT-2026-07-19.md`

Primary repositories:

- Megalodon: `/project/Megalodon`
- Vampire fork: `/project/vampire-leancheck`
- Generated and temporary artifacts: `/project/tmp`

Current active branches:

- Megalodon: `vampire/megalodon5`
- Vampire fork: `vampire/megalodon5`

Current implementation heads used for this report refresh:

- Megalodon committed implementation baseline:
  `902f346 Transport nested Skolem equality branches`
- Vampire fork: `6f74b74bf Emit Skolem parent instantiation contracts`

Important working-tree note:

- This report is written primarily against the current committed
  implementation state, with one clearly marked uncommitted Megalodon
  binder-shift experiment noted below.
- The source-local candidate restriction described below is committed as
  `bff2db2 Prefer source locals for refutation instantiation`.
- The dependent Skolem witness acceptance described below is committed as
  `6d79df9 Accept dependent Skolem witness applications`.
- The CNF formula-clause projection candidate fix described below is committed
  as `8a58aeb Try all CNF result binders for formula projection`.
- The reconstruction-delta alias merge described below is committed as
  `433316d Merge reconstruction deltas through aliases`.
- The later Skolem CPS work is committed as:
  `d387edd Replay Skolem CPS under forall binders`,
  `45620c1 Replace dependent Skolem heads with lambdas`, and
  `cffc1cc Orient Skolem base equality branches`, followed by
  `902f346 Transport nested Skolem equality branches`.
- At report-write time the Megalodon worktree also contains one uncommitted
  experiment in `src/vampire_cert_v1.ml`: a one-line shift of the Skolem CPS
  result formula under a source-only existential binder. It is not counted as
  a milestone until it builds, passes smoke, preserves the `10654:21`
  regression, and moves or closes the `11925:125` blocker.
- The Megalodon branch is ahead of its remote and contains many committed
  reconstruction changes.
- Both worktrees contain many untracked generated hammer exports, Vampire
  outputs, build artifacts, and scratch files. Those are not counted as source
  implementation changes.
- All substantial generated test artifacts mentioned here are under
  `/project/tmp`, following the project requirement.

This report is intended for expert review. It takes into account the earlier
reports, the July 12 through July 16 audit reports and audit responses in
`/project/Megalodon/reports`, the Prover9/Ivy analysis documents, the small
kernel plans, the Smolka-style transformation discussions, and the most recent
source-context and Skolemization work on `vampire/megalodon5`.

This file supersedes earlier July 19 versions of the same report. It preserves
the substantive analysis from the previous refresh and updates it for the
current source-linking work. The main implementation changes since the first
July 19 expert report are `5f33fe0`, which introduced source/current-goal
terms as candidates for refutation instantiation, `3466f58`, which binds local
source hypotheses into native certificate checking and adds a checked
equality-symmetry transport fallback in the source-audit replay path,
`bff2db2`, which removes broad closed-subterm mining from the theorem-local
refutation-instantiation path, and `6d79df9`, which allows dependent Skolem
witness applications such as `sK1 X1` and `sK2 X0` in the native Skolem CPS
proof path, and `8a58aeb`, which tries all result binders of the required type
when projecting CNF formula clauses under nested quantifiers, and `433316d`,
which merges reconstruction delta entries through source-name aliases so
folded Megalodon definitions and exported certificate names can meet at the
final theorem-site boundary. Together they
move one focused original-site target forward, retire the earlier
local-hypothesis indexing experiment, remove a bad source-subterm candidate
class, advance the main failing target past the previous
nullary-witness-only Skolem blocker, and remove the immediate
`cnf_formula_clause` binder-selection failure. The harder target
`11925:125` still fails. The current committed frontier has two linked
aspects: final theorem-site source-goal composition is still not proved, and a
strict fail-fast Skolem diagnostic exposes a remaining nested dependent-binder
issue at step `u276`. The earlier non-propositional-parent opening class has
moved; the current failure is a mismatch between a source branch opened under
a source-only existential and a result formula whose outer dependency variable
must be shifted into that local comparison context.

This refresh does not claim new broad proof-reconstruction pass counts beyond
the checks already recorded below. Its purpose is to give the expert reviewer
a stable, candid state report after the branch had accumulated the local-source
binding work and the post-audit trust-boundary corrections.

The report is deliberately conservative. The project has made real technical
progress, but it has not completed the original goal.

## Executive Summary

The original project goal remains:

```text
Given a theorem in an original Megalodon development, export the theorem and
its context as a THF/TPTP problem, let Vampire solve it with a per-problem
timeout of at most 10 seconds, emit a detailed certificate, import the
certificate into Megalodon, elaborate it into Megalodon proof terms, and check
the proof of the original theorem in the original Megalodon context.
```

That goal is not finished.

There is still no broad suite of 100 original Megalodon theorems reconstructed
from Vampire proofs with all of the following restrictions:

- no `aby`;
- no `admit`;
- no `-allowincompleteqed`;
- no Python proof-producing step;
- no bridge assumptions;
- no certificate-derived `Known` proposition;
- no hidden extra premise;
- no library-specific hardwired reconstruction pattern;
- no unverified mapping from an exported THF axiom back to the original
  Megalodon theorem, lemma, definition, local hypothesis, or generated fact.

The most important progress is architectural and trust-boundary progress:

- The Python proof-producing path has been quarantined from qualifying runs.
- Vampire now emits a native Megalodon-oriented S-expression certificate with
  rich `kernel_v1` metadata.
- Megalodon parses that certificate in OCaml and uses native `Syntax.tm` and
  `Syntax.pf` values for the counted native proof-term paths.
- Counted native paths now reject certificate-derived `Known "vampire_*"`
  propositions rather than treating them as proved implications.
- Source maps are no longer just name links. Supported exported formulas are
  semantically checked against certificate inputs, and source-context checking
  can resolve many global facts to actual Megalodon `Known hash` proofs.
- Generated equalities from Megalodon `set` commands are treated as generated
  reflexivity/conversion obligations, not as arbitrary source claims.
- Local source hypotheses are now part of the native source context, including
  standalone local source facts that appear in the exported problem but are not
  global library theorems.
- Some original-site reconstruction works. A focused live target at
  `examples/hammer/100thms_12_h.mg:10654:21` reconstructs through the native
  Vampire path in the original Megalodon process.

The most important remaining gap is also architectural:

- The project still lacks a small, stable, complete Prover9/Ivy-style
  primitive proof-object layer into which Vampire lowers its internal
  inferences.
- Too much proof-specific logic remains inside Megalodon's large
  `src/vampire_cert_v1.ml` importer and Vampire's large
  `Shell/MegalodonChecker/MegalodonChecker.cpp` exporter.
- Skolemization and Smolka-style source/preprocessing transformations are only
  partially proof-producing.
- Final composition from the exported refutation back into the original
  Megalodon theorem still fails on representative larger examples, currently
  because definition-expanded exported conjectures are not yet transported
  back to the original theorem statements in a checked, source-linked way.

The current system should be viewed as a substantially improved prototype and
test laboratory, not as a finished proof reconstruction system.

## Evidence Classes Used in This Report

The previous audits repeatedly found that reports were too optimistic because
they grouped parsing, script generation, structural validation, and actual
proof reconstruction under the same word "pass." This report uses the stricter
evidence classes now adopted by the branch.

### E1: Original-Context Reconstruction

This is the actual goal.

```text
The proof checks at the original Megalodon theorem site, using the original
global lemmas, local hypotheses, definitions, generated facts, and conjecture
binding. It uses no aby, no admit, no incomplete QED, no bridge premise, no
certificate-derived Known proposition, and no Python proof-producing step.
```

Current status:

- There is no broad E1 suite.
- There is at least one focused original-site success with the native Vampire
  path, but it was run in a targeted hammer mode and is not enough to count as
  a final theorem-suite milestone.
- A representative larger target still fails at final source-goal composition.

### E2: Exported-Problem Native Proof Reconstruction

```text
Megalodon checks native proof terms derived from a Vampire certificate against
the exported THF/TPTP problem and source map. This proves the exported problem
or a post-preprocessing refutation, but it has not necessarily been composed
back into the original theorem command.
```

Current status:

- There is meaningful E2-like evidence for cached and focused certificates.
- Recent source-context checks make this evidence stronger than the earlier
  textual skeleton results.
- It still does not replace original-context proof checking.

### E3: Focused Proof-Term Regression

```text
A synthetic, cached, or narrowly selected certificate exercises one inference
family, one preprocessing transformation, one Skolemization pattern, or one
known blocker.
```

Current status:

- This is where most successful current development lives.
- These regressions are useful and should be kept.
- They should not be reported as 100-theorem proof reconstruction.

### E4: Structural and Integration Evidence

```text
Vampire emits certificates, Megalodon parses them, metadata is present,
source maps are audited, trust policy checks run, or `kernel_v1` records pass
schema and consistency checks.
```

Current status:

- The branch has a lot of E4 evidence.
- E4 is important for finding breakage and measuring coverage.
- E4 does not prove theorems.

## What Has Been Done

The project has gone through several distinct phases.

### Phase 1: Python Skeleton Translation

The initial path took Vampire proof output and translated it into Megalodon
scripts, often as declarative `claim` skeletons. This quickly produced useful
human-readable artifacts and helped identify common inference classes:
resolution, factoring, superposition, demodulation, CNF, FOOL, AVATAR,
Skolemization, and source/preprocessing transformations.

This phase was useful for exploration but not acceptable as the final path.
The Python script accumulated a large amount of logical reconstruction logic,
including bounded proof search, equality transport, quantified proof search,
and library-pattern recognition. That contradicted the requirement that the
reconstruction be general and driven by Vampire's proof data rather than by a
separate heuristic prover.

Current status:

- Python remains useful for diagnostics, corpus analysis, and offline
  summarization.
- Python is not considered part of any qualifying proof reconstruction path.
- New proof-producing Python logic should remain prohibited.

### Phase 2: Native S-Expression Certificate

The next path introduced a native Vampire certificate intended for Megalodon.
This was the first major course correction after the audits.

Vampire now emits structured S-expression data containing formulas, clauses,
step identities, source names, source roles, source-map information, inference
names, parent steps, substitutions, positions, and `kernel_v1` records.

This was a clear improvement over scraping ordinary TPTP proof text. Vampire
has access to substitutions, parents, selected literals, generated symbol
information, Skolem metadata, and inference details at the point where proof
search and preprocessing happen. The audits correctly pushed us to export
that information from Vampire instead of rediscovering it in Megalodon or
Python.

Current status:

- The native certificate format exists and is used by the current branch.
- It is still too broad and too ad hoc to be called a finished Ivy-style
  certificate calculus.
- `kernel_v1` metadata is an audit and migration vocabulary, not yet a clean
  standalone primitive proof object.

### Phase 3: Closed Source-Linked Textual Reconstruction

The branch then added stricter source validation and closed textual modes.
This fixed two early audit failures:

- Unsupported derived Vampire clauses were previously allowed to become
  theorem assumptions.
- Source names were previously too close to label links; a source name could
  be present even when the formula did not semantically match.

The later closed mode rejected bridge and derived assumptions. Source maps
were checked more semantically for the supported THF fragment. That made
closed exported-problem evidence more meaningful.

Current status:

- This work is valuable as a regression oracle and source-linking baseline.
- It is still not the final desired proof-term path because textual emitted
  scripts and generated proof helpers are not the clean trust boundary.

### Phase 4: Native `Syntax.pf` Proof-Term Seed

The project then moved proof reconstruction into Megalodon's native proof-term
machinery. This was the right direction.

The key point is that a qualifying importer should produce Megalodon proof
ASTs and use Megalodon's checker:

```ocaml
source_context -> certificate -> Syntax.tm * Syntax.pf
```

instead of only producing text:

```ocaml
certificate -> string
```

The branch now has native proof-term checking for a core and for selected
preprocessing/refutation paths. It uses `check_propofpf` and native proof
terms in important places.

A major trust-boundary correction happened after the July 16 audit. Earlier
native preprocessing checks sometimes created the desired implication as a new
`Known` theorem and then checked a proof by applying that newly installed
known proposition. That was equivalent to a hidden bridge axiom. The counted
native paths now reject certificate-derived `Known "vampire_*"` propositions.

Current status:

- There is a real native proof-term path.
- It is still incomplete.
- The native proof-term implementation is embedded in large modules rather
  than isolated as a small kernel elaborator.

### Phase 5: Source-Context Integration

The most recent work has focused on connecting the certificate back to the
original Megalodon context rather than merely checking the exported THF
problem.

The source-context layer now distinguishes:

- global facts that can be checked as `Known hash`;
- local hypotheses;
- definitions;
- generated facts;
- generated equalities from `set`;
- conjecture facts;
- unresolved or unsupported facts.

Recent work also synthesizes standalone local source hypotheses from the
source map. This matters because many exported problems contain local facts
that are not global library theorems but are nevertheless legitimate
assumptions at the original theorem site. They must become `Hyp`-based proof
bindings in the native reconstruction, not missing axioms and not global
`Known` assumptions.

Current status:

- Source-context auditing is much stronger.
- Global source facts can be connected to checked `Known hash` proofs in the
  loaded Megalodon development.
- Standalone local source facts are now represented in the source context.
- Generated set equalities are handled as reflexivity/conversion obligations
  in source-obligation auditing.
- Full final composition into original theorem goals still fails on larger
  examples.

### Phase 6: Skolemization Metadata and Partial Proof Replay

Skolemization has been one of the hardest parts of the project. Earlier
versions tried to infer too much from generated names and formula strings.
That was both brittle and too Megalodon-side.

The current Vampire fork emits much richer Skolem metadata:

- top-level Skolem quantifier metadata;
- macro-edge shape metadata;
- macro-edge binders;
- macro-edge free-variable metadata;
- macro-edge child metadata;
- branch contracts;
- introduced witness contracts;
- proof contract metadata;
- parent instantiation contracts.

Megalodon now consumes many of these records and uses them to avoid treating
generated Skolem symbols as ordinary original variables or globally scoped
proof terms. Several earlier binder and de Bruijn escape failures were fixed
by preserving contract variables and branch binders across the CPS-style
Skolem replay.

Current status:

- Skolemization is no longer opaque string rewriting.
- Several focused Skolem proof-term cases now get past earlier blockers.
- Dependent witness terms whose heads are introduced Skolem symbols are now
  accepted by the native Skolem CPS path. This removes the earlier restriction
  that introduced witnesses had to be nullary symbols.
- General dependent Skolemization and source-connected Skolem transformations
  remain incomplete.
- The next failures are now around replaying formula/CNF projections under
  dependent Skolem binders, transporting generated Skolem witnesses through
  source/preprocessing transformations, and composing the refutation back into
  the exact original theorem goal.

## Audit-Driven Course Corrections

The audits were useful and, in several places, correct. The current branch is
materially different because of them.

### July 12 Audit

The July 12 audit identified that the then-current implementation had become
three overlapping systems:

- a Vampire exporter;
- a large Python reconstructor;
- a Megalodon-side tactic/skeleton generator.

It also warned that reported successes were mostly admission-based or
source-context syntax checks rather than proof reconstruction. That criticism
was valid. It led to the quarantine of Python from qualifying runs and the
shift toward native certificate import.

### July 13 Audit

The July 13 audit accepted the conceptual Prover9/Ivy-style pivot but found
that the new path had already started recreating the old monolith. It also
found that unsupported derived Vampire clauses could become assumptions.

That defect has since been addressed in closed and counted modes. Derived
Vampire clauses cannot count as source assumptions in the qualifying path.

The audit also asked for OCaml inside Megalodon rather than Python for accepted
reconstruction. The current path follows that.

### July 14 Audit

The July 14 audit found the critical source-map loophole: source linking was
too label-based. A certificate could attach a nontrivial formula to a source
name whose exported declaration was `$true`.

This led to stronger formula checking and fail-closed source validation for
the supported THF fragment. The current source-obligation audit goes further
by checking supported source obligations against the loaded Megalodon context.

### July 15 Audit

The July 15 audit accepted that closed mode and semantic source checks had
become meaningful. It still warned that the implementation was growing in the
wrong place and that source-linked exported proof checking was not the same as
original-context reconstruction.

That warning remains true. It is one of the central messages of this report.

### July 16 Audit

The July 16 audit found a subtler trust-boundary failure: the native
preprocessing path could dynamically install certificate-derived `Known`
propositions and then use them as if they were library theorems.

The current branch has addressed this in counted paths. It includes a native
proof `Known` provenance scan and rejects generated certificate-derived proof
constants in counted native core/preprocess checks. Older preprocessing
frontier numbers that relied on dynamic `Known` insertion must remain
classified as structural/transitional evidence, not proof reconstruction.

The July 16 audit also recommended a real small-kernel primitive layer and
better original-context API. Those remain only partially implemented.

## Current Implementation State

### Megalodon Code

The main relevant files are:

- `src/vampire_cert_v1.ml`: native certificate parsing, checking,
  proof-term elaboration, source/preprocessing logic, and legacy textual
  machinery. It is currently about 32,800 lines. It is too large.
- `src/vampire_source_context.ml`: source-context resolution for global
  known facts, local hypotheses, definitions, generated facts, conjecture
  facts, and source issues. It is about 810 lines and closer to the shape the
  final design needs.
- `src/megalodon.ml`: command-line integration, live `vampireaby` path,
  original-context handling, source-context plumbing, and final proof
  composition. It is about 12,050 lines.
- `tests/vampire_certificate/run_native_cert_v1_smoke.sh`: the main focused
  smoke regression script, now about 4,500 lines.
- `tests/vampire_certificate/run_kernel_v1_metadata_audit.sh`: broad
  `kernel_v1` metadata consistency audit, about 1,660 lines.

The most recent Megalodon commits include:

- `433316d Merge reconstruction deltas through aliases`
- `8a6ebe1 Refresh July 19 expert report`
- `8a58aeb Try all CNF result binders for formula projection`
- `6d79df9 Accept dependent Skolem witness applications`
- `bff2db2 Prefer source locals for refutation instantiation`
- `f894e62 Refresh July 19 expert report`
- `9fa2663 Refresh July 19 expert report`
- `47b9d4b Refresh July 19 reconstruction report`
- `3466f58 Bind local source hypotheses in native certificates`
- `5f33fe0 Use source terms for refutation instantiation`
- `8e6e1cf Synthesize standalone local source hypotheses`
- `08ce383 Check preprocess final conjecture composition`
- `1fb6a47 Classify live Skolem Vampire exceptions`
- `6a1cef8 Preserve Skolem branch binders in CPS assumptions`
- `2cd712c Detect stale unnamed binder proof failures`
- `15ef545 Classify stale Skolem binder certificates`
- `721865b Bound Skolem proof frontier checks`
- `3222909 Combine Skolem CPS rejection scans`

The most recent committed source-context improvement is important. It
synthesizes source-context local hypotheses from source-map bindings whose
kind is `local_fact` and whose native source name is present. Those local
hypotheses are now also bound into native core and preprocess proof checking
using the certificate's native source propositions, instead of being omitted
from the core source-proof spine or shifted blindly under certificate-local
variables. This avoids the earlier proof-hypothesis out-of-bounds failure and
preserves the distinction between the exported certificate context and the
live Megalodon theorem context.

The same commit adds a narrow checked equality-symmetry transport fallback in
source-audit replay. When the source proof establishes `x = y` and the current
goal is definitionally the symmetric equality `y = x`, Megalodon constructs a
native equality-symmetry proof term and checks it against the current goal. The
smoke suite contains a focused regression for this case.

The latest committed source-linking change merges reconstruction deltas
through source aliases during final theorem-site reconstruction. It is a
general attempt to make the live Megalodon definition environment, source-map
names, TPTP names, and certificate-local names visible to the same
definitional-conversion checks. It did not close the hard `11925:125` target,
but it is a better basis for the next source-goal transport fix than the
previous single-delta table.

An earlier July 19 experiment added audited definitional facts into the final
reconstruction delta. It was useful for diagnosing the final source-goal
composition failure, but it should be read as historical context unless it is
present in the committed code being reviewed. The current live uncommitted
source patch is instead the narrower Skolem binder-shift experiment described
in the latest supplement below.

### Vampire Code

The main relevant files are:

- `Shell/MegalodonChecker/MegalodonChecker.cpp`: the current Megalodon
  certificate exporter. It is about 25,980 lines and remains too large.
- `Shell/MegalodonChecker/MegalodonKernelSyntax.cpp`: helper syntax and
  metadata routines for the emerging `kernel_v1` layer.
- `Shell/MegalodonChecker/MegalodonKernelSyntax.hpp`: declarations for that
  helper layer.
- `Shell/MegalodonChecker/CERTIFICATE_PLAN.md`: earlier plan text for the
  certificate direction.

The most recent Vampire commits include:

- `6f74b74bf Emit Skolem parent instantiation contracts`
- `dfcc40289 Emit Skolem contract child fields`
- `3ed96581f Emit Skolem edge introduced witness contracts`
- `605efa196 Emit Skolem macro edge branch contracts`
- `06d7ec37f Emit Skolem proof contract metadata`
- `18253e552 Emit Skolem macro edge child metadata`
- `a78e7edc0 Emit Skolem macro-edge free-variable metadata`
- `38f0c9f42 Emit top-level Skolem quantifier metadata`

This is the right repository for much of the proof detail. Vampire has the
needed information at proof-emission time. However, the implementation has not
yet been factored into a clean primitive builder analogous to Prover9's proof
transformation into Ivy objects.

## Current Source-Linking Status

Source linking is much better than it was, but it is not complete.

### Global Lemmas and Definitions

For hash-backed source facts, the source-context path loads the Megalodon
development and checks whether the referenced hash is available as a known
fact with the expected proposition. When that succeeds, the source fact can be
used as a real `Known hash` proof, not as an imported certificate assumption.

This is a major difference from the earlier source-map-only path. It means
some exported axioms are now connected to real original Megalodon library
lemmas.

Limitations:

- The mapping is still partial.
- Not all exported facts have a corresponding global claim.
- Definitions and generated facts need separate handling.
- The final theorem proof still needs to be composed back into the exact
  original proof goal, including local binders and local facts.

### Local Hypotheses

Local hypotheses are now represented in the source context and are connected
to native certificate checking. The current branch handles them in two steps.

First, source-map entries tagged as local facts are resolved to
`Vampire_source_context.LocalHyp` entries. This is necessary because local
facts may appear as TPTP axioms but are not global Megalodon theorems.

Second, native core and preprocess checking receives an external-hypothesis
list whose entries are indexed by the live local proof context but whose
propositions are computed in the same native certificate context used by the
preprocess elaborator. This is the important correction from today's work:
the earlier experiment tried either to omit local facts from native checking
or to shift all live local hypotheses blindly under certificate variables.
Both were wrong. The committed code instead asks
`Vampire_cert_v1.native_certificate_source_bindings_native_context` for the
certificate-side source proposition and places it at the local-hypothesis
index supplied by the source resolver.

Focused synthetic smoke fixtures demonstrate the intended pattern:

- a local fact `Hp : p` is exported;
- the certificate refutes `p` and `~p`;
- source-context strict mode consumes the local fact;
- native preprocess proof checking succeeds;
- remaining source assumptions are reduced to the conjecture.

The smoke suite also now checks local equality transport: from a local
hypothesis proving `x = y`, the native source-audit path can prove a current
goal `y = x` by constructing and checking a Megalodon equality-symmetry proof
term. This is not a broad theorem-proving tactic; it is a narrow checked
transport for a proof term whose proposition is already known.

This is a real step toward original-context reconstruction. It does not yet
solve all local-binder, source/preprocessing, or conjecture-composition cases.

### Generated `set` Facts

Some TPTP axioms come from Megalodon's `set` command and do not correspond to
ordinary user-written `claim`s. In the common equality case, they should be
proved by reflexivity or conversion, not assumed.

Current status:

- Source-obligation auditing recognizes equality-shaped generated facts.
- Reflexivity/conversion-style generated facts are counted separately from
  global knowns and local hypotheses.
- The latest broad source-obligation audit found formula obligations checked
  and no missing source obligations in the tested corpus.

This is still a source/preprocessing layer, not a complete final proof of all
set-generated obligations in every original theorem.

### Conjecture Binding

The refutation proves inconsistency of the source assumptions plus a negated
conjecture. To prove the original theorem, Megalodon must convert the
refutation into a proof of the current goal at the original proof site.

Current status:

- There is code for guided negated-conjecture reconstruction and final
  conjecture composition.
- The focused original-site target at `10654:21` succeeds.
- The larger target at `11925:125` fails precisely at this final
  source-goal transport/composition stage.

This is now one of the main blockers.

## Current Smolka-Style Transformation Status

The Smolka-style lesson, as interpreted for this project, is not "duplicate
all of Vampire's preprocessing in Megalodon." The useful principle is:

```text
Keep the ATP proof graph and source correspondence, but justify each local
source/preprocessing edge by a small, explicit target-side proof-producing
procedure.
```

The project has partial support for this but not a finished framework.

### What Works Partially

The current native paths can prove or check selected transformations and
clausal steps without dynamic `Known` insertion:

- selected resolution and clausal-core steps;
- selected instantiation/substitution cases;
- some single predicate folds;
- some rectification and variable-map cases;
- selected direct or metadata-backed Skolem patterns;
- some final-conjecture composition cases;
- generated equality/reflexivity source obligations in auditing.

### What Is Still Open

The general Smolka-style transformation layer is incomplete for:

- full FOOL transformations;
- ENNF;
- CNF projection from source formulas;
- broad definition introduction and predicate-definition folding;
- general formula normalization;
- full dependent Skolemization;
- AVATAR and SAT-layer explanation;
- final source-goal transport through intermediate arithmetic/equality
  consequences;
- proof compression and intelligible declarative script output after native
  reconstruction.

The current problem is not lack of one more formula recognizer. The missing
piece is a disciplined source/preprocessing certificate layer whose rules have
explicit semantics, required Vampire fields, and native Megalodon proof-term
elaboration.

## Current Skolemization Status

Skolemization has improved substantially since the early reports.

Earlier problems:

- generated Skolem names were treated too much like original symbols;
- dependencies were inferred from names or formulas;
- generated witnesses escaped their intended binder scopes;
- delayed sibling formulas in CPS-style replay lost fresh binders;
- de Bruijn-like scope issues produced invalid proof terms;
- final dependent Skolem transport often failed after a local fix.

Current improvements:

- Vampire emits explicit Skolem contract metadata.
- Generated Skolem dependencies and introduced witnesses are represented in
  the certificate.
- Megalodon preserves branch binders and contract variables in several
  formerly failing CPS paths.
- Megalodon avoids installing context-dependent epsilon witnesses as arity-zero
  global definitions.
- Megalodon now accepts introduced witness terms whose head is a Skolem symbol
  even when the witness is a dependent application, for example `sK1 X1` or
  `sK2 X0`.
- Some focused Skolem proof-term frontier tests now get past earlier blockers.

Remaining limitations:

- There is not yet a fully general Skolemization proof rule.
- Dependent Skolem functions remain hard, especially when later
  paramodulation/equality transport unfolds or compares them.
- Earlier representative failures reached `u273/u281`, where a dependent
  Skolem branch with witness `sK2 X0` was opened far enough for a CNF
  formula-clause projection to fail. The latest committed frontier has moved
  to `u276`, where the nested transport logic succeeds farther but exposes a
  source/result binder-shifting mismatch.
- Source-connected Skolemization, where the transformation is composed all the
  way from original formula to clause refutation and then back to the original
  theorem, remains incomplete.

This is one of the right places to continue work, but only with explicit
Vampire-owned metadata and proof-producing Megalodon rules. It should not be
handled by name guessing or broad Megalodon-side search.

## Recent Verification Results

The following are recent checks from the current branch. They use `/project/tmp`
for artifacts.

### Build

Megalodon built successfully:

```text
TMPDIR=/project/tmp ./makeopt
```

This was run after the latest source-context changes.

### Main Native Certificate Smoke

The focused native certificate smoke test passed:

```text
TMPDIR=/project/tmp tests/vampire_certificate/run_native_cert_v1_smoke.sh
```

The latest artifact symlink is:

```text
/project/tmp/latest_native_cert_v1 -> /project/tmp/native_cert_v1.ltUt1e
```

This smoke test includes many targeted positive and negative checks, including
source-context strict checks, closed-mode checks, native core/preprocess proof
checks, `kernel_v1` metadata checks, dynamic-`Known` rejection checks, generated
source obligations, standalone local-hypothesis fixtures, native local source
binding, and a local equality-symmetry transport fixture.

### Source-Obligation Audit

The latest broad source-obligation audit passed:

```text
TMPDIR=/project/tmp JOBS=10 STRICT=1 MIN_AUDIT_PASS=1 \
  tests/vampire_certificate/run_native_cert_v1_source_obligation_audit.sh
```

Reported result:

```text
AUDIT_PASS 96
cases 96
total 388
formula_checked 388
formula_unsupported 0
formula_missing 0
equality_checked 2
set_reflexivity_checked 0
true_checked 0
artifacts /project/tmp/native_cert_v1_source_obligations.b4LUYa
latest /project/tmp/latest_native_cert_v1_source_obligations
```

This is strong source-linking evidence for the audited corpus. It is not, by
itself, proof reconstruction of the original theorems.

### Cached Strict Preprocess Checks

Cached strict exported preprocess proof-term checks passed for selected cases,
including:

- `hammer.10654.21`
- `hammer.11925.125`

These checks show that the certificate/preprocess refutation can be checked in
the exported/cached context. They do not guarantee original-site goal closure.

### Focused Original-Site Success

The target:

```text
examples/hammer/100thms_12_h.mg:10654:21
```

passed a focused live native Vampire reconstruction with Vampire timeout 10:

```text
timeout 90s env TMPDIR=/project/tmp bin/megalodon -allowincompleteqed \
  -vampireaby /project/vampire-leancheck/vampire_rel_vampire/megalodon5_11094 \
  -vampireabyoutdir /project/tmp/vampireaby_live_target_10654 \
  -vampireabytimeout 10 -vampireabyschedule casc \
  -vampireabyproof megalodon \
  -vampireabynativestrict -vampireabytarget 10654 21 \
  -vampireabytargetstop \
  examples/hammer/100thms_12_h.mg
```

Key output:

```text
Vampire target stop after reconstructed aby proof term at line 10654 char 21.
```

This is the strongest current focused original-site evidence. It is not a
qualifying final theorem-suite result because the run uses the targeted hammer
mode and `-allowincompleteqed` for the surrounding file.

### Representative Original-Site Failure

The target:

```text
examples/hammer/100thms_12_h.mg:11925:125
```

still fails:

```text
Failure at line 11925 char 125:
Vampire native certificate did not reconstruct current proof goal at line
11925 char 125
```

Important diagnostic facts from this failure:

- The source map includes local facts such as `Lek` and `Hk`.
- The source map includes known lemmas such as `nat_p_omega`, `omega_SNo`,
  `SNo_eps_SNoS_omega`, `add_SNo_SNoS_omega`, `add_SNo_1_ordsucc`,
  `mul_SNo_oneR`, and `mul_SNo_distrL`.
- The certificate/preprocess path now gets past the earlier
  nullary-witness-only Skolem restriction. The remaining immediate native
  blocker is inside dependent Skolem/CNF replay.
- The committed local-hypothesis source binding makes the exported local facts
  visible to native core/preprocess checking, so the immediate blocker is no
  longer a missing local proof-hypothesis index.
- The source-local candidate restriction removes the bad theorem-local
  candidate class that included terms such as `eps_ In`, `eps_ #In`,
  `eps_ omega`, and `eps_ #Empty`.
- The newest committed focused run fails at `u276`: the previous `u273`
  nested equality-orientation failure has moved, but the source existential
  branch and result formula are compared under mismatched binder depth. The
  current uncommitted one-line experiment shifts the result formula under the
  source-only existential binder to test that diagnosis.
- Final source-goal reconstruction remains unsolved after that native
  preprocessing failure. The live goal is still the quantified induction-like
  proposition over `nat_p`.

This failure is useful because it separates two issues:

- native certificate/preprocess replay can succeed;
- final source-goal transport back to the original theorem can still fail.

An attempted broad generic source-premise closure search was rejected because
it made the target too slow and hit the outer timeout. That experiment was not
committed. The next fix should be narrower and guided by certificate/source
bindings, not a general Megalodon-side theorem search.

### Late July 19 Refresh: Source-Term Instantiation

After the earlier version of this report, commit `5f33fe0` added a narrower
source-term instantiation mechanism for final refutation-to-goal composition.
The change collects candidate terms from the current source map, the current
goal, and available source propositions, then uses those terms when trying to
instantiate non-propositional quantifiers during final refutation recovery.

The relevant Megalodon-side change is intentionally limited:

- it scans source/current-goal terms for closed candidate subterms;
- it avoids lifting subterms that depend on local binders outside their scope;
- it caps the candidate set to keep focused iterations fast;
- it wires the candidate terms into
  `vampire_instantiated_refutation_candidates`;
- it does not add a library-specific rule for any particular theorem name.

Verification for this committed change:

```text
TMPDIR=/project/tmp ./makeopt

timeout 90s env TMPDIR=/project/tmp bin/megalodon -allowincompleteqed \
  -vampireaby /project/vampire-leancheck/vampire_rel_vampire/megalodon5_11094 \
  -vampireabyoutdir /project/tmp/vampireaby_live_target_10654_candidate_terms_final \
  -vampireabytimeout 10 -vampireabyschedule casc \
  -vampireabyproof megalodon -vampireabynativestrict \
  -vampireabytarget 10654 21 -vampireabytargetstop \
  examples/hammer/100thms_12_h.mg

TMPDIR=/project/tmp tests/vampire_certificate/run_native_cert_v1_smoke.sh
```

The build passed, the focused `10654:21` target passed, and the main native
certificate smoke passed. The harder `11925:125` target still failed, so this
commit is progress on the source-goal composition frontier but not a general
solution.

### Late July 19 Refresh: Native Local-Hypothesis Binding

Commit `3466f58` turns the local-hypothesis experiment into committed
implementation. The motivating failure was `11925:125`, whose exported THF
contains local facts:

```text
% megalodon_source_map (local_type "k_tp" "k" "")
thf(k_tp,type,(k : $i)).
% megalodon_source_map (local_fact "c_Lek" "Lek" "")
thf(c_Lek,axiom,(c_SNo @ (eps_5F @ k))).
% megalodon_source_map (local_fact "c_Hk" "Hk" "")
thf(c_Hk,axiom,((c_In @ k) @ omega)).
```

The investigation found a real context-indexing issue:

- if local source proofs are fed into native preprocess/core without live
  external hypotheses, proof checking fails with a proof-hypothesis index
  out-of-bounds error;
- if the live Megalodon proof context is passed directly as external
  hypotheses, the local hypothesis is in scope but its proposition is indexed
  differently from the exported certificate proposition;
- blindly shifting those external hypotheses by the full certificate variable
  context overshoots the target index and is known wrong.

The committed correction is to build the external-hypothesis list from
`Vampire_source_context.LocalHyp` entries by local proof index, but to use the
certificate-side source proposition computed in the native certificate
context. This gives native preprocess/core checking the proposition it expects
without pretending that the live local context can be blindly inserted under
all certificate variables.

The same commit leaves the final proof obligation checked against the live
Megalodon goal. It does not turn local facts into global facts and it does not
introduce a certificate-derived `Known` theorem.

Verification for this committed change:

```text
TMPDIR=/project/tmp ./makeopt

TMPDIR=/project/tmp tests/vampire_certificate/run_native_cert_v1_smoke.sh

timeout 90s env TMPDIR=/project/tmp bin/megalodon -allowincompleteqed \
  -vampireaby /project/vampire-leancheck/vampire_rel_vampire/megalodon5_11094 \
  -vampireabyoutdir /project/tmp/vampireaby_live_target_10654_report_20260719 \
  -vampireabytimeout 10 -vampireabyschedule casc \
  -vampireabyproof megalodon -vampireabynativestrict \
  -vampireabytarget 10654 21 -vampireabytargetstop \
  examples/hammer/100thms_12_h.mg

timeout 120s env TMPDIR=/project/tmp MEGALODON_CERT_DEBUG=1 bin/megalodon \
  -allowincompleteqed \
  -vampireaby /project/vampire-leancheck/vampire_rel_vampire/megalodon5_11094 \
  -vampireabyoutdir /project/tmp/vampireaby_live_target_11925_report_20260719 \
  -vampireabytimeout 10 -vampireabyschedule casc \
  -vampireabyproof megalodon -vampireabynativestrict \
  -vampireabytarget 11925 125 -vampireabytargetstop \
  examples/hammer/100thms_12_h.mg
```

Results:

- build passed;
- native certificate v1 smoke passed;
- `10654:21` passed with `Vampire target stop after reconstructed aby proof
  term at line 10654 char 21`;
- `11925:125` still failed, but after the local source facts were mapped into
  the native context. The remaining log shows wrong-proposition candidates and
  non-applicable equality transport against a quantified induction-like goal,
  not the earlier local-hypothesis index failure.

This changes the next blocker. It is now a source/preprocessing and final-goal
alignment problem around the shape:

```text
forall n:set,
  nat_p n ->
  In (mul_SNo (eps_ k) n) (SNoS_ omega) ->
  In (mul_SNo (eps_ k) (ordsucc n)) (SNoS_ omega)
```

rather than a failure to make local assumptions visible at all.

### Final July 19 Refresh: Source-Local Candidate Restriction

After `3466f58`, the focused `11925:125` investigation found a second
source-linking problem. The refutation-instantiation path was still mining
closed non-propositional subterms from source propositions and using them as
candidate instantiations for theorem-local variables. On the representative
failure this produced nonsensical candidate targets such as applying `eps_` to
global symbols or library constants rather than to the live theorem-local
variable `k`.

Commit `bff2db2` removes that broad fallback from
`vampire_instantiated_refutation_candidates` for non-propositional terms. With
a source map available, the candidate stream is now:

- explicit `local_type` source-map variables whose names and types match the
  live Megalodon local context;
- ordinary live context variables of the requested type;
- no mined closed source subterms for this refutation-instantiation path.

This is intentionally not a theorem-specific fix. It uses only the source-map
relation that Megalodon already emits, for example:

```text
% megalodon_source_map (local_type "k_tp" "k" "")
```

The source-audit goal-reconstruction path still has a controlled fallback to
source proposition terms when no live local term exists, because that path
also handles global source obligations. The stricter restriction is applied
where the broad fallback was actively creating bad theorem-local
instantiations.

Verification for this committed change:

```text
TMPDIR=/project/tmp ./makeopt

timeout 90s env TMPDIR=/project/tmp bin/megalodon -allowincompleteqed \
  -vampireaby /project/vampire-leancheck/vampire_rel_vampire/megalodon5_11094 \
  -vampireabyoutdir /project/tmp/vampireaby_live_target_10654_local_type_only_20260719 \
  -vampireabytimeout 10 -vampireabyschedule casc \
  -vampireabyproof megalodon -vampireabynativestrict \
  -vampireabytarget 10654 21 -vampireabytargetstop \
  examples/hammer/100thms_12_h.mg

timeout 120s env TMPDIR=/project/tmp MEGALODON_CERT_DEBUG_GUIDED=1 \
  MEGALODON_CERT_DEBUG=1 bin/megalodon -allowincompleteqed \
  -vampireaby /project/vampire-leancheck/vampire_rel_vampire/megalodon5_11094 \
  -vampireabyoutdir /project/tmp/vampireaby_live_target_11925_local_type_only_20260719 \
  -vampireabytimeout 10 -vampireabyschedule casc \
  -vampireabyproof megalodon -vampireabynativestrict \
  -vampireabytarget 11925 125 -vampireabytargetstop \
  examples/hammer/100thms_12_h.mg
```

Results:

- build passed;
- the focused `10654:21` original-site regression still passed;
- the malformed `eps_ In`, `eps_ #In`, `eps_ omega`, and `eps_ #Empty`
  candidate class disappeared from the `11925:125` diagnostic log;
- `11925:125` still failed, now with a repeated native proof-term application
  mismatch where the expected term contains Vampire's Skolem witness
  `#sK1 _4` but the actual proof term contains Megalodon's classical-choice
  witness `#Eps_i (...)`;
- after those Skolem/choice mismatches, source-context direct goal
  reconstruction still reports that it did not find a proof of the current
  induction-step goal.

This latest result is useful because it separates two concerns. Source-local
variable selection is no longer obviously wrong on the target. The next real
blocker is Skolemization/choice-witness transport and final theorem-site
composition. That is exactly the area where the audits recommended moving more
detail into Vampire-side certificates and proving a small number of explicit
Smolka-style transformation rules in Megalodon.

### Final July 19 Refresh: Dependent Skolem Witness Applications

Commit `6d79df9` advances the same failing target by accepting dependent
introduced witness terms in the native Skolem CPS proof path. Previously the
Skolem replay accepted only witnesses that were nullary `TmH` symbols. That
was too restrictive: real Vampire Skolemization often introduces witnesses
whose head is a Skolem symbol applied to dependency variables.

The change flattens a witness application, checks that its head is an
introduced Skolem symbol, and preserves the full witness term for the branch
contract. This is general; it does not inspect theorem or library names. It
accepts patterns such as:

```text
sK1 X1
sK2 X0
```

Verification for this committed change:

```text
TMPDIR=/project/tmp ./makeopt

TMPDIR=/project/tmp tests/vampire_certificate/run_native_cert_v1_smoke.sh

timeout 90s env TMPDIR=/project/tmp bin/megalodon -allowincompleteqed \
  -vampireaby /project/vampire-leancheck/vampire_rel_vampire/megalodon5_11094 \
  -vampireabyoutdir /project/tmp/vampireaby_live_target_10654_dep_witness_20260719 \
  -vampireabytimeout 10 -vampireabyschedule casc \
  -vampireabyproof megalodon -vampireabynativestrict \
  -vampireabytarget 10654 21 -vampireabytargetstop \
  examples/hammer/100thms_12_h.mg

timeout 90s env TMPDIR=/project/tmp MEGALODON_CERT_DEBUG=1 \
  MEGALODON_CERT_DEBUG_GUIDED=1 \
  MEGALODON_CERT_FAIL_FAST_SKOLEM_CPS=1 \
  bin/megalodon -allowincompleteqed \
  -vampireaby /project/vampire-leancheck/vampire_rel_vampire/megalodon5_11094 \
  -vampireabyoutdir /project/tmp/vampireaby_live_target_11925_dep_witness_20260719 \
  -vampireabytimeout 10 -vampireabyschedule casc \
  -vampireabyproof megalodon -vampireabynativestrict \
  -vampireabytarget 11925 125 -vampireabytargetstop \
  examples/hammer/100thms_12_h.mg
```

Results:

- build passed;
- the main native certificate smoke passed;
- the focused `10654:21` original-site regression still passed;
- the old `u271: native preprocess Skolem CPS currently supports only
  nullary introduced witnesses` blocker disappeared;
- `11925:125` still failed, but the new failure is later and more precise:
  `u273` rebuilds the result proof with one result free variable instead of
  two step variables, then shadow replay stops at `u281` with
  `cnf_formula_clause cannot instantiate formula universal binder to project
  target clause`.

The new frontier is therefore not witness-shape recognition. It is proof
replay under dependent Skolem binders, specifically CNF formula-clause
projection from a Skolemized formula with target variables supplied by the
certificate.

### Final July 19 Refresh: CNF Result-Binder Projection

Commit `8a58aeb` removes the next local replay blocker in the same target.
The failing Skolem branch reached a CNF formula-clause projection step under
nested universal binders. The certificate result contained more than one
result binder of type `set`, but the importer only tried the pending parent
variable candidates and then one fallback result variable of the right type.
On the representative step this meant that the outer result binder was tried
where the inner result binder was needed, producing:

```text
u281: native preprocess proof-term cnf_formula_clause cannot instantiate
formula universal binder to project target clause
```

The fix is deliberately general. It does not special-case theorem names,
library symbols, `nat_p`, or the target line. The CNF formula-clause proof
builder now gathers all result binders whose type matches the formula
universal binder, combines them with the pending parent-variable candidates,
deduplicates candidates by term identity, and tries them in order. This makes
the binder choice come from the typed certificate/result shape rather than
from a name heuristic.

Verification for this committed change:

```text
cd /project/Megalodon
TMPDIR=/project/tmp ./makeopt

TMPDIR=/project/tmp tests/vampire_certificate/run_native_cert_v1_smoke.sh

timeout 90s env TMPDIR=/project/tmp bin/megalodon -allowincompleteqed \
  -vampireaby /project/vampire-leancheck/vampire_rel_vampire/megalodon5_11094 \
  -vampireabyoutdir /project/tmp/vampireaby_live_target_10654_cnf_candidates_final_20260719 \
  -vampireabytimeout 10 -vampireabyschedule casc \
  -vampireabyproof megalodon -vampireabynativestrict \
  -vampireabytarget 10654 21 -vampireabytargetstop \
  examples/hammer/100thms_12_h.mg

timeout 90s env TMPDIR=/project/tmp MEGALODON_CERT_DEBUG=1 \
  MEGALODON_CERT_DEBUG_GUIDED=1 \
  MEGALODON_CERT_FAIL_FAST_SKOLEM_CPS=1 \
  bin/megalodon -allowincompleteqed \
  -vampireaby /project/vampire-leancheck/vampire_rel_vampire/megalodon5_11094 \
  -vampireabyoutdir /project/tmp/vampireaby_live_target_11925_cnf_candidates_final_20260719 \
  -vampireabytimeout 10 -vampireabyschedule casc \
  -vampireabyproof megalodon -vampireabynativestrict \
  -vampireabytarget 11925 125 -vampireabytargetstop \
  examples/hammer/100thms_12_h.mg
```

Results:

- build passed;
- the native certificate smoke test passed;
- the focused `10654:21` original-site regression still passed;
- the old `u281` CNF formula-clause binder-instantiation failure disappeared;
- the focused `11925:125` target moved forward to:

```text
u273: native preprocess Skolem shadow replay reached empty clause
u273: native preprocess Skolem CPS using shadow replay continuation
Failure at line 11925 char 125:
Vampire native certificate did not reconstruct current proof goal at line
11925 char 125
```

The new diagnostic is important. It shows that the Skolem/CNF branch can now
reach an empty-clause shadow replay, but the final reconstructed proposition
does not yet compose with the exact original theorem-site goal. The remaining
exported negated-conjecture proposition contains an unfolded version of the
source predicate `nat_p`:

```text
forall n:set,
  (forall P:set -> prop,
    P Empty ->
    (forall x:set, P x -> P (ordsucc x)) ->
    P n) ->
  In (mul_SNo (eps_ k) n) (SNoS_ omega) ->
  In (mul_SNo (eps_ k) (ordsucc n)) (SNoS_ omega)
```

The live Megalodon goal at the theorem site is phrased with the original
predicate:

```text
forall n:set,
  nat_p n ->
  In (mul_SNo (eps_ k) n) (SNoS_ omega) ->
  In (mul_SNo (eps_ k) (ordsucc n)) (SNoS_ omega)
```

This is exactly the source-linking/Smolka-style transformation issue the
audits have emphasized. The next fix should not be a hardwired `nat_p`
rewrite. It should be a general checked transport from the
definition-expanded exported conjecture back to the original Megalodon goal,
using the original source context, definition information, and Megalodon's own
logical constants and equality.

### Late July 19 Refresh: Delta Aliases and Audited Definitions

Commit `433316d` adds a general reconstruction-delta merge at the final
original-context boundary. The motivating failure was not that Vampire lacked
the proof of the exported refutation; it was that the live Megalodon theorem
site and the exported certificate often name the same definitional content
through different keys. One side may have a source hash, another side a TPTP
source name, another side a Megalodon source name, and another side a folded
definition head.

The committed change introduces `vampire_merge_reconstruction_delta` and uses
it in the final reconstruction helpers:

- `vampire_reconstruct_goal_from_proved_prop`;
- `vampire_reconstruct_goal_from_supplied_refutation`;
- `vampire_guided_negated_conjecture_reconstruction`.

The merge policy is intentionally conservative. If a key is absent, the extra
delta entry is added. If a key is present and is not a 64-character hash-like
identifier, the extra entry may replace it. Hash-keyed entries are preserved.
This matches the source-name alias policy used by the core checker and avoids
silently overwriting actual hashed Megalodon definitions.

Verification for the committed change:

```text
cd /project/Megalodon
TMPDIR=/project/tmp ./makeopt

TMPDIR=/project/tmp tests/vampire_certificate/run_native_cert_v1_smoke.sh

timeout 90s env TMPDIR=/project/tmp bin/megalodon -allowincompleteqed \
  -vampireaby /project/vampire-leancheck/vampire_rel_vampire/megalodon5_11094 \
  -vampireabyoutdir /project/tmp/vampireaby_live_target_10654_delta_merge_20260719 \
  -vampireabytimeout 10 -vampireabyschedule casc \
  -vampireabyproof megalodon -vampireabynativestrict \
  -vampireabytarget 10654 21 -vampireabytargetstop \
  examples/hammer/100thms_12_h.mg
```

Results:

- build passed;
- the native certificate smoke test passed;
- the focused `10654:21` original-site regression still passed;
- `11925:125` still failed.

The useful new diagnostic came from running `11925:125` with
`MEGALODON_CERT_FAIL_FAST_SKOLEM_CPS=1`:

```text
u276: native preprocess Skolem CPS cannot open non-prop parent step variable
```

The relevant certificate step is a Skolemization step whose parent formula has
a retained universal dependency `X0 : set` and an existential
`X1 : set -> prop`, with the introduced witness term `sK3 X0`. The current
Skolem CPS proof path still contains legacy logic that opens parent step
variables by applying dummy arguments at the top level. That works only for
propositional variables, where the dummy can be `false`. It is wrong for a
retained object variable such as `X0 : set`.

The correct fix is not to invent a dummy set term and not to special-case the
library theorem. The parent proof should be rebuilt under the matching
universal binder, applying the stored parent proof to the bound variable in
the proper scope, and then continuing the Skolem transformation. This is a
general dependent-binder proof replay problem.

After the committed delta-merge change, I tested one additional working-tree
experiment that has not been committed at the time of this report. The helper
`vampire_add_audited_definition_delta` reads source-context audit entries
classified as `Definitional`, extracts orientable equality sides, and copies
the definition body into the reconstruction delta under all available names:
source name, TPTP name, source hash, and the definitional head name. The final
goal-reconstruction calls then use this enriched reconstruction delta.

Verification for the uncommitted experiment:

```text
cd /project/Megalodon
TMPDIR=/project/tmp ./makeopt

TMPDIR=/project/tmp tests/vampire_certificate/run_native_cert_v1_smoke.sh \
  > /project/tmp/native_cert_v1_smoke_audited_defs_20260719.log

timeout 90s env TMPDIR=/project/tmp bin/megalodon -allowincompleteqed \
  -vampireaby /project/vampire-leancheck/vampire_rel_vampire/megalodon5_11094 \
  -vampireabyoutdir /project/tmp/vampireaby_live_target_10654_audited_defs_20260719 \
  -vampireabytimeout 10 -vampireabyschedule casc \
  -vampireabyproof megalodon -vampireabynativestrict \
  -vampireabytarget 10654 21 -vampireabytargetstop \
  examples/hammer/100thms_12_h.mg

timeout 90s env TMPDIR=/project/tmp MEGALODON_CERT_DEBUG=1 \
  MEGALODON_CERT_DEBUG_GUIDED=1 \
  bin/megalodon -allowincompleteqed \
  -vampireaby /project/vampire-leancheck/vampire_rel_vampire/megalodon5_11094 \
  -vampireabyoutdir /project/tmp/vampireaby_live_target_11925_audited_defs_20260719 \
  -vampireabytimeout 10 -vampireabyschedule casc \
  -vampireabyproof megalodon -vampireabynativestrict \
  -vampireabytarget 11925 125 -vampireabytargetstop \
  examples/hammer/100thms_12_h.mg
```

Results:

- build passed;
- the native certificate smoke test passed;
- the focused `10654:21` original-site regression still passed;
- `11925:125` still failed with
  `Vampire native certificate did not reconstruct current proof goal`;
- the initial source-target conversion diagnostics improved, which suggests
  that audited definitions are useful for the final source-goal transport;
- later candidate exploration still tried bad instantiations in which a live
  set variable was effectively replaced by proposition-shaped terms such as
  `False` or `forall _:prop, _0`.

This experiment is therefore promising but not finished. It should probably
be retained only if the next iteration tightens final theorem-site
instantiation so non-propositional locals receive only terms of the exact
expected type and scope. It also reinforces the main architectural conclusion:
the final bridge from exported refutation to live theorem goal needs a
checked, source-context-aware transport rule, not broad Megalodon-side
candidate search.

## Current Test Discipline

The current test discipline is better than the early iterative workflow.

Principles:

- use `/project/tmp` for generated artifacts;
- do not repeatedly rerun slow Vampire searches when cached certificates are
  enough;
- use Vampire timeout at most 10 seconds per problem;
- export batches where possible;
- run Vampire and certificate checks in parallel, usually with 10 workers;
- classify evidence explicitly as E1, E2, E3, or E4;
- run broad structural tests less often than focused proof-term tests;
- do not count `aby`, `admit`, incomplete QED, bridge assumptions, or
  certificate-derived `Known` propositions;
- assume classical logic and use the available `xm`;
- use Megalodon's native logical constants and equality rather than private
  replacements.

Problems remaining in the test setup:

- There is still no committed broad E1 harness.
- Some important live target commands still use `-allowincompleteqed` because
  they run inside larger files with unrelated incomplete proofs.
- The huge number of untracked generated hammer files in the worktree makes
  status noisy.
- The old broad integration numbers are not comparable with the newer counted
  proof-term numbers.

## What Is General and What Is Not

The user has repeatedly asked whether the implementation is becoming
hardwired to the current library. That is a valid concern.

General work:

- source-map parsing and semantic formula checking;
- resolving global facts by hash;
- representing local facts as local hypotheses;
- generated equality/reflexivity handling;
- native proof-term checking with `Syntax.pf`;
- rejecting certificate-derived `Known`;
- Vampire-side emission of substitutions, pivots, positions, parent formulas,
  result clauses, Skolem dependencies, and branch contracts;
- `kernel_v1` metadata audits.

Risky or not-yet-general work:

- any proof search in Megalodon that tries to rediscover source consequences
  without explicit certificate guidance;
- broad source-premise closure over all source lemmas;
- ad hoc reconstruction of arithmetic/set-theoretic intermediate facts;
- specialized textual disjunction reshaping helpers;
- one-off fixes for individual hammer targets;
- relying on theorem names such as `omega_SNo` or `mul_SNo_distrL` in the
  reconstruction logic.

The current committed code does not intentionally hardwire the large failing
target's theorem names. The failed broad closure experiment was deliberately
not committed because it moved in the wrong direction: it was too slow and too
close to a Megalodon-side prover.

The next source-goal fix should be general but narrow:

- use the exact remaining source bindings and expected goal shape;
- instantiate source facts only with terms already present in the goal,
  certificate, or source context;
- avoid unrestricted recursive closure;
- produce checked proof terms for each derived intermediate fact;
- fail quickly when no guided reconstruction exists.

## Prover9/Ivy Path Status

The audits repeatedly recommended a Prover9/Ivy-style direction. The branch is
partially on that path, but not enough.

What has moved toward that path:

- Vampire emits richer proof data instead of expecting Megalodon to guess.
- `kernel_v1` records identify rule kinds, parents, substitutions, selected
  literals, result clauses, rewrite fields, and Skolem transformation data.
- Megalodon has native proof-term checks for a small subset.
- Broad metadata audits enforce required fields for many rule families.

What is still missing:

- A small first-class Vampire-side `MegalodonKernelStep` list that macro
  inferences lower into.
- A stable primitive calculus with a small set of constructors.
- A separate Megalodon module that checks only that primitive calculus and
  elaborates it to `Syntax.pf`.
- A proof-transformation pass in Vampire analogous to Prover9's lowering of
  hyper-resolution, demodulation, and other macros into Ivy-checkable
  resolution/paramodulation steps.
- A clean separation between clausal kernel proof objects and source/
  preprocessing transformations.

The current `kernel_v1` metadata is useful but still too attached to the
large existing exporter. It is not yet the final small certificate language.

## Recommended Architecture From Here

The architecture should be:

```text
Original Megalodon context
  -> THF/TPTP export with lossless source map
  -> Vampire search, timeout <= 10 seconds
  -> Vampire proof normalization into small certificate objects
  -> Megalodon source/preprocessing proof layer
  -> Megalodon clausal kernel proof layer
  -> final composition into the original theorem goal
  -> Megalodon kernel check of the final Syntax.pf
```

The clausal kernel should remain small. A reasonable first set is:

- input from an already proved source/preprocessing clause;
- explicit instantiation/substitution;
- alpha-renaming;
- binary resolution;
- factoring;
- equality resolution;
- equality factoring;
- paramodulation/superposition with exact position and orientation;
- reflexive simplification;
- contradiction/empty-clause close.

Macro rules should be lowered in Vampire:

- unit-resulting resolution -> binary resolution chain;
- hyper-resolution -> binary resolution chain;
- demodulation -> paramodulation/simplification proof;
- subsumption resolution -> primitive resolution/factoring chain;
- AVATAR -> separate propositional certificate layer, not mixed into the
  first clausal MVP;
- Skolemization -> separate source/preprocessing transformation certificate.

The source/preprocessing layer should have separate rules for:

- source input from `Known hash`;
- local hypothesis input from `Hyp`;
- generated equality/reflexivity input;
- definitions/conversions;
- conjecture negation and final contradiction-to-goal transport;
- rectification;
- FOOL;
- ENNF;
- CNF projection;
- Skolemization.

Each rule needs:

- exact certificate syntax;
- semantic meaning;
- required Vampire fields;
- Megalodon proof-term elaboration;
- positive tests;
- negative tests;
- failure mode when unsupported.

## Immediate Technical Priorities

### 1. Make Original-Context Closure the Main Milestone

Do not optimize for another broad structural pass count. The next real
milestone should be:

```text
10 original-context Megalodon theorem sites reconstructed by the native
Vampire certificate path, with no aby, no admit, no incomplete QED in the
counted theorem, no bridge assumptions, no certificate-derived Known, and
Vampire timeout <= 10 seconds.
```

The target should then increase to 100 only after the 10 are committed and
reproducible.

### 2. Finish the Source-Context API

The source context should become an explicit typed object:

```ocaml
type source_proof =
  | GlobalKnown of string * Syntax.tm
  | LocalHyp of int * string * Syntax.tm
  | Definition of string * Syntax.tm * Syntax.pf
  | Generated of string * Syntax.tm * Syntax.pf
  | Conjecture of string * Syntax.tm
```

The current `vampire_source_context.ml` is close to this direction. The
remaining work is to make final theorem composition consume this object
uniformly.

### 3. Solve Final Goal Transport Narrowly

The `11925:125` failure should be used as a representative blocker, but not as
a reason to hardwire its lemmas.

The desired fix is a guided proof-producing source transport:

- collect candidate terms from the current goal, certificate result, and
  source bindings;
- instantiate only source facts whose conclusion can match the needed premise
  or goal;
- recursively solve premises only through already available source proofs or
  very small checked instantiation/application chains;
- cap the search aggressively;
- emit `Syntax.pf` at each step;
- fail quickly when the certificate does not provide enough guidance.

The rejected broad closure experiment showed that unrestricted source-premise
search is too slow and architecturally wrong.

### 4. Continue Skolemization, But Only With Explicit Contracts

Skolemization should remain a major focus. The correct path is:

- Vampire emits dependencies, sorts, introduced symbols, parent/child formulas,
  branch contracts, and witness contracts.
- Megalodon checks a local transformation theorem using classical choice and
  `xm`.
- Generated Skolem symbols remain generated certificate symbols, not original
  variables or arbitrary globals.
- Dependent Skolem definitions unfold only under the correct context.

Avoid name-based Skolem dependency recovery.

### 5. Extract the Small Kernel

The next architectural cleanup should extract the genuine native proof-term
seed into smaller modules:

- `vampire_kernel_syntax.ml`
- `vampire_kernel_check.ml`
- `vampire_kernel_elab.ml`
- `vampire_source_context.ml`
- `vampire_preprocess_elab.ml`

The existing `vampire_cert_v1.ml` can remain as a legacy parser/oracle during
the migration, but new counted proof-term logic should stop accumulating
there.

On the Vampire side, a corresponding `MegalodonKernelStep` builder should be
introduced and used by macro expansions before printing. The current
`MegalodonKernelSyntax.*` files are a start, but the main exporter still owns
too much of the logic.

## Current Blockers

### Blocker 1: No Broad E1 Suite

There is still no 100-theorem original-context suite. There is not yet even a
committed 10-theorem original-context suite under the strict final criteria.

### Blocker 2: Final Source-Goal Composition

The branch can check selected exported/preprocess refutations but still fail
to turn them into a proof of the exact original goal. The `11925:125` failure
is the current representative. Its current form is not primarily a clausal
replay failure: in non-fail-fast runs the replay reaches an empty-clause
shadow path, but the remaining exported negated conjecture is still not
transported to the exact original Megalodon goal. In fail-fast Skolem
diagnostics, the same target also exposes a dependent-binder replay issue at
`u276`, currently narrowed to source/result binder alignment in the Skolem CPS
base comparison. The immediate engineering task is therefore checked
definition/source transport plus principled Skolem binder replay, not another
ad hoc clausal inference heuristic.

### Blocker 3: Incomplete Smolka-Style Transformations

FOOL, ENNF, CNF, broad definitions, generated facts, conjecture negation, and
Skolemization need explicit proof-producing rules. They are not solved by
structural metadata alone. The latest `11925:125` state is a concrete example:
Vampire has enough information to refute the exported problem, but Megalodon
still needs a checked Smolka-style route from the exported, transformed
problem back to the exact source theorem statement.

### Blocker 4: Monolith Growth

The importer and exporter remain too large:

- `src/vampire_cert_v1.ml`: about 32,800 lines.
- `Shell/MegalodonChecker/MegalodonChecker.cpp`: about 25,980 lines.

This makes expert review and trust-boundary reasoning harder. The next phase
should reduce or isolate the proof-critical core, not add thousands more lines
to those files.

### Blocker 5: Primitive Proof Object Not Yet Stable

`kernel_v1` records provide useful data, but they are not yet a finalized
Ivy-like proof object. The project needs a small specification and a
Vampire-side lowering pass.

## What the Expert Should Review

The most useful review questions are:

1. Is the current source-context representation adequate for original
   Megalodon theorem-site reconstruction, especially local hypotheses,
   generated `set` equalities, definitions, and conjecture negation?

2. Is the current `kernel_v1` metadata close enough to become the primitive
   proof object, or should it be replaced by a smaller first-class
   `MegalodonKernelStep` list?

3. Which Vampire macro inferences should be lowered first to a small
   Prover9/Ivy-style calculus?

4. What is the minimal sound Skolemization certificate for Megalodon, using
   classical choice and explicit dependencies?

5. Should final source-goal transport be treated as part of the source/
   preprocessing layer, or as a separate theorem-composition layer?

6. Where should the line be drawn between Megalodon-side deterministic
   elaboration and forbidden Megalodon-side proof search?

7. What should count as the first publishable milestone: ten original-context
   proofs, one specific hard proof, or a restricted already-clausal suite?

## Recommended Next Milestones

### Milestone A: Ten Original-Context Proofs

Produce ten original theorem-site reconstructions in
`examples/hammer/100thms_12_h.mg` or a similarly representative development.

Criteria:

- Vampire timeout <= 10 seconds.
- THF input.
- Native certificate path.
- `Syntax.pf` proof terms.
- No `aby`.
- No `admit`.
- No hidden bridge or certificate-derived `Known`.
- Original local and global context consumed through source context.
- Reproducible harness committed.

### Milestone B: Source-Goal Transport Fix

Use `10654:21` as a passing regression and `11925:125` as the next failing
frontier. Implement narrow guided source transport that is general and
proof-producing.

### Milestone C: Skolemization Rule

Specify and implement one fully checked dependent Skolemization transformation
using Vampire-emitted contracts and Megalodon proof terms.

### Milestone D: Kernel Extraction

Move native clausal proof-term checking out of the large importer into a small
module with an explicit certificate AST and proof-term elaborator.

### Milestone E: Vampire Primitive Lowering

Start with one macro rule, probably unit-resulting resolution or
subsumption-resolution, and lower it in Vampire to primitive kernel steps with
complete substitutions, pivots, and result clauses.

### Milestone F: Fresh Held-Out Suite

Only after ten original-context proofs and one real macro lowering exist, run a
fresh held-out suite:

- export all candidate problems once;
- run Vampire in parallel with timeout 10;
- check certificates in parallel;
- classify each result as E1, E2, E3, or E4;
- report numerator and denominator.

## Honest Current Status

The project is progressing, but not yet converged.

What is solid:

- native certificate parsing exists;
- source-context checking is much stronger;
- dynamic `Known` insertion is rejected in counted paths;
- local hypotheses are now represented and bound into native certificate
  checking;
- checked equality-symmetry transport works for the focused source-audit
  replay case;
- theorem-local non-propositional candidate selection is now tied to live local
  variables and explicit source-map `local_type` entries, not arbitrary closed
  source subterms;
- reconstruction deltas are now merged through source aliases in the committed
  final theorem-site reconstruction path;
- generated equality source obligations are audited;
- Skolem metadata is much richer;
- recursive nested Skolem formula transport through matching logical structure
  and equality-symmetry leaves is now committed in `902f346`;
- focused native proof-term regressions pass;
- at least one original-site target reconstructs in the live native path.

What is not solid:

- there is no broad original-context suite;
- final theorem composition fails on representative larger targets;
- audited-definition delta enrichment remains an incomplete source-linking
  direction rather than a finished final-goal composition mechanism;
- Skolemization is partial;
- dependent Skolem witness applications are now recognized, but Vampire
  Skolem witnesses and Megalodon classical-choice witnesses are not yet
  transported by a checked, source-connected proof rule;
- the latest hard target still exposes a source/result binder-alignment
  problem under Skolem CPS, now around shifting the result formula beneath a
  source-only existential binder;
- Smolka-style preprocessing is partial;
- the primitive proof-object design is still not small enough;
- core proof logic is still embedded in large files;
- old structural pass counts must not be treated as proof reconstruction.

The right near-term behavior is to stop chasing broad pass counts and focus on
closing a small number of original theorem sites completely, while extracting
the primitive proof checker and moving more inference lowering into Vampire.

## Appendix: Current Commands Worth Keeping

Build:

```text
cd /project/Megalodon
TMPDIR=/project/tmp ./makeopt
```

Focused smoke:

```text
cd /project/Megalodon
TMPDIR=/project/tmp tests/vampire_certificate/run_native_cert_v1_smoke.sh
```

Source-obligation audit:

```text
cd /project/Megalodon
TMPDIR=/project/tmp JOBS=10 STRICT=1 MIN_AUDIT_PASS=1 \
  tests/vampire_certificate/run_native_cert_v1_source_obligation_audit.sh
```

Focused original-site passing target:

```text
cd /project/Megalodon
timeout 90s env TMPDIR=/project/tmp bin/megalodon -allowincompleteqed \
  -vampireaby /project/vampire-leancheck/vampire_rel_vampire/megalodon5_11094 \
  -vampireabyoutdir /project/tmp/vampireaby_live_target_10654 \
  -vampireabytimeout 10 -vampireabyschedule casc \
  -vampireabyproof megalodon \
  -vampireabynativestrict -vampireabytarget 10654 21 \
  -vampireabytargetstop \
  examples/hammer/100thms_12_h.mg
```

Focused original-site failing target:

```text
cd /project/Megalodon
timeout 120s env TMPDIR=/project/tmp bin/megalodon -allowincompleteqed \
  -vampireaby /project/vampire-leancheck/vampire_rel_vampire/megalodon5_11094 \
  -vampireabyoutdir /project/tmp/vampireaby_live_target_11925 \
  -vampireabytimeout 10 -vampireabyschedule casc \
  -vampireabyproof megalodon \
  -vampireabynativestrict -vampireabytarget 11925 125 \
  -vampireabytargetstop \
  examples/hammer/100thms_12_h.mg
```

## Final Assessment

The current branch is a better foundation than the earlier Python/textual
prototype, but it is still not the final architecture. The project should
continue, but with stricter milestone discipline:

- source-linked original theorem closure first;
- small kernel extraction now;
- Vampire-side primitive lowering before adding more Megalodon-side
  reconstruction;
- Skolemization and Smolka transformations as explicit proof-producing
  certificate rules;
- no broad pass-count claims unless they are classified by evidence class.

If the next iteration produces ten genuine original-context reconstructions,
the project will have a credible path to the requested 100. If it instead adds
more broad importer logic without that milestone, the audits' main concern
will remain unresolved.

## July 19 Second Refresh: Current Expert Report

This supplement records the second July 19 report refresh. It should be read
as historical state for that refresh, not as an additional success claim. The
earlier sections of this file preserve the historical July 19 report text and
its context. The final supplement at the end of the file is the latest state
for the current requested report.

The short version is:

- the project is closer to the audit-recommended architecture than it was at
  the start of the week;
- the direction is now mostly "Vampire explains, Megalodon checks";
- source-linking has improved materially but is not complete;
- Skolemization and Smolka-style preprocessing are the main active frontier;
- the current implementation is still too monolithic;
- there is still no qualifying 100-theorem result, and there is not yet a
  committed 10-theorem original-context gate.

The most important recent technical fact is that the large failing target
`examples/hammer/100thms_12_h.mg:11925:125` has moved through several distinct
Skolem blockers. It is no longer failing at the earlier nullary-witness-only
assumption. It is no longer failing simply because a dependent Skolem witness
such as `sK3 X0` is treated as an illegal higher-order application. It is also
no longer failing at the whole-formula equality-orientation case handled by
commit `cffc1cc`. At that refresh point the remaining observed failure was
nested: a Skolem CPS branch had source and target formulas that agreed modulo
logical structure but contained an equality leaf with swapped sides under
conjunctions, disjunctions, universals, and implications. That nested transport
helper has since been wired in, tested, and committed as `902f346 Transport
nested Skolem equality branches`; the latest remaining failure is described at
the end of this report.

### Branch and Worktree State at That Refresh

The active branches are:

```text
/project/Megalodon          vampire/megalodon5
/project/vampire-leancheck  vampire/megalodon5
```

The committed heads used for that supplement were:

```text
Megalodon          cffc1cc Orient Skolem base equality branches
Vampire fork       6f74b74bf Emit Skolem parent instantiation contracts
```

The Megalodon branch is far ahead of its remote and contains the accumulated
work of the previous branch sequence. The Vampire fork has no tracked source
modifications at this report point, but contains untracked build products and
binaries. The Megalodon worktree contains many untracked generated hammer
problems, Vampire outputs, and build/test scratch files; these are working
artifacts, not source milestones. Temporary and generated outputs used in
recent focused runs are under `/project/tmp`, consistent with the project
constraint.

At that refresh point there was one uncommitted Megalodon source patch:

```text
src/vampire_cert_v1.ml
  adds skolem_formula_transport, a recursive proof-term transport helper
```

It is included in the report because it explains the immediate direction at
that time. It has since been built, tested, and committed as `902f346`, so it
can now be counted as an incremental Skolem transport milestone, but not as a
full theorem reconstruction milestone.

### What Has Been Done

The work can be grouped into seven streams.

First, the project moved away from Python proof reconstruction as the
qualifying path. The old Python translators and validators still exist as
diagnostic tools and historical scaffolding, but they are no longer supposed
to count toward the proof-reconstruction goal. The accepted path is native
certificate emission by Vampire and native proof-term checking by Megalodon.
This follows the audits, which repeatedly warned that a large Python or
Megalodon-side heuristic reconstructor would become a second prover.

Second, Vampire now emits richer native certificate data. The relevant code is
still too concentrated inside `Shell/MegalodonChecker/MegalodonChecker.cpp`,
but the direction is right: Vampire records exact proof information while it
still has access to substitutions, sorts, selected literals, parent and result
clauses, macro-edge structure, Skolem witness metadata, and AVATAR/SAT state.
The latest Vampire commits in this phase particularly target Skolemization:
macro edges, child fields, introduced witness contracts, parent step
variables, and parent instantiation contracts are now part of the emitted
certificate vocabulary.

Third, Megalodon parses and checks the native certificate in OCaml, using
native `Syntax.tm` and `Syntax.pf` terms rather than generated textual proof
scripts as the qualifying route. The implementation is still mostly in the
large `src/vampire_cert_v1.ml` file, which is a real maintainability problem,
but the important trust-boundary direction has changed: Megalodon is asked to
check explicit certificate data, not to invent proof details after the fact.

Fourth, dynamic certificate-derived `Known` propositions were removed from
counted native paths. Earlier audit reports identified this as a critical
defect: constructing the desired proposition, registering it as a temporary
known theorem, and applying it is just a hidden bridge axiom. The current
native checking path is much stricter. Transitional modes may still exist for
debugging, but they are not qualifying evidence.

Fifth, source linking has become more concrete. The branch now distinguishes
global known facts, local theorem hypotheses, definitional facts, generated
facts, conjecture facts, unresolved facts, and `set`-command equalities.
Generated equalities from Megalodon `set` commands are not expected to have
source theorem claims; they should be reconstructed by conversion/reflexivity.
This was an important correction because treating them as ordinary missing
axioms would either fail unnecessarily or tempt the importer to introduce
bridge assumptions.

Sixth, the project started to handle original-site proof composition rather
than only detached exported problems. The focused target
`examples/hammer/100thms_12_h.mg:10654:21` is the important current passing
regression. It runs inside the original Megalodon process with a native
Vampire proof path and target stop after the reconstructed proof term. This is
not a full theorem-suite milestone because the command still uses
`-allowincompleteqed` for the larger file and is focused with
`-vampireabytargetstop`, but it is meaningful evidence that the source/context
plumbing can work at the original proof site.

Seventh, Skolemization has moved from opaque rewriting toward explicit
proof-producing contracts. The commits after the earlier July 19 report are
particularly relevant:

```text
d387edd Replay Skolem CPS under forall binders
45620c1 Replace dependent Skolem heads with lambdas
cffc1cc Orient Skolem base equality branches
```

These commits address concrete classes of failures:

- parent proofs with non-propositional step variables can remain quantified
  instead of being opened by a fake dummy object;
- dependent Skolem witnesses are represented as lambda replacements, so a
  head such as `sK3` can be replaced by a function whose application reduces
  to the chosen witness under the binder;
- direct base equality mismatches caused only by swapped sides can be solved
  with Megalodon's equality symmetry proof construction.

This is real progress on the Smolka-style preprocessing layer, but it is not
yet complete.

### Source Linking: Current State

The project is no longer merely recovering names from TPTP strings. The
current approach has several source-linking mechanisms:

- source maps identify which exported THF formulas came from which original
  Megalodon facts;
- global Megalodon facts can be opened through their existing hashes;
- local source hypotheses can be introduced as local proof assumptions instead
  of pretending to be global facts;
- reconstruction deltas merge aliases so exported TPTP names, source names,
  hashes, and definitional heads can refer to the same underlying fact;
- generated `set` equalities are intended to be proved by reflexivity or
  conversion rather than by lookup;
- final theorem-site code can propose current source terms as candidates for
  refutation instantiation without mining arbitrary closed subterms.

The strongest source-linking improvement is that the importer is now much more
careful about the provenance of facts. The July 12 and July 13 audits were
right to object that unsupported derived clauses could become assumptions.
Current strict native paths reject that style of hidden premise. The design is
now closer to the intended source proof type:

```text
GlobalKnown(hash, proposition)
LocalHyp(index, proposition)
Definitional(proposition, proof by conversion/unfolding)
Generated(proposition, proof by reflexivity/conversion or a transformation)
Conjecture(proposition, binding to the original goal)
```

This model is still not fully extracted as a clean module with a small API.
The expert should pay special attention to that. The current code does source
work, but it does not yet have a compact, auditable source-context
abstraction that can be reviewed independently of the certificate replay
machinery.

The remaining source-linking gaps are:

- the final refutation-to-original-conjecture composition is not robust on
  representative larger examples;
- source theorem definitions and exported unfolded definitions can still fail
  to meet unless the right reconstruction delta alias is present;
- generated symbols introduced by Vampire preprocessing, Skolemization,
  predicate definition, CNF, and AVATAR need a uniform generated-definition
  environment;
- source hashes, local hypotheses, generated facts, and set equalities need a
  single final audit trail for a qualifying run;
- there is no committed 10-theorem E1 suite proving that these mechanisms
  work together end to end.

### Skolemization and Smolka-Style Transformations

Skolemization is currently the main technical frontier. The paper and the
project discussions point to a Smolka-style perspective: preprocessing should
not be treated as a bag of unproved formula rewrites. It should be represented
as a checked transformation from the original source formula to the clausified
or refutation-ready formulas used by Vampire, with explicit treatment of
negation, normalization, rectification, quantifier movement, choice,
Skolemization, and CNF-like decomposition.

The current branch has started that layer but has not finished it.

What exists:

- Vampire emits Skolem formula and macro-edge metadata;
- Vampire emits child/result contract fields for Skolem branches;
- Vampire emits introduced witness symbols and their dependency information;
- Vampire emits parent step variables and parent instantiation contracts;
- Megalodon parses these fields as typed certificate data;
- Megalodon validates important consistency properties before using them;
- Megalodon can replay several focused Skolem proof-term cases;
- dependent witnesses such as `sK1 X0` and `sK3 X0` are no longer rejected
  merely because the Skolem symbol is applied to dependencies;
- Megalodon can keep context-dependent witnesses out of arity-zero global
  definitions, avoiding an earlier unsound shape;
- some equality-orientation mismatches in Skolem branch contracts can now be
  solved by a native equality-symmetry proof term.

What remains missing:

- a clean, small Skolem transformation certificate independent of the broad
  importer;
- a complete proof-producing transformation from original quantified formulas
  to the formulas consumed by the clausal core;
- a principled handling of all binder orientations and source/result variable
  alignments;
- a general treatment of nested formula transport under conjunction,
  disjunction, implication, and universal binders;
- a consistent generated-symbol environment across Skolem witnesses,
  generated predicates, predicate definitions, CNF formulas, and AVATAR split
  predicates;
- a final composition proof connecting the Skolemized refutation back to the
  original theorem statement.

The current failing target illustrates this well. In the second July 19
refresh, target `11925:125` moved from one Skolem failure class to another.
The previous step `u276` failure involved opening a parent proof with a
non-propositional variable. The recent commits moved beyond that class for
some cases by keeping quantified parent proofs and by replacing dependent
Skolem heads with lambdas. At that time the observed failure at `u273` reached
the shadow replay empty clause and entered the Skolem CPS path, but the source
and target formulas differed by nested equality orientation inside a larger
logical formula. The whole-formula equality symmetry committed in `cffc1cc`
was too shallow for this nested shape, which led to the recursive formula
transport that is now committed as `902f346`.

The right long-term lesson is not "add one more clever Megalodon heuristic."
The helper is acceptable only if it is a small checked proof constructor for a
clearly specified transformation: preserve logical structure, recurse through
matching connectives and binders, and use equality symmetry at equality leaves.
If the structures do not match, fail closed. That is a bounded proof rule, not
an open-ended search. Still, it should ultimately be driven by explicit
Vampire-emitted transformation records, not by Megalodon discovering the shape
from before/after formulas.

### Equality and Logical Constants

The current design uses Megalodon's equality and logical constants as the
trusted basis. Earlier questions asked why a private equality was being used.
That is not the intended final state. Any compatibility constants in the
certificate layer should be parsed and elaborated back to Megalodon's own
logical library.

The practical consequences are:

- equality reflexivity for generated `set` facts should be a Megalodon proof
  by reflexivity/conversion;
- equality symmetry in Skolem branch transport should use the native positive
  equality symmetry proof constructor;
- `False`, disjunction, conjunction encodings, implication, universal
  quantification, and excluded middle should come from the existing Megalodon
  logical context;
- `xm` is assumed available throughout this work;
- no final proof may rely on `aby`, because in Megalodon `aby` is an
  admission.

The current branch is broadly aligned with that direction, but there are
still implementation-level wrappers such as `vampire_and` and `vampire_or` in
certificate formulas. These should be understood as certificate-level formula
constructors that elaborate to Megalodon proof terms, not as a separate logic.

### Prover9/Ivy-Style Path

The audits recommended studying Prover9/Ivy and using that style: lower rich
prover inferences into a small set of primitive proof objects, then check
those primitives in the receiving system. The current project direction now
accepts that recommendation, but implementation has only partially caught up.

There is a core certificate specification and a growing kernel vocabulary.
There are primitive records for resolution, factoring, equality resolution,
equality factoring, substitution, paramodulation/superposition variants,
truth conflicts, and selected preprocessing or macro records. Vampire has
started emitting more exact data for complex inferences instead of leaving
Megalodon to rediscover it.

However, the project still does not have the desired clean boundary:

```text
Vampire internal inference
  -> explicit sequence of primitive kernel steps
  -> small serialized certificate
  -> small Megalodon primitive checker
```

Instead, the current implementation still has:

- a very large Vampire exporter with many special cases;
- a very large Megalodon importer/checker/elaborator module;
- partial primitive records embedded alongside transitional macro handling;
- focused proof-term constructors mixed with parsing, source auditing,
  transformation checking, and old compatibility paths.

The recommendation remains the same as in the audits:

- preserve the current implementation as a regression oracle;
- stop growing broad Megalodon-side reconstruction logic except for tightly
  bounded checked proof constructors;
- extract a small primitive certificate IR;
- move macro expansion and proof detail into Vampire;
- make Megalodon a simple checker/elaborator for those records;
- require each qualifying run to prove its final theorem in Megalodon's
  original context.

### Current Testing Evidence

The current testing evidence should be read conservatively.

Recent committed work has been checked with focused commands of this kind:

```text
TMPDIR=/project/tmp ./makeopt
TMPDIR=/project/tmp tests/vampire_certificate/run_native_cert_v1_smoke.sh
```

The key focused original-site commands are:

```text
timeout 90s env TMPDIR=/project/tmp \
  bin/megalodon -allowincompleteqed \
  -vampireaby /project/vampire-leancheck/vampire_rel_vampire/megalodon5_11094 \
  -vampireabyoutdir /project/tmp/vampireaby_live_target_10654_skolem_eqsym_20260719 \
  -vampireabytimeout 10 -vampireabyschedule casc \
  -vampireabyproof megalodon -vampireabynativestrict \
  -vampireabytarget 10654 21 -vampireabytargetstop \
  examples/hammer/100thms_12_h.mg
```

This focused target passed after the recent Skolem work. It is an important
regression and should be kept.

The representative hard target is:

```text
timeout 90s env TMPDIR=/project/tmp \
  MEGALODON_CERT_DEBUG=1 \
  MEGALODON_CERT_DEBUG_GUIDED=1 \
  MEGALODON_CERT_FAIL_FAST_SKOLEM_CPS=1 \
  bin/megalodon -allowincompleteqed \
  -vampireaby /project/vampire-leancheck/vampire_rel_vampire/megalodon5_11094 \
  -vampireabyoutdir /project/tmp/vampireaby_live_target_11925_skolem_eqsym_20260719 \
  -vampireabytimeout 10 -vampireabyschedule casc \
  -vampireabyproof megalodon -vampireabynativestrict \
  -vampireabytarget 11925 125 -vampireabytargetstop \
  examples/hammer/100thms_12_h.mg
```

At the last committed test point this target failed in the nested Skolem
transport class described above. This is useful because it is a precise,
reproducible frontier, but it is not a pass.

No expensive broad run should be used as the main iteration loop right now.
The faster pattern is:

1. Generate or reuse exported problems and certificates in `/project/tmp`.
2. Keep Vampire at a 10 second per-problem timeout.
3. Run Vampire and certificate checks in parallel when broad validation is
   needed.
4. Iterate primarily on one or a few focused cached/frontier cases.
5. Promote fixes into a small parallel regression only after the focused
   blocker moves.

This testing discipline responds directly to the user's earlier concern about
slow sequential iteration.

### Current Problems

The main problems are technical and architectural.

The first problem is incomplete original-context composition. The importer can
check pieces of the exported proof and can resolve many source facts, but the
final step from a Vampire refutation of the exported problem back to the
original Megalodon theorem is not yet robust. The system needs an auditable
source-context API and a final theorem-site gate.

The second problem is incomplete Smolka-style preprocessing. Skolemization,
rectification, FOOL transformations, CNF projection, definition folding, and
generated facts are not yet a clean proof-producing transformation layer.
There are focused successes, but no complete general pipeline.

The third problem is generated-definition consistency. Skolem symbols,
generated predicates, split predicates, predicate definitions, and CNF helper
symbols must be preserved across the certificate and proof replay. Local
context-dependent witnesses must not overwrite certificate-wide generated
definitions, and compact certificate formulas must not be silently replaced by
expanded formulas unless the replacement is itself proved.

The fourth problem is monolith growth. `src/vampire_cert_v1.ml` is too large
and contains too many responsibilities. `MegalodonChecker.cpp` is also too
large. The project has not yet achieved the clean Ivy-like separation that
the reports describe.

The fifth problem is that focused successes can be mistaken for completion.
The branch contains meaningful E2/E3 evidence and strong E4 integration
evidence. It does not yet contain the desired broad E1 evidence.

The sixth problem is that the primitive proof-object set is still not fully
stable. The current core rules are plausible, but macro transformations such
as Skolemization, AVATAR, and some superposition/demodulation families still
need explicit lowering plans and proof-producing checkers.

### Recommended Next Work

The next work should be ordered as follows.

1. Finish or revert the current uncommitted Skolem binder-shift experiment.
   The recursive nested Skolem transport helper itself has now been committed
   as `902f346`; the remaining one-line experiment shifts the result formula
   under a source-only existential binder. It should be kept only if it builds,
   passes smoke, preserves the passing `10654:21` target, and moves or closes
   `11925:125`. It must fail closed on non-matching structures.

2. Convert the nested transport idea into an explicit certificate-level rule.
   The Megalodon helper should become a checker for a small transformation,
   not a general formula-search routine. The natural rule is structural
   transport through matching binders/connectives with equality symmetry at
   leaves.

3. Extract source context into a small module. The expert-facing abstraction
   should make it obvious whether a proof uses a global known theorem, a local
   hypothesis, a generated reflexive equality, a definitional unfolding, or a
   conjecture assumption.

4. Build a committed 3-case then 10-case original-context gate. This should
   use real original Megalodon theorem sites, no `aby`, no `admit`, no
   certificate-derived `Known`, no bridge assumptions, no Python proof
   construction, and Vampire timeout at most 10 seconds.

5. Extract the primitive kernel checker. New proof work should move toward
   modules such as `vampire_kernel_syntax`, `vampire_kernel_check`,
   `vampire_kernel_elab`, and `vampire_source_context`, rather than further
   expansion of `vampire_cert_v1.ml`.

6. Move more macro expansion to Vampire. For frequent classes such as
   superposition, demodulation, subsumption resolution, AVATAR SAT traces, and
   Skolem macro edges, Vampire should emit the primitive explanation while it
   still knows the exact proof-search data.

7. Keep broad tests parallel and bounded. The 100-theorem suite should be run
   only after a smaller original-context gate is credible. Broad runs should
   use cached solvable problems, 10-20 jobs where appropriate, `/project/tmp`
   for artifacts, and a 10 second Vampire timeout.

### Suggested Expert Review Focus

An expert reviewer should inspect the project through the following questions.

1. Does any qualifying path still allow certificate-derived `Known`
   propositions or bridge assumptions?

2. Is the source-context model explicit enough to prove that every exported
   source formula is connected to an original Megalodon lemma, local
   hypothesis, definition, generated equality, or conjecture fact?

3. Is the current Skolem CPS replay a bounded proof-producing transformation,
   or is it becoming another Megalodon-side reconstruction heuristic?

4. Are generated definitions preserved consistently across Skolem,
   predicate-definition, CNF, and AVATAR boundaries?

5. Is Vampire emitting enough exact primitive data, especially for
   substitutions, literal selections, positions, directions, binder identities,
   generated witnesses, and parent/result variable mappings?

6. Is the primitive kernel small and stable enough to be reviewed like an Ivy
   proof-object checker?

7. Is there a path to a small original-context suite before any further broad
   pass-count work?

### Current Bottom Line

The project is not done. It has not produced the requested 100 proof
reconstructions. It has not yet produced a committed 10-proof
original-context milestone.

But the project is no longer on the old all-or-nothing Python skeleton path.
The current work is mostly aligned with the audit direction: richer
Vampire-side certificates, native Megalodon proof terms, stricter trust
policy, explicit source linking, and focused Smolka/Skolem transformation
proofs.

The next decisive milestone is not another structural `PASS 100`. It is a
small number of fully glued original Megalodon theorem proofs, with a
reviewable source-context trail and no hidden assumptions. If that milestone
is achieved, scaling to 100 becomes an engineering problem. If it is not
achieved, further importer growth will likely repeat the architectural
problems identified by the audits.

## July 19 Final Refresh: Latest State for Expert Review

This final refresh is the current state of the report as of the latest local
inspection on 2026-07-19. It updates the preceding historical July 19 sections
without changing the evidence classification rules.

The active branches remain:

```text
/project/Megalodon          vampire/megalodon5
/project/vampire-leancheck  vampire/megalodon5
```

The committed heads are:

```text
Megalodon          902f346 Transport nested Skolem equality branches
Vampire fork       6f74b74bf Emit Skolem parent instantiation contracts
```

The Megalodon worktree has one tracked source modification at report-write
time:

```text
src/vampire_cert_v1.ml
  in the Skolem CPS source-existential branch, pass
  (tmshift 0 1 result) to the recursive comparison/replay call
```

This is intentionally recorded as an uncommitted experiment. It is a plausible
fix for the current `11925:125` diagnostic, but it is not yet a delivered
milestone because it has not yet passed the build, smoke, passing-target, and
hard-target checks.

### What Changed Since the Previous Refresh

The nested Skolem formula transport helper is now committed. The relevant
Megalodon commit is:

```text
902f346 Transport nested Skolem equality branches
```

That commit adds bounded recursive transport for Skolem CPS formula branches.
The proof constructor descends through matching formula structure and uses
Megalodon's positive equality-symmetry proof constructor only at equality
leaves whose sides are swapped. It deliberately fails closed when the
structures do not match.

This is a better kind of Megalodon-side proof constructor than the earlier
large heuristic reconstruction code. It is still inside the large importer,
which is not ideal, but the rule itself is bounded and checkable:

- preserve matching `forall`, implication, conjunction, and disjunction
  structure;
- recurse under binders with explicit term/proof depth accounting;
- apply equality symmetry only at matching equality leaves;
- reject unsupported source/target shapes instead of searching broadly.

The validation recorded before that commit was:

```text
TMPDIR=/project/tmp ./makeopt
TMPDIR=/project/tmp tests/vampire_certificate/run_native_cert_v1_smoke.sh
focused 10654:21 original-site regression
focused 11925:125 fail-fast diagnostic
```

The build, smoke, and `10654:21` regression passed. The `11925:125` target did
not pass, but it moved from the earlier `u273` nested equality-orientation
failure to a later `u276` binder-alignment failure. That is legitimate
frontier movement, not final success.

### Current Hard Blocker

The current hard target is still:

```text
examples/hammer/100thms_12_h.mg:11925:125
```

The latest committed fail-fast run exposes step `u276`. The relevant shape is:

```text
source branch: opened under a source existential witness
result branch: no corresponding source existential binder
problem: outer dependency variables in the result formula are compared at the
         wrong de Bruijn depth unless the result formula is shifted under the
         source-only binder
```

Concretely, the diagnostic showed a leaf comparison of this form:

```text
source = (((eq prop) (_0 _1)) (forall _:prop, (_0 -> _0)))
target = (((eq prop) (forall _:prop, (_0 -> _0))) (_0 _0))
```

The nested transport rule correctly refused to accept this as equality
symmetry. It is not just a swapped equality; one side contains `_0 _1` and the
other contains `_0 _0`. The diagnosis is that the comparison context is wrong,
not that the transport rule should become more permissive.

The uncommitted one-line experiment changes the recursive Skolem CPS call
under a source existential from comparing against `result` to comparing
against `tmshift 0 1 result`. The intended meaning is simple: when the source
proof opens an existential binder that does not exist in the result formula,
the result formula must be lifted under that temporary source-only binder
before local formula comparison and proof construction.

This is the next thing to verify. If it passes the focused checks and moves or
closes `11925:125`, it should be committed as a narrow Skolem binder-context
fix. If it regresses smoke or `10654:21`, it should be reverted.

### Source Linking Status

Source linking is better than at the start of the project, but still not good
enough for the final goal.

What is now meaningfully present:

- exported source formulas are not treated as assumptions merely because a
  TPTP label exists;
- many global facts are resolved to actual Megalodon known theorem hashes;
- local facts from source maps can become local hypotheses in the native
  checking path;
- generated `set` equalities are understood as reflexivity/conversion
  obligations rather than missing source lemmas;
- reconstruction deltas are merged through aliases so that exported names,
  source names, hashes, and definitional heads can meet more often at the
  final theorem boundary.

What remains incomplete:

- there is still no compact, separately auditable source-context API that
  exposes the proof provenance of every used source formula;
- final refutation-to-original-conjecture composition still fails on
  representative larger examples;
- definitional unfolding/folding and generated symbol environments are not
  yet uniform across source facts, Skolem symbols, predicate definitions, CNF
  helper formulas, and AVATAR split predicates;
- no committed 10-case E1 original-context suite exists.

The expert reviewer should treat source linking as partially implemented. It
is no longer just string-name recovery, but it is not yet the clean original
Megalodon theorem proof pipeline requested by the project goal.

### Smolka-Style Transformation Status

The Smolka-style direction remains the right lens: preprocessing needs proof
objects, not trust. The current implementation has moved from opaque
rewriting toward proof-producing transformation fragments, especially for
Skolemization, but it has not yet reached a clean transformation calculus.

Current positive facts:

- Vampire emits substantially richer Skolem metadata than before;
- Megalodon consumes introduced witnesses, dependencies, branch contracts,
  child/result fields, and parent instantiation contracts;
- dependent Skolem witness applications are no longer rejected merely because
  the Skolem head is applied to local dependencies;
- several Skolem CPS replay cases preserve binders that previously escaped or
  collapsed;
- nested equality orientation inside Skolem branches is now handled by a
  checked recursive transport rule, not by a broad formula search.

Current negative facts:

- Skolemization is not yet a complete checked transformation from original
  formula to clausal refutation formula;
- rectification, FOOL transformations, ENNF, CNF projection, and definition
  folding still do not live in a small reviewed transformation layer;
- much of this logic remains embedded in `src/vampire_cert_v1.ml`;
- Vampire still emits rich macro metadata more often than explicit
  Prover9/Ivy-style primitive expansions;
- the current `11925:125` blocker shows that binder transport through
  Skolemization remains delicate.

### Architectural Assessment

The branch is closer to the audit recommendation than before, but the pivot is
not complete.

The good direction is clear:

```text
Vampire emits exact proof/transformation detail.
Megalodon checks small proof objects and source-context links.
No Python proof construction.
No certificate-derived Known.
No aby or admit in qualifying results.
```

The current implementation still violates the spirit of the desired final
architecture in one major way: the OCaml importer and the Vampire exporter are
large, mixed-responsibility modules. Even the better proof constructors are
mostly being added inside the monolith. The expert should therefore judge not
only whether individual focused fixes are sound, but whether the next month of
work will actually extract a small primitive checker and transformation
layer.

### Recommended Immediate Work

1. Test the current `tmshift 0 1 result` experiment:

```text
cd /project/Megalodon
TMPDIR=/project/tmp ./makeopt
TMPDIR=/project/tmp tests/vampire_certificate/run_native_cert_v1_smoke.sh
focused 10654:21 original-site regression
focused 11925:125 fail-fast Skolem diagnostic
```

2. If it passes build, smoke, and `10654:21`, and moves or closes `11925:125`,
   commit it as a narrow binder-context fix. If it fails those gates, revert
   it.

3. Extract the Skolem transport rule out of the broad importer into a small
   reviewed transformation/checking module. The rule should be stated in terms
   of source formula, target formula, binder context, and checked equality
   transport.

4. Define the source-context API explicitly, with constructors for global
   known theorem, local hypothesis, generated reflexive equality,
   definitional conversion, generated transformation fact, and conjecture
   assumption.

5. Build a very small E1 gate before any broader pass-count work:

```text
3 original theorem sites, then 10 original theorem sites
Vampire timeout <= 10 seconds
parallel only when broad validation is needed
all artifacts under /project/tmp
no aby, no admit, no certificate-derived Known, no bridge premise
```

6. Continue moving proof detail into Vampire. In particular, Skolem macro
   edges and frequent clausal inferences should lower to explicit primitive
   records while Vampire still has substitutions, positions, literal
   selections, directions, sorts, and generated-symbol provenance available.

### Final Current Assessment

The project is still incomplete. There is no 100-theorem result and no
committed 10-theorem original-context result.

The current branch is nevertheless a materially better basis for expert review
than the earlier Python/textual skeleton system. It now has stronger
Vampire-emitted metadata, native Megalodon proof-term construction, stricter
trust policy, better source-context handling, and concrete Skolem/Smolka
frontier movement.

The deciding question is whether the next implementation steps reduce the
system to a small reviewed certificate checker and source/preprocessing
transformation layer, or whether the project continues to grow a large
Megalodon-side reconstructor. The audits recommend the former, and the latest
work is partially but not yet fully aligned with that recommendation.
